# upload-program 開發憲法(CLAUDE.md)

**專案:** upload-program
**建立日期:** 2026-07-25 15:23
**最後更新:** 2026-08-31 16:40
**版本:** v2.8

> 本檔為本專案的最高開發準則,所有開發行為(含 AI 協作)必須遵守。
> **本專案內**與其他文件衝突時,以本檔為準;修改本檔需在文末「版本歷史」留下紀錄。
> 例外見〈位階〉:平台技術規約的偏離權不在本專案。

---

## 專案概述

- **系統:** upload-program — 公司內部**開發者程式分享平台**(簡易型 GitHub)
  登入後建立專案,在專案內上傳**原始碼、執行檔、程式文件**,以版本為單位發布、瀏覽、搜尋、下載。
- **技術:** Python 3.12 + FastAPI + PostgreSQL + MinIO(物件儲存),部署於 Cats 共用 VM
- **開發藍圖:** 一切修正與新功能依 [docs/開發計畫書.md](docs/開發計畫書.md) 與
  [docs/任務表.md](docs/任務表.md) 的里程碑(M1–M7)與任務編號(T01–T26)執行
- **正式資料:** 尚未上線,目前無正式資料。**一旦上線,任何動到資料庫的操作,動手前必先備份並於 staging 演練**
- **MVP 不做:** git 協定(clone/push)、PR / code review、issue、線上編輯、CI 建置、外網公開

---

## 位階(本專案與平台的關係)

```
平台技術規約 ── platform-charter / SSO 接入契約 / Cats 新服務接入指南
        │        管「做出來的東西要長怎樣」;偏離鐵則需 Platform 團隊同意,本專案無權自行豁免
        ▼
本開發憲法(十條)
        │        管「怎麼把東西做出來」;本專案內最高
        ▼
docs/開發計畫書.md → docs/任務表.md → docs/dev-logs/
```

兩者管轄不重疊。本檔「以本檔為準」的效力**限於開發流程**;平台鐵則(non-root、RS256、
業務庫只存 sub、log 走 stdout、禁 latest tag…)不因本檔而鬆動。

---

## 第一條:敏捷式開發

1. 以**小步快跑**為原則:每個任務(Tnn)是一個獨立可交付、可回滾的增量,不做大爆炸式改動。
2. 依任務表的里程碑順序推進;每完成一個任務即更新任務表狀態(⬜→🔵→✅)。
3. 每個增量完成的定義(Definition of Done):
   - 驗收標準達成
   - 測試全綠(M2 之後含 CI 綠燈)
   - 開發日誌已寫(見第六條)
   - 相關文件已更新版本與歷史(見第七條)
4. 需求變更不直接改程式:先更新開發計畫書/任務表,再依新任務執行。

## 第二條:先寫開發計畫

1. **任何開發動作之前,必須先有書面計畫**。無計畫不動工。
2. 小任務:**動工前**先在 `docs/dev-logs/` 建立該任務的日誌檔,寫妥「目標、影響範圍、驗收標準、
   回滾方式」四項計畫段落,完工後在同一檔補上結果——不得事後補寫計畫段。
   (若確實發生事後補寫,必須在日誌開頭明確標示「**本篇為事後補記,違反第二條**」,不得偽裝流程跑過。)
3. 大任務(新功能、資料遷移、重構):先在 `docs/plans/` 建立獨立計畫文件,經確認後才動工。
4. 計畫必須明確標註**對現有資料的影響**(不動 / 加欄位 / UPDATE / 刪除),分級 🟢🟡🔴。
5. 執行中發現計畫與現實不符:除了記錄於日誌與任務表,**必須回寫修正到來源計畫文件**並升版,
   不得讓計畫書留著已知錯誤的內容。

## 第三條:依需求先寫測試(TDD)

1. **先寫一個「現在會失敗」的紅測試**,把預期行為釘住,才開始修改程式;修正完成的定義就是測試轉綠。
2. 測試永久保留在 `tests/`,納入 CI 防止回歸。
3. 測試資料一律在 fixture 現造假資料;**嚴禁**將任何真實個資、正式資料匯出檔放進測試與 CI。
4. 純重構可不寫新測試,但**既有測試必須維持全綠**作為安全網。
5. 「測試全綠」指**實際跑過並看到綠燈**;未執行的測試不得宣稱通過。

## 第四條:文件一律 Light 版本,md 與 HTML 並存

1. 所有產出的文件(HTML 報告、圖表、簡報、匯出頁面)**一律使用淺色(light)主題**,白底深字。
2. 禁止使用深色(dark)背景配色;不使用 `prefers-color-scheme: dark` 自動切換為深色。
3. 圖表配色以淺色背景可讀為準。
4. **正式文件必須同時提供 Markdown 與 HTML 兩種版本**(開發計畫書、任務表、架構/設計文件、SOP、報告):
   md 為權威來源(進版控、供審閱),HTML 為單檔可下載的發布版;兩版內容必須同步更新,歧異時以 md 為準。
   開發日誌等內部紀錄可僅提供 md。
5. **HTML 版含架構圖/流程圖等圖示時,一律以內嵌 SVG 呈現**(light 配色、無外部依賴,單檔離線可開);
   md 版以對應的文字圖(ASCII)呈現即可。
6. HTML 版由 `python tools/render_docs.py` 自 md 產生,避免兩版人工同步而漂移;
   圖示以 `<!--SVG:名稱-->` 標記,產生時抽換為 `docs/assets/名稱.svg`。

## 第五條:必須寫註釋

1. 所有新增或修改的程式碼必須附上**中文註釋**,說明「為什麼這樣做」(意圖與限制),而不只是「做了什麼」。
2. 函式/類別必須有 docstring:用途、參數、回傳值、副作用(特別是會動到資料庫或物件儲存的)。
3. 複雜邏輯(串流上傳、magic bytes 判型、權限判定、資料遷移)必須逐段註釋。
4. 修 bug 時在修正處註明根本原因,避免後人改回去。

## 第六條:寫完後必須寫開發日誌

1. 每完成一個任務,在 `docs/dev-logs/` 新增一篇開發日誌,檔名格式:`YYYY-MM-DD_Tnn_簡述.md`
   (例:`2026-07-25_T01_專案骨架.md`)。
2. 日誌必要內容:
   - **日期時間**與**對應任務編號**
   - 做了什麼(異動檔案清單)
   - 為什麼這樣做(決策理由、捨棄的替代方案)
   - 測試結果(紅→綠的證據)
   - 對現有資料的實際影響
   - 遺留問題與後續建議
3. 當天未完成的工作也要寫進度日誌,標明中斷點,方便接續。

## 第七條:文件必須標註日期時間、版本與歷史

1. 所有文件(計畫書、任務表、開發日誌、README、設計文件)開頭必須有:

   ```markdown
   **建立日期:** YYYY-MM-DD HH:MM
   **最後更新:** YYYY-MM-DD HH:MM
   **版本:** vX.Y
   ```

2. 文件末尾必須維護「版本歷史」表:

   ```markdown
   ## 版本歷史
   | 版本 | 日期 | 修改人 | 摘要 |
   |---|---|---|---|
| v2.6 | 2026-08-25 08:50 | Claude(Benny 裁示 T99 三題) | **紅線同步:L1b 通知用信箱依契約通則落地**(T99)。原文寫「業務庫只存 sub,不存 email/姓名/密碼」,而 T59 起已依 §4.2a **L1 通則**存了顯示名稱、T99 起依 **L1b 通則**存通知信箱 —— 🔴 **紅線與實作互相打架比沒有紅線更危險**,所以據實改寫:兩者都是**通則落地**(不是個案例外、不需申請),並逐條列出必須全滿足的條件(只信 `email_verified=true`、不得顯示在任何頁面、不得作 join key、寄信失敗不得阻斷業務流程、須有清除工具)。**密碼不落地那一條沒有例外,原封不動**。⚠ **本列原編為 v2.4,2026-08-31 合併 PR #33 時改編為 v2.6** —— 該分支自 v2.3 切出,不知道另一條線也用了 v2.4(它的 v2.4 是第九條、v2.5 是第十條)。**內容一字未改,只改版號**;沿用會讓本檔出現兩個 v2.4,而版本歷史一旦撞號就再也分不出誰在前 |
   | v1.0 | 2026-07-25 | Benny | 初版 |
   ```

3. 版本規則:內容實質變更升次版號(v1.1→v1.2),結構性/方向性大改升主版號(v1.x→v2.0)。
4. 每次修改文件,**必須同時更新「最後更新」時間與版本歷史**,缺一不可。

---

## 第八條:發版儀式(Release 必附 title 與 content)

1. **每次發布 Release 必須提供 title 與 content**,不得只推一個裸 tag。
   Release 頁是使用者與未來的自己唯一會看的「這一版是什麼」——tag 本身說不出任何事。
2. **title 格式:** `vX.Y.Z — 一句話重點`(例:`v0.1.6 — 發布三類齊備、使用教學頁、首頁版本號`)。
3. **content 必含四段**(缺一不可):
   - **本版內容**:任務編號 + 一句話(T65 發布三類齊備、T66 使用教學頁…)
   - **對現有資料的影響**:🟢 不動 / 🟡 加欄位 / 🔴 UPDATE 或刪除;**有無 migration**
   - **部署注意**:`.env` 是否新增變數、是否需跑 `alembic upgrade head`、換版後的驗證點
   - **測試**:該版的測試總數與 CI 綠燈狀態
4. 🔴 **`app/version.py` 的 `APP_VERSION` 必須在打 tag 之前隨 PR 改成該版**。
   版本號顯示在服務首頁(T68),tag 與常數不一致等於對使用者說謊,換版驗證也會判錯。
5. **同一個版本 tag 不重發**;發錯就發下一個 patch 版(v0.1.3 事故的結論)。
6. 發版前一分鐘檢查(runbook §A.1):`alembic history` 驗 migration 鏈、
   有 migration 的版本本機 up→down→up 演練過。

---

## 第九條:文件識別與跨專案文件規約

> 第七條管「文件有沒有日期與版本」,本條管「文件離開 repo 之後還認不認得出自己是誰」。
> 文件會被當附件寄出、列印、貼進聊天視窗——那時候只剩抬頭與檔名能自證身分。

1. **每份文件抬頭必須標明專案**,與第七條三項並列:

   ```markdown
   **專案:** upload-program
   **建立日期:** YYYY-MM-DD HH:MM
   **最後更新:** YYYY-MM-DD HH:MM
   **版本:** vX.Y
   ```

2. **跨專案文件**(要交付給其他專案/團隊的:申請、通報、回覆、聲明、同步、交付)
   抬頭再加三欄,合計四欄缺一不可:

   ```markdown
   **專案:** upload-program
   **發文專案:** upload-program        ← 給予方(誰發的)
   **受文專案:** cats-portal           ← 授予方(發給誰)
   **發文時間:** YYYY-MM-DD HH:MM      ← 第一次送出的時間,不隨改版變動
   ```

   `發文時間` 與 `最後更新` 是兩件事:前者供雙方對帳「這是哪一封」,
   後者供本方追版本。改版時只動後者。

3. 🔴 **跨專案文件的檔名必須自己說得出「誰發給誰、什麼時候」**:

   ```
   <發文專案>_致_<受文專案>_<類別>_<主題>_<YYYYMMDD-HHmm>.md
   ```

   - 類別限六種:`申請` / `通報` / `回覆` / `聲明` / `同步` / `交付`
   - 例:`upload-program_致_cats-portal_回覆_session時序_20260812-1140.md`
   - **理由**:對方收到的往往只有一個檔名。內文寫得再清楚,檔名說不出話,
     對帳就得靠人腦。(實例:收到過 `catsportal____uploadprogram_____.md`,
     中文全被傳輸吃掉只剩底線——這種檔名連是誰發的都認不出來。)

4. **收到的他方文件**放 `docs/plans/inbox/`,同格式(來源在前,故自然依來源分群),
   收到什麼原樣保存,**不改內容**;我方的判讀另立回覆文件,不寫進對方的檔案。

5. **每份跨專案文件必須登記進 `docs/plans/跨專案文件索引.md`**——
   一頁看完「發過什麼、給誰、何時、對方回了沒」。索引是「哪些算跨專案文件」的
   唯一登記處,規則不散成兩份。

6. ⚠️ **本條對「生效後新建的文件」有強制力;既有文件不回頭改名。**
   舊檔被開發日誌大量引用,為了套用新規則去回頭改寫日誌,等於拿新規則竄改舊歷史
   (第六條精神反對)。既有檔案以索引補齊 metadata,新舊之間的橋是索引不是改名。

7. 本條由 `tests/test_doc_conventions.py` 自動守門(抬頭欄位、檔名可解析、
   索引不漂移)。**規約若沒有測試,三個月後就是一句沒人記得的話。**

---

## 第十條:指令必須標明「在哪台機器、在哪個目錄」

> 本專案的文件裡同時躺著**三種機器**的指令,而它們長得一模一樣——
> 黑底、`$` 開頭。貼錯機器的代價不對稱:在開發機跑到 VM 的指令頂多報錯,
> **在 VM 跑到開發機的指令會出事**。

1. **每個可貼的指令區塊,正上方必須標明機器與工作目錄**,三選一:

   ```markdown
   > 🖥️ **在哪執行:** WSL · 工作目錄 `~/upload-program-`
   > 🖥️ **在哪執行:** Cats VM(ssh)· 工作目錄 `/opt/upload-program`
   > 📄 **編輯哪個檔:** `/opt/upload-program/docker-compose.yml`
   ```

2. 🔴 **`🖥️ 執行` 與 `📄 編輯` 必須分清楚。**
   這是**已經出過事**的分界:2026-08-11 換版第一次失敗,根因就是
   「改 `docker-compose.yml` 的 image tag」那一步**是編輯檔案,
   卻被當成 shell 指令貼進終端機**——結果 pull 拉到的仍是舊版,
   而容器顯示 Running,看起來一切正常。

3. 工作目錄一律寫**絕對路徑或 `~` 起頭**。
   不寫「專案根目錄」「部署目錄」這種要讀者自己換算的話——
   要換算就有機會算錯,而算錯不會有錯誤訊息。

4. 在聊天中回覆指令時**同樣適用**:一次給多段指令時,每一段都要說在哪跑。
   使用者不會記得三段前那句「以下都在 VM 上」。

5. 本條由 `tests/test_doc_conventions.py` 守門(runbook 的每個 `bash` /
   `cron` 區塊都要有標記,且標記要指名得出實際機器)。
   **忘記標不會有任何錯誤訊息**,文件會慢慢退回原狀而沒人看得見——
   與第九條同一課。

---

## 第十一條:多目標工作線的彩色標示,以及佔位符不得混進指令

> 第十條管「**一個指令在哪下**」。本條管「**一條橫跨多個目標的工作線,
> 怎麼讓人不會走錯目標**」。兩者疊加,不互相取代。

### 三層的關係(先講清楚,否則會被當成三套規則)

| 層 | 管什麼 | 形式 | 在哪 |
|---|---|---|---|
| 一 | 單一指令的機器 / 路徑 / 專案 | `【機器】【路徑】【專案】` | 第十條 |
| 二 | 那個動作是**執行**還是**編輯** | `> 🖥️ 在哪執行:` / `> 📄 編輯哪個檔:` | 第十條 2 |
| **三(本條)** | 一條線橫跨 ≥2 個目標時,哪一步在哪個目標 | 顏色方塊 + 步驟標題 | 本條 |

1. **一條工作線涉及 ≥2 個資料夾/目標時,開頭先給圖例。**
   顏色 = 資料夾(對話介面沒有文字顏色,所以用彩色方塊):🟦 🟨 🟥 🟩 🟪。

2. **對映一經指定,整條工作線不得改變。**
   已完成的目標標 ✅ 並**明說「不會再叫你進去」** ——
   少了那句話,讀的人每一步都要重新確認自己該在哪個目錄。

3. 🟩 **綠色固定保留給「GitHub 網頁(瀏覽器)」**,與終端機動作區分。
   🔴 這一格不得挪用給別的資料夾:瀏覽器與終端機貼錯的後果不同,
   而顏色的用途就是讓那條界線**不必用眼睛讀字**。

4. **每個步驟標題 = 三連色塊 + 目標名 + 路徑 + 執行處**:

   ```
   🟦🟦🟦 步驟 1|workOrder runner|~/actions-runner-workorder|WSL 終端機
   ```

   三連而非單一色塊:單一方塊在長訊息裡掃不出來,三連才形成看得見的色帶。

5. 🔴 **佔位符不得混在可貼的指令裡。**
   指令區塊**要嘛整段可以直接貼**,要嘛把要替換的地方寫成**顯眼的中文詞**
   (例:`貼token`),並在區塊外單獨交代怎麼取得。

   > **為什麼這是紅線而不是排版偏好 —— 2026-08-31 實際踩到:**
   > `<拿新token>` 被整段貼上,**`<` 被 bash 當成重導向**,指令當場炸掉。
   > 尖括號是程式文件的佔位符慣例,而**那個慣例在 shell 裡會產生語法**。
   > 中文詞不會被 shell 解讀,而且貼上去一定會失敗得很明顯 ——
   > **「明顯地壞掉」勝過「安靜地做了別的事」**(與第十條 2 的執行/編輯之分同一個道理)。

6. ⚠ **顏色本身不帶語意。** 它只是資料夾的代號,所以「哪一個不能動」
   仍然要寫成字(例:🟥 `~/actions-runner` **別動**)——
   不要指望讀的人記得紅色代表危險。

7. 本條與第十條同樣**只治理對話回應與 runbook 類文件的可貼區塊**;
   程式碼註解不受此限。

## 平台整合契約(權威在 cats-portal,勿在本 repo 另立版本)

- **SSO 接入**:`cats-portal/DOCS/帳號系統接入契約_SSO.md`——接 Keycloak 前必讀
  (RS256/JWKS、業務庫只存 sub、首登「待開通」deny-by-default、single logout、PKCE)。
  **本專案已對齊版本:v2.0(2026-07-31)**——v2.0 新增 §10 單一登出(SLO)接入義務,
  我方 session 為 cookie 形態故**無豁免空間**,front-channel logout 端點已實作(T74),對照結果見
  [dev-logs/2026-07-28_T51_SSO契約v1.7對齊.md](docs/dev-logs/2026-07-28_T51_SSO契約v1.7對齊.md)。
  🔴 v1.5 起 **IdP 與各 App 同源**(`catsapp.sporton.com.tw/auth/...`),
  §4.10 的額外義務(cookie Path、嚴格 CSP、前端 base path、禁 token 進 localStorage)
  **對本專案有強制力**——同源之下,我們出現 XSS 等於全平台帳號淪陷。
- **gateway / cats-edge**:`cats-portal/DOCS/App服務對齊指南_portal-gateway整合.md`
  ——改 compose network、部署重建(先 disconnect→up→reload gateway)、要新增/修改路由時必讀。
- **gateway 變更公告**:`cats-portal/DOCS/gateway變更通知_D8接管.md`(portal-gateway 已接管 80/443)。
- **接入實務**:Cats 平台新服務接入指南 v2.0(容器化架構與資料)。
- 上述文件之修改一律走 cats-portal 的 PR;本 repo **只引用、不複製維護**(避免兩真相)。
- Claude session 要讀最新版:`add repo fttp0165/cats-portal` 後讀 `DOCS/` 對應檔案。

---

## 附則:本專案紅線

**平台鐵則(偏離需 Platform 同意)**

- 🔴 不自建帳號系統、不存密碼、不自簽 JWT——一切走 Keycloak;JWT 必須 RS256、公鑰取自 JWKS。
- 🔴 業務庫**只存 `sub`** + 本地角色。PLM 的姓名快取是 SSO 契約 §6.1 具名特例,
  **本專案不繼承**。
  ⚠ 兩個**依契約通則 §4.2a 落地**的快取欄位(不是個案例外,不需申請):
  **L1 顯示名稱**(`display_name_cache`,T59)與 **L1b 通知用信箱**(`notify_email`,T99)。
  兩者都必須滿足通則的全部條件 —— 只從本人 ID token 取得、每次登入覆寫(含覆寫成 NULL)、
  不進 log、nullable、有整批清除工具。
  ⚠ **2026-08-31 契約升 v3.4:L1 的顯示範圍放寬**(Benny 裁決准我方 2026-08-12 的申請)——
  顯示名稱得出現在**本服務的 HTML 頁面**,含管理後台**與專案 / 版本 / 內容頁的
  擁有者、作者、上傳者辨識**(T132)。🔴 **放寬的只有「顯示在哪」**:
  仍**不進 API 回應**、🔴 **不得再散布至本服務以外**(不寄信、不匯出、不轉發)、
  不得作授權依據、不得進 log;沒有名字時**退回 `sub` 前 8 碼**(契約第 6 項要求)。
  🔴 **L1b 一個字未放寬**:🔴 **只信 `email_verified=true`**、
  🔴 **不得顯示在任何頁面**(含管理後台清單)、🔴 不得作 join key 或授權依據、
  🔴 寄信失敗不得阻斷業務流程 —— 姓名回答「這是誰做的」,信箱回答「怎麼聯絡他」,
  後者放上頁面沒有業務理由,只是外洩面。**密碼永遠不落地,這一條沒有例外。**
- 🔴 容器 non-root(UID 1000)、EXPOSE 8080 綁 `0.0.0.0`、`/health` 不查 DB、SIGTERM 30 秒優雅關閉。
- 🔴 DB / MinIO **不上 `cats-edge`、不對主機發布 port**;`cats-edge` 寫進 compose 且 `external: true` + `name: cats-edge`。
- 🔴 production compose 不用 `latest` tag;部署是改 tag 後 pull,不在正式機 `git pull && docker build`。
- 🔴 換版重建對外容器後**必須** `docker exec portal-gateway nginx -s reload`,否則 502。
- 🔴 **絕不啟動舊的 `nginx-gateway`**——它會搶 80/443,全平台一起掛。
- 🔴 log 走 stdout、JSON 單行,不記密碼 / 完整 JWT / 個資;錯誤回應一律 RFC 7807。

**本專案自我加嚴(散布可執行檔的代價)**

- 🔴 上傳檔案必須驗 **magic bytes**,不信副檔名、不信前端送的 Content-Type;HTML/SVG 一律拒收。
- 🔴 下載一律 `Content-Disposition: attachment` + `X-Content-Type-Options: nosniff`,
  絕不讓上傳內容在本服務網域被瀏覽器執行。
- 🔴 每個檔案保存 SHA-256 供使用者校驗;掃毒未接上前,`scan_status` 必須誠實標示 `not_scanned`。

**git 紅線**

- 🔴 secret、`.env`、私鑰絕不進 git(`.env.example` 只列變數名)。
- 🔴 `*.sql`、`*.xlsx`、上傳檔目錄、`node_modules/`、`.venv/` 不進 git。
- 🔴 動資料的 migration 必須提供 backward(可回滾),且先在 staging 雙向演練。

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v2.8 | 2026-08-31 16:40 | Claude(Benny 裁決 A 案) | **紅線同步契約 v3.4:L1 顯示名稱的顯示範圍放寬**(得出現在本服務 HTML 頁面,含專案/版本/內容頁的擁有者、作者、上傳者辨識;T132)。🔴 **放寬的只有「顯示在哪」** —— 仍不進 API 回應、🔴 新增「不得再散布至本服務以外」、仍不得作授權依據、仍不得進 log,沒名字時退回 `sub` 前 8 碼(契約第 6 項要求)。🔴 **L1b 一個字未放寬**(仍不得顯示在任何頁面):姓名回答「這是誰做的」,信箱回答「怎麼聯絡他」,後者放上頁面沒有業務理由只是外洩面。**紅線與實作打架比沒有紅線更危險**,所以契約一改這裡就跟著改 |
| v2.7 | 2026-08-31 15:10 | Claude(Benny 指示入憲) | **新增第十一條〈多目標工作線的彩色標示,以及佔位符不得混進指令〉**。第十條管「一個指令在哪下」,本條管「**一條橫跨多個目標的工作線怎麼讓人不會走錯目標**」:顏色=資料夾(🟦🟨🟥🟩🟪)、對映整條線不變、已完成標 ✅ 並**明說不會再叫你進去**、🟩 固定保留給 GitHub 網頁(瀏覽器與終端機貼錯的後果不同)、步驟標題=**三連**色塊+目標名+路徑+執行處(單一方塊在長訊息裡掃不出來)。🔴 **佔位符不得混在可貼指令裡** —— 2026-08-31 實際踩到 `<拿新token>` 被整段貼上、**`<` 被 bash 當成重導向**而炸掉:尖括號是程式文件的佔位符慣例,而**那個慣例在 shell 裡會產生語法**;改用不會被 shell 解讀的中文詞(如 `貼token`),因為**「明顯地壞掉」勝過「安靜地做了別的事」**。⚠ 明載**顏色本身不帶語意**(哪一個不能動仍要寫成字)。依「新增條文往後加」放在第十條之後,既有十條一字未動 |
| v2.5 | 2026-08-12 21:00 | Claude(Benny 裁示) | **新增第十條〈指令必須標明「在哪台機器、在哪個目錄」〉**:文件裡同時有 WSL 開發機 / Windows / Cats VM 三種機器的指令,長得一模一樣但貼錯的代價不對稱(在 VM 跑到開發機的指令會出事);規定每個可貼區塊正上方標 `🖥️ 在哪執行` 或 `📄 編輯哪個檔`,🔴 執行與編輯必須分清楚——那正是 2026-08-11 換版失敗的根因(改 image tag 是編輯檔案,卻被當成 shell 指令貼進終端機);工作目錄一律絕對路徑或 `~` 起頭,不寫「專案根目錄」這種要讀者換算的話;聊天回覆指令同樣適用;由 `tests/test_doc_conventions.py` 守門 |
| v2.4 | 2026-08-12 17:10 | Claude(Benny 裁示) | **新增第九條〈文件識別與跨專案文件規約〉**:所有文件抬頭加 `專案`;跨專案文件加 `發文專案 / 受文專案 / 發文時間`(發文時間不隨改版變動,供雙方對帳);🔴 檔名格式 `<發文專案>_致_<受文專案>_<類別>_<主題>_<YYYYMMDD-HHmm>.md`(類別限六種);收到的他方文件原樣存 `docs/plans/inbox/`;一律登記進跨專案文件索引;⚠️ 只對生效後新建文件有強制力,既有檔案不回頭改名(避免以新規則竄改舊日誌),以索引為橋;由 `tests/test_doc_conventions.py` 自動守門 |
| v1.0 | 2026-07-25 15:23 | Claude(Benny 授權) | 初版:平台整合契約指標段落 + 本 repo SSO 接入狀態與紅線 |
| v1.1 | 2026-07-25 15:35 | Claude(Benny 授權) | 補上專案定位(簡易型 GitHub)與 MVP 範圍界線;設計細節移至設計文件 |
| v2.3 | 2026-07-31 23:00 | Claude(Benny 授權) | 平台整合契約段落記錄**已對齊 SSO 契約 v2.0**(原 v1.7):v2.0 新增 §10 單一登出義務,我方 session 為 cookie 形態不主張豁免,front-channel logout 端點隨 T74 實作 |
| v2.2 | 2026-07-31 17:50 | Claude(Benny 裁示) | **新增第八條〈發版儀式〉**:每次 Release 必附 title 與 content(title 格式、content 四段:本版內容 / 資料影響 / 部署注意 / 測試);🔴 `APP_VERSION` 須在打 tag 前隨 PR 改好(版本號顯示於首頁,T68);重申同版不重發與發版前 migration 鏈檢查 |
| v2.1 | 2026-07-28 02:06 | Claude(Benny 授權) | 平台整合契約段落記錄**已對齊 SSO 契約 v1.7**(原為 v1.0),並標明 v1.5 起 IdP 與各 App **同源**、§4.10 額外義務對本專案有強制力;對照結果與缺口另立 T51–T53 |
| v2.0 | 2026-07-25 15:53 | Claude(Benny 授權) | **改版為開發憲法**:採用 PLM_HY 七條並在地化(tests/ 取代 projects/tests.py、FastAPI 取代 Django、本專案自己的 M1–M7 / T01–T26);新增〈位階〉界定平台技術規約與本憲法的管轄分界;第二條增訂事後補寫須明示違反;第三條增訂「測試全綠指實際跑過」;第四條增訂 HTML 由 tools/render_docs.py 產生以防兩版漂移;紅線重編為平台鐵則 / 自我加嚴 / git 三組;依第七條補上日期、版本與本歷史表 |
