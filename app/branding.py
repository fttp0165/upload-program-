"""服務對使用者顯示的名稱——**單一來源**。

改名之前這個字串散在五個地方(導航列品牌、側欄副標、首頁大標、教學頁、
OpenAPI summary),改一次要同步五處;與 T68 那個停在 `0.1.0` 五個版本的
寫死版本號是同一類問題,所以收成一份。

🔴 這**不是**技術識別名。`upload-program` 才是平台登記的服務名、gateway 路徑
(`/upload/`)、GHCR image 名、compose 服務名與 log 的 `service` 欄位;
行銷名稱可以改,技術識別改了要 portal 重做路由與白名單。兩者刻意分開。
"""

SITE_NAME = "AI 小程式分享平台"
