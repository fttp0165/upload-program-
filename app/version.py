"""本服務的版本號——**單一真相**。

🔴 這個常數必須在**打 tag 之前**隨 PR 改成該版(憲法〈發版儀式〉),
tag 與這裡不一致的話,首頁會對使用者說謊,換版驗證也會判錯。

為什麼放在程式裡而不是讀 env:版本是「這份程式碼是哪一版」的事實,
不是部署時可以覆寫的參數——env 覆寫等於允許同一份 image 自稱不同版本。
"""

APP_VERSION = "0.2.0"
