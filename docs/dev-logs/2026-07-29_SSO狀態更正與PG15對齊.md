# SSO 狀態更正(施工單 v1.1/v1.2)與 PG15 對齊

**建立日期:** 2026-07-29 07:30
**最後更新:** 2026-07-29 07:30
**版本:** v1.0
**對應任務:** T26/T28 狀態回寫(第二條 5)、T54 漏抓補正、T27 runbook 增補

> **本篇為文件回寫 + 一處 compose 修正**,起因是 portal 的兩版文件更新
> 與 client 建置腳本的實際執行輸出。

## 收到的三項新事實

1. **裁決函 v1.1 / 施工單 v1.1 更正:Keycloak 早已上線**(2026-07-27 起,
   realm `sporton`,已有三個 client)。施工單 v1.0 寫「卡 Platform P0-A」是
   照抄前置清單而未核對實際進度——portal 自己更正了。
   我方文件全面跟著錯(SSO 接入計畫 v1.3、任務表 v1.28–29、回覆文件、
   開發計畫書 v1.3 都記著「唯一阻擋項是 Keycloak 部署」)。
2. **施工單 v1.2 新增 §3.0:施工順序反轉。** nginx 對寫死主機名的上游是在
   **載入設定當下**解析,容器不存在不是回 502,而是 `[emerg]` 讓整份設定
   載入失敗、gateway 起不來 = 全平台中斷。所以 portal 的 `/upload/` 路由
   **刻意保持註解**,正確順序是:**我方容器先上線 → `getent` 確認解析 →
   portal 才解除註解 + 低峰 reload**。我方不需要等 portal 的路由才能部署。
3. **client 已實際建立**(Benny 貼出的執行輸出):冪等腳本執行完畢、回讀
   驗證通過(confidential + PKCE S256、禁 implicit/password、redirect URI
   完整比對無萬用字元);secret 交付檔已產出(600 權限),待安全管道交付。

## 做了什麼

| 類型 | 檔案 | 內容 |
|---|---|---|
| 修正 | `docker-compose.yml` | 🔴 `postgres:16.4` → `postgres:15.8`——施工單 §3 明載「全平台統一 PG15」,**T54 逐項核對時漏抓**(當時把注意力放在三項硬性約束上)。尚未上線,不涉任何資料遷移 |
| 增補 | `docs/runbook_換版與備份還原.md` v1.1 | 新增 **§A0 首次上線**八步(secret 收取、bootstrap sub 可後填、容器先上線、getent 確認、portal 解除註解、冒煙、掛 cron) |
| 回寫 | `SSO接入計畫.md` v1.4、`任務表.md` v1.30、`回覆_portal施工單.md` v1.1、`待portal提供資訊.md` v1.3、`開發計畫書.md` v1.4 | 移除所有「卡 Keycloak / P0-A」的記載(以刪除線保留更正痕跡)、記錄 client 已建立與順序反轉;`待portal提供資訊` 補 G6(PG15)、G7(施工順序);開發計畫書 §7 的「PostgreSQL 16」一併對齊 15 |

版本歷史裡的舊記載(任務表 v1.28 等)**不抹除**——那是當時的事實記錄,
更正由新版本條目承載。

## 測試結果

程式碼零改動(只動 compose 與文件);`docker compose config -q` 通過、
329 passed、ruff 全綠、`render_docs --check` 通過。

## 對現有資料的影響

🟢 無。PG 主版變更發生在尚未存在的部署上。

## 目前的真實狀態(回寫後)

- **沒有任何外部阻擋。** 下一步是:secret 安全管道交付 → 我方照 runbook §A0
  部署容器 → portal 解除註解 reload → T29 冒煙。
- 球的順序:portal(交付 secret)→ **我方**(容器上線)→ portal(reload)→ 我方(冒煙)。

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-07-29 07:30 | Claude(Benny 授權) | 初版:三項新事實(IdP 早已上線、施工順序反轉、client 已建立)、PG15 漏抓補正、五份文件回寫清單 |
