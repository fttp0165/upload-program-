# T63 T2.0 groups claim 通知對齊(只讀不判)

**建立日期:** 2026-07-31 09:00
**最後更新:** 2026-07-31 09:00
**版本:** v1.0
**對應任務:** T63

---

## 計畫與結果(小任務,一次記錄;動工前後同日)

**輸入:** portal 通知(2026-07-31):`groups` claim 已就緒(字串陣列、full.path=true、
47 節點),🔴 但**尚無人被指派**——現在以 `/svc/upload` 判開通會把所有使用者
(含正在用的)鎖在外面,症狀像 SSO 壞掉。裁示:只讀不判,啟用另行通知。

**現況盤點:** 本服務**完全不讀 groups**(驗 token 只取 sub/name),
維持管理員後台開通——已符合裁示,無需改程式。

**做了什麼:** 新增 `tests/test_groups_claim.py` 4 條**防禦測試**,
把「不准提早接」釘成紅綠燈:
1. pending 帶 `/svc/upload` 登入仍是 pending(不自動開通)
2. active 帶 groups 照常通行
3. groups 空陣列/整個不存在都能承受(portal §5 兩種形態)
4. User 模型無 groups 欄位——§4.2a **L2 判斷用屬性不得快取**,以結構保證

**測試:** 4 條新增全綠,全套 **353 passed**、ruff 綠。
**對現有資料的影響:** 🟢 純測試。
**回滾:** git revert。

## 遺留與後續

- 等 portal 通知 `/svc/*` 啟用時機;屆時若接「自動給最小預設角色」另立任務
  (契約 §4.4 明定為**可選**,是自願接受訊號不是被迫)
- 若日後做主管介面(L2):分段比對、連 `/` 一起比、空窗期寫進文件
  (portal 裁決 §7 兩項硬條件)
