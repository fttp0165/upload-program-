# 明日待辦(2026-08-02):v0.2.0 發版與換版

**建立日期:** 2026-08-01 03:50
**最後更新:** 2026-08-01 03:50
**版本:** v1.0
**承接:** [2026-08-01 日結](2026-08-01_日結_問題回報系統與v0.2.0發版準備.md)

> 依憲法第六條 3「標明中斷點,方便接續」而立。
> **這份是待辦清單不是計畫書**——底下每一項真的動工前,該任務仍須依第二條先有計畫段。
> 順序有意義:1 → 2 → 3 是**串的**,4 之後才可以並行。

---

## 優先序一覽

| # | 事項 | 性質 | 阻塞於 |
|---|---|---|---|
| 1 | runbook 補「不可逆」清單 + `purge_issues` cron | 🔴 發版前必做 | — |
| 2 | 合併 PR #22 → 發 **v0.2.0** | 發版 | 1、CI 綠 |
| 3 | VM 換版(跨三版 + **第一次帶 migration**) | 🔴 動正式資料 | 2 |
| 4 | SLO 聲明文送 portal | 對外 | — |
| 5 | T73 申請書重寫(§4.2a L1 變更) | 對外 | — |
| 6 | T29 冒煙餘 4 項 | 收尾 | 3 |
| 7 | `markdown` 補進 requirements-dev | 雜項 | — |

---

## 1. 🔴 runbook 補完(發版前,不是發版後)

今天新增兩支 backward 會刪資料的 migration,runbook 還不知道它們。
**先補文件再發版**——不然換版當下若要回滾,查表查不到這兩條。

- [ ] §B 的〈已知的不可逆回滾〉表補兩列:

  | migration | downgrade 會失去 | 可否重建 |
  |---|---|---|
  | `0007_issues` | 全部問題回報與討論串 | ❌ |
  | `0008_issue_attachments` | 全部回報附件(DB 紀錄;MinIO 物件另留孤兒) | ❌ |

- [ ] §C.2 cron 補一行(比照 `purge_audit`,**注意 `--yes` 不是 `--apply`**):

  ```cron
  # 已關閉滿 365 天的問題回報清除(04:30)
  30 4 * * *  cd /opt/upload-program && docker compose exec -T svc python tools/purge_issues.py --yes >> /var/log/upload-issue-purge.log 2>&1
  ```

- [ ] runbook 升版 + 版本歷史(第七條)。

---

## 2. 發 v0.2.0

- [ ] PR #22 CI 綠 → 合併 → 確認 `main` HEAD
- [ ] 確認 `app/version.py` 是 `0.2.0`(**已經是**,第八條 4 已滿足,打 tag 前不需再動)
- [ ] `alembic history` 驗鏈完整(第八條 6)
- [ ] 打 tag → 等 CI 推 GHCR image
- [ ] Release **title + content 四段**(第八條 3):
  - **title**:`v0.2.0 — 問題回報系統(回報／討論串／貼圖)、README v2.0`
  - **本版內容**:T76 README v2.0、T77 回報主體、T78 附件與貼圖、T79 總覽待辦與清除工具
  - **資料影響**:🟡 加兩張表(`0007_issues`、`0008_issue_attachments`,已雙向演練);
    🔴 backward 是 `DROP TABLE`,回滾銷毀使用者回報
  - **部署注意**:`.env` 無新增變數;🔴 **必跑 `alembic upgrade head`**;
    換版後 `docker exec portal-gateway nginx -s reload`
  - **測試**:463 passed,CI 綠

---

## 3. 🔴 VM 換版(今天最需要小心的一步)

**三個第一次同時發生**:第一次跨三版(v0.1.6 → v0.2.0)、第一次帶 migration 上正式機、
而 T27 的還原演練**還沒做過**。照 runbook §A.2,但額外加一條:

- [ ] **換版前備份,並當場驗一次備份可讀**(不是只確認檔案存在)。
      沒還原過的備份等於沒有備份;偏偏這是第一次動 schema 的換版。
- [ ] `docker login ghcr.io`(runner 的 post-step 會登出共用憑證,**每次 pull 前都要**)
- [ ] compose 改 tag → `pull` → `up -d svc`(不動 db / minio)
- [ ] 🔴 `alembic upgrade head`
- [ ] 🔴 `docker exec portal-gateway nginx -s reload`(不做 = 靜默 502)
- [ ] 冒煙(§A.4),本版哨兵建議挑 **`/upload/static/issue-upload.js`**
      ——這是 v0.2.0 才有的靜態檔,200 才證明跑的真的是新版;首頁 200 證明不了任何事
- [ ] 換版後驗證點:首頁版本號顯示 **0.2.0**、`/issues/new` 可送出、
      詳情頁可貼圖且圖片顯示得出來、管理總覽出現「未處理的回報」
- [ ] 🔴 既有系統零影響(portal / 其他 App 各點一次)

> 跳過 v0.1.7 / v0.1.8 是可以的——它們沒有 migration,v0.2.0 的 schema 是累積的。
> 但**冒煙要當成三版一起驗**:T69 更名、T70 總覽、T74 logout 端點都是這中間進去的。

---

## 4. SLO 聲明文送 portal

端點(T74)早就實作好了,卡在對方尚未登錄 `frontchannel.logout.url`。

- [ ] 送出 [聲明_SLO_session形態_給portal](../plans/聲明_SLO_session形態_給portal.md)
- [ ] 對方登錄後,重跑契約 §10 的 A→B 復現步驟並回填結果
- [ ] 契約 §10.6 本服務的狀態應可從「⏸ 待其聲明」轉正

---

## 5. T73 申請書重寫(不是催進度,是**改題目**)

原申請書問的是「請授予 `view-users`」。契約 v2.0 §4.2a L1 **明文禁止呼叫 Admin API**
之後,這個問法已經無效——現在要請求的是**變更平台通則**。

- [ ] 重寫 [申請_AdminAPI即時查詢_給portal](../plans/申請_AdminAPI即時查詢_給portal.md):
      標題與訴求改為「請求變更 §4.2a L1」,明講這是通則層級而非單一 App 授權
- [ ] 保留原有承諾(只顯示不落地 / 只單筆查不列舉 / 2 秒逾時回退顯示 sub /
      請求內去重不跨請求快取 / 上線後刪 `display_name_cache` 欄位並請撤銷 §6.1 特例)
- [ ] 🔴 **裁示前一律不動工**(T59 立下的慣例);過渡期維持現行快取

---

## 6–7. 收尾雜項

- [ ] **T29** 冒煙清單餘 4 項實跑(換版後做,順便一次驗完)
- [ ] `markdown` 補進 `requirements-dev.txt`——`tools/render_docs.py` 依賴它,
      今天在乾淨環境跑 `--check` 直接 `ModuleNotFoundError`,新人會踩

---

## 明確**不做**的事

寫下來是為了避免明天有人「順手」做掉:

- ❌ **不動 T73 的程式碼**(裁示前不動工)
- ❌ **不在正式機 `git pull && docker build`**(改 tag 後 pull,平台鐵則)
- ❌ **不啟動舊的 `nginx-gateway`**(會搶 80/443,全平台一起掛)
- ❌ **v0.2.0 若發錯不重發同版**,改發 v0.2.1(第八條 5)
- ❌ **不放寬 CSP**、不把 token 放進 localStorage——回報系統的貼圖走的是
  同源 XHR,不需要動這兩條

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-08-01 03:50 | Claude(Benny:寫明日待辦文件) | 初版:七項待辦與優先序(runbook 補完 → 發 v0.2.0 → VM 換版為串行)、換版三個「第一次」的加嚴步驟與哨兵檔、對外兩件(SLO 聲明、T73 改題目)、明確不做清單 |
