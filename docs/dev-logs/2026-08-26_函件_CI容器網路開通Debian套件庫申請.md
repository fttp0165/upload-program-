# 開發日誌:擬發函件——申請 CI 容器網路開通 Debian 套件庫(給 cats-portal)

**建立日期:** 2026-08-26 16:59
**最後更新:** 2026-08-26 16:59
**版本:** v1.0
**對應任務編號:** 無(計畫外文件任務;緣起 T93 的遺留問題)

---

## 計畫(動工前寫妥)

### 目標

新增函件
`docs/plans/uploadprogram_致_catsportal_申請_CI容器網路開通Debian套件庫_202608260750.md`
(+ HTML),向 cats-portal(Platform)申請把 **CI 容器 → Debian 套件庫**這條網路路徑
**正式化**。緣起 T93 的三個事實:

1. 2026-07-30(run #85)apt 套件庫**不可達**,builder 的 apt 因此整段拿掉;
2. 2026-08-24(T93 診斷)實測 **`APT_OK`**——可達性**變了,而我方沒有收到任何變更通知**;
   現在 base image 的 CVE 修補(runtime 層 apt 升級)**整條建置在這個未登記的狀態上**;
3. 同日實測 `deb.debian.org` CDN 節點索引不一致,`apt-get` 回 0 而什麼都沒升級
   (無聲失敗),被迫在 Dockerfile 裡做版本驗證 + 重試迴圈。

### 影響範圍

🟢 純文件,不動程式、不動資料、不動 CI 行為:

- 新增函件 md + HTML(`tools/render_docs.py` TARGETS 加一行)
- `docs/任務表.md`「等外部回覆」加第 3 項,升版 v1.76
- `docs/狀態總覽.md` §5 同步,升版 v1.1
- 本篇 dev-log

### 驗收標準

1. 函件檔頭含 From / To / 發文日期時間(**標時區**)/ 回覆對象四列
   (cats-portal 憲法第四條 7),並含第七條的日期/版本/版本歷史
2. 申請項目具體可裁示(確認政策 → allowlist 文件化 → 建議項),不是「請幫忙看看」
3. `python tools/render_docs.py --check` 通過
4. 任務表與狀態總覽的「等外部回覆」與函件一致

### 回滾方式

`git revert` 本次 commit。函件若已送出(對方已讀),依慣例**不撤檔**,以後續版次更正。

---

## 結果(完工後補)

- 異動檔案:
  - `docs/plans/uploadprogram_致_catsportal_申請_CI容器網路開通Debian套件庫_202608260750.md`(新增 v1.0)
  - 同名 `.html`(render_docs.py 產生)
  - `tools/render_docs.py`(TARGETS +1)
  - `docs/任務表.md` v1.75 → v1.76(等外部回覆 +1)
  - `docs/狀態總覽.md` v1.0 → v1.1(§5 同步)
- 為什麼這樣做:apt 可達性兩次無聲改變(07-30 不通、08-24 變通)證明這條路
  **沒有契約保障**;T93 的 CVE 修補鏈現在踩在它上面,哪天再靜默關掉,CI 會紅在
  「與本批改動無關」的地方,又是一輪 T93 式的考古。申請正式化就是把「可用」
  變成「有依據的可用」。檔名時戳採發文當下(`202608260750` 為 UTC,= 15:50 UTC+8),
  函件檔名帶日期是第四條 7 的要求(往來要按時間排序),與常設清單不帶日期不衝突。
- 測試結果:`python tools/render_docs.py --check` 通過。純文件,不跑 pytest 全套。
- 對現有資料的實際影響:🟢 無。
- 遺留問題:等 portal 回覆後,依裁示決定是否把 Dockerfile 的重試迴圈簡化
  (若平台提供 apt cache/mirror,CDN 不一致的 workaround 可退場——另立任務)。

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-08-26 | Claude(Benny 指示) | 初版:計畫 + 結果 |
