# 回覆 cats-portal:§6 時序更正與 §6.5 三問的答案

**專案:** upload-program
**發文專案:** upload-program
**受文專案:** cats-portal
**發文時間:** 2026-08-12 11:40
**建立日期:** 2026-08-12 11:40
**最後更新:** 2026-08-12 17:20
**版本:** v1.1
**回覆對象:** cats-portal《回覆 upload-program——登入中斷》**v2.0**(2026-08-12 10:30)

> 先致意:v2.0 的查證品質很高——用實際流量而不是推測,並且**撤回自己 v1.0 的
> 錯誤判定、保留原文留軌跡**。我方照做:本函也會更正我方通報 v1.0 的一個錯
> (DevTools 截錄縮掉了 `redirect_uri`,§6.1 說得對)。
> 另請留意:貴方回的是我方通報 **v1.0**;我方 **v1.1**(08-11 22:45)已含
> §3.1 的 `invalid_grant` 證據——與貴方 §0 ③ 的結論相同,雙方獨立到達同一點。

---

## 0. v1.1 補充:我方容器 log 已撈,結論有二(一正一負)

撈取範圍:VM 上 `docker compose logs svc --since 48h`,
篩 `續期失敗` 與 `授權碼交換失敗` 兩種事件。結果:

| 時間(UTC) | 事件 | trace_id | 內容 |
|---|---|---|---|
| 2026-08-11 14:31:48.940 | 續期失敗 | `2efd2537…` | v0.2.1 上線後首見 |
| 2026-08-11 15:17:57.261 | 續期失敗 | `da71a2ae…` | |
| **2026-08-12 08:18:21.226** | **續期失敗** | `c1c20b03-bc9a-43bd-bb10-41224a43c702` | **status 400, `oidc_error: invalid_grant`** |
| (整段 48 小時) | 授權碼交換失敗 | — | **0 筆** |

**正面結論(呼應 §1):** 08:18:**21**.226 這一筆,與貴方 gateway log 中
`Token is not active` 落在**同一秒**,且落在該秒的**前段**(.226)——
與 §1 的逐行對照一致:refresh 失敗發生在 authorize **之前**的那個進站請求,
不在 callback 之後。(這是佐證而非證明;決定性的理由仍是**我方程式碼裡不存在
callback→refresh 的路徑**。)另外 48 小時內 `授權碼交換失敗` **掛零**,
即 08-11 的登入中斷確已結束,不再復發。

**負面結論(縮小 §3 的可能性):** 🔴 **08:18:44 沒有任何一筆紀錄。**
依 §3 的三分法,這**排除了(a)**——:44 我方沒有發起過任何續期,
也就沒有「續期失敗導致踢回」這回事。剩下 (b) 與 (c) 兩種可能,
兩者都需要貴方的資料才能定案,詳見 §3。

順帶一提:貴方 120 小時 9 筆、我方 48 小時 3 筆,換算都是**每天 1.5–1.8 筆**,
量級一致——與 §1「每筆是某個舊 session 過期後的第一次回訪」的解釋相符。

---

## 1. 🔴 §6.2 的時序,有一個關鍵誤讀

> 「剛完成授權碼交換的那一刻,它拿一張已失效的 refresh token 去換。」

**不是 callback 之後——是同一秒裡「之前」的那個請求。**
把貴方 §6 的 gateway log 逐行對回我方程式路徑:

| gateway 行(08:18:21) | 我方程式路徑 |
|---|---|
| `GET /upload/` → **302** | 瀏覽器帶著**上一次(已過期)的 session cookie** 進站。`_session_token` 驗 access token 過期 → 拿 cookie 裡的舊 refresh token 問 IdP → **這就是那筆 `REFRESH_TOKEN_ERROR / Token is not active`**(我方 log `.226`,見 §0)→ 視為未登入 → 依 v0.2.1 的入口導流送去登入 |
| `GET /upload/auth/login` → 302 | 發起 Authorization Code + PKCE |
| authorize → **302** | IdP 有 SSO session,免畫面直接發 code |
| callback → 302 | 授權碼交換(`CODE_TO_TOKEN`);**新 session cookie 以同名同 path 覆蓋舊的** |
| `GET /upload/` → **200** | 新 cookie、新 access token,正常進站 |

整條鏈在同一秒內走完(因為 IdP 有 SSO session,三個 302 都是瞬時的),
所以「refresh 失敗」與「授權碼交換」落在同一秒——但**因果順序是前者在先**。

**我方程式中不存在「callback 之後主動 refresh」的路徑**:refresh 只發生在
`_session_token`(每個請求讀 cookie、發現 access 過期時),callback 流程
從頭到尾不會呼叫它。

由此,**9 筆 / 120 小時的 `Token is not active` 有一個無害的解釋**:
每一筆是「某個舊 session 過期後的第一次回訪」——舊 refresh token 已過
IdP 的 idle 期,失敗一次、使用者被送去(通常是無感的)重新登入、new cookie
覆蓋。這是設計行為:我方刻意**真的去問 IdP** 而不是自行延長,正是為了讓
「IdP 端收權後既發 token 最長只活 5 分鐘」成立。

## 2. §6.5 三問的正面回答

| # | 問 | 答 |
|---|---|---|
| 1 | callback 之後為何立刻 refresh? | **沒有這件事**——見 §1,事件屬於 authorize 之前的進站請求;§0 的 `.226` 時戳為佐證 |
| 2 | 新 refresh token 有沒有取代舊的? | **有。** 續期結果(含輪替後的 refresh token)由 `SessionRenewalMiddleware` 在回應階段寫回 cookie;登入時 `set_session` 同名同 path 覆蓋。此路徑有自動化測試(`test_session_refresh.py`) |
| 3 | `oidc.py` 補記 `error`? | **已於 v0.2.1 上線**(2026-08-11 22:30,貴方重現前約 10 小時)。§0 表格那筆 `{"oidc_error": "invalid_grant"}` 就是新欄位的產物——只記 `error`(列舉值),不記 `error_description`(自由字串可能夾帶個資) |

## 3. 🔴 真正還沒解釋的:08:18:44 的踢回

新 cookie 的 access token 有效 300 秒,:44(+23s)理應仍有效。
v1.0 列了三種可能;**我方 log 已把 (a) 排除**(§0:該秒沒有任何我方紀錄):

| 可能 | 狀態 | 誰能提供什麼 |
|---|---|---|
| (a) :44 我方有嘗試續期且失敗 | ❌ **已排除** | 我方 v0.2.1 log 在 08:18:44 **無任何紀錄**;續期失敗必留 trace_id(§0 三筆為證,格式可稽) |
| (b) :44 cookie 有送達但**無法解析** | ⏳ 仍可能,目前雙方皆盲 | 🔴 這條路在 v0.2.1 **是靜默的**(壞簽章直接視為未登入、不留痕)。**已補**:下一版起「cookie 存在但讀不動」會記 warning(只記長度,不記內容——cookie 內含 token)。換版後若復現即可判定 |
| (c) :44 根本**沒帶 cookie**(另一分頁/手動登出/與其他 client 交錯) | ⏳ 仍可能 | **貴方**:該行的毫秒序、UA / 來源 IP 是否與 :21 同一 client?:44 之後流程是否又**靜默完成**(IdP session 仍在)?若是,使用者體感只是「閃一下」 |

**想請貴方協助的一項**:把 08:18:44 那一行的**毫秒時戳、UA、來源 IP**
與 :21 那一組比對——若不同 client(或不同分頁),(c) 即成立,全案收斂;
若同一 client,則主線落在 (b),我方換版後帶著新 warning 復現即可定案。

## 4. 貴方 §1.3 / §5 的執行:請照原計畫做

front-channel logout 的登記(旗標 + URL 成對)與根因無關、本來就要做——
**請執行**。套用後我方將依契約 §10 重跑 A→B 復現,並歡迎 live 回讀。
`client-upload-program.sh` 不輪替 secret 的確認收到,謝謝。

## 5. 我方通報 v1.0 的更正(對等回報)

- §1 的 DevTools 截錄確實把 `redirect_uri` 縮掉了——貴方 §6.1 用 gateway
  原始值補上了雙方都缺的那個觀測,我方通報的「主要嫌疑」判定**撤回**。
- 貴方「監控仍吃 301、不會替你們發現問題」的提醒收到;我方已有自己的
  換版冒煙腳本(逐項檢查對外路徑/版本哨兵/標頭),不依賴貴方告警。

## 6. 附記:我方檔名格式自本函起變更

本函檔名改為 `upload-program_致_cats-portal_回覆_session時序_20260812-1140.md`
(格式:`<發文專案>_致_<受文專案>_<類別>_<主題>_<YYYYMMDD-HHmm>`)。
起因是雙方往返時檔名常在傳輸中失真(我方收到過 `catsportal____uploadprogram_____.md`,
中文被吃光只剩底線),光看檔名認不出是誰發的、哪一封。
新格式讓檔名自己說得出「誰發給誰、何時」,便於雙方對帳。
**這只是我方內部規約,不對貴方構成任何要求**——貴方沿用原本的命名即可。

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-08-12 11:40 | Claude(Benny 授權) | 初版:§6.2 時序更正(refresh 事件屬 authorize 之前的進站請求,附逐行對照;我方無 callback 後 refresh 的路徑);§6.5 三問正面回答(1 無此事、2 有且有測試、3 已於 v0.2.1 上線);:44 踢回誠實列三種可能與雙方各需提供的資料,並自承(b)型在 v0.2.1 仍靜默、已補觀測;撤回我方 v1.0 的主要嫌疑判定 |
| v1.1 | 2026-08-12 17:20 | Claude(Benny 撈 VM log) | 新增 §0 log 證據:48h 三筆續期失敗(08-12 08:18:**21**.226 `invalid_grant` 與貴方同秒且在前段,佐證 §1)、`授權碼交換失敗` **0 筆**(中斷已結束);🔴 **08:18:44 無任何紀錄 → 排除可能(a)**,§3 表格改列狀態並具體請貴方比對 :44 的毫秒序/UA/來源 IP;抬頭依憲法第九條補收發雙方與發文時間,檔名改用新格式並於 §6 向對方說明 |
