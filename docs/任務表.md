# upload-program 任務表

**建立日期:** 2026-07-25 15:53
**最後更新:** 2026-07-26 00:28
**版本:** v1.2

> 狀態:⬜ 未開始 / 🔵 進行中 / ✅ 完成 / ⚠️ 完成但有欠項
> 資料影響:🟢 不動資料 / 🟡 加欄位或加表 / 🔴 UPDATE 或刪除
> **完成定義(憲法第一條 3):** 驗收達成 + 測試全綠(實際跑過)+ 開發日誌已寫 + 文件已升版

---

## M1 規則與計畫

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T01 | 採用開發憲法並在地化(CLAUDE.md v2.0) | ✅ | 🟢 | 七條齊備、位階界定清楚、有版本歷史 |
| T02 | 開發計畫書(里程碑、資料分級、回滾、§10 八問) | ✅ | 🟢 | M1–M7 皆有出場條件;八問全部有答案 |
| T03 | 任務表(本檔) | ✅ | 🟢 | 每項任務有驗收標準與資料分級 |
| T04 | 設計文件升版(領域模型、API、儲存、安全) | ✅ | 🟢 | 與現行程式碼一致;有版本歷史 |
| T05 | 文件產生器 `tools/render_docs.py` + HTML 版 | ✅ | 🟢 | 正式文件皆有 md + HTML;light 主題;架構圖內嵌 SVG;`--check` 可驗同步 |
| T31 | 功能需求大綱(補列於 M1) | ✅ | 🟢 | 七大模組功能項齊備,每項有編號/優先級/里程碑/實作狀態;範圍外與開放問題明列 |
| T32 | 開放問題 Q1–Q8 定案與需求回寫 | ✅ | 🟢 | 決定全數回寫大綱/計畫書/任務表並升版;受影響功能項的優先級與里程碑同步調整 |

## M2 骨架與規約落地

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T06 | 設定模組(全走 env、缺值 fail-fast) | ⚠️ | 🟢 | 缺必要變數即啟動失敗;`api_prefix` 無預設值 |
| T07 | JSON 單行 log + trace_id 中介層 | ⚠️ | 🟢 | 走 stdout;含 timestamp/level/service/message;不記 JWT |
| T08 | RFC 7807 錯誤格式(401/403 語意分離) | ⚠️ | 🟢 | 所有錯誤皆 `application/problem+json`;待開通有專屬 type |
| T09 | 資料模型與 alembic 初始 migration | ⚠️ | 🟡 | `users` 無 email/姓名/密碼欄;`downgrade()` 可回滾 |
| T10 | Dockerfile(multi-stage、non-root、8080、healthcheck) | ⚠️ | 🟢 | image ≤300MB;`/health` 60 秒內綠;SIGTERM 優雅關閉 |
| T11 | docker-compose(cats-edge external+name、DB/MinIO 不曝 port) | ⚠️ | 🟢 | `docker compose config -q` 通過;三層網路正確 |
| T12 | CI(測試 + lint + build + Trivy + push GHCR) | ⬜ | 🟢 | CI 全綠;有 high/critical CVE 即失敗 |

> ⚠️ T06–T11 的程式碼已完成但**未跑過測試**,且違反第二、三條(無計畫、非 TDD)。
> 依憲法第三條 5,未實際跑綠不得標 ✅。

## M3 身分接入

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T13 | OIDC discovery + PKCE 登入導向 | ⚠️ | 🟢 | 端點動態取;`response_type=code`、`S256`;禁 implicit |
| T14 | Token 驗證(RS256、JWKS 快取 1h、kid 輪替、±30s) | ⚠️ | 🟢 | HS256/`alg=none`/竄改/過期/別人的 aud 一律 401 |
| T15 | 首登建零角色 user + 403 待開通 | ⚠️ | 🟡 | DB 只有 sub;文案指引找管理員;`/v1/me` 待開通也看得到 |
| T16 | Single logout + session cookie(path 綁前綴) | ⚠️ | 🟢 | 導 IdP logout;cookie httpOnly/Secure/SameSite=Lax |
| T17 | 管理後台:開通、停用、指派平台角色 | ⚠️ | 🟢 | 非 admin 一律 403;不得停用自己 |

## M4 核心業務

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T18 | 專案 CRUD + 可見性(internal / private) | ⚠️ | 🟡 | private 對非成員回 **404**(不洩漏存在) |
| T19 | 成員與角色(owner / maintainer / viewer) | ⚠️ | 🟡 | 權限矩陣測試全綠;maintainer 不能刪專案 |
| T20 | 版本(draft → published)與發布鎖定 | ⚠️ | 🟡 | 空版本不可發布;已發布不可換檔;重複發布冪等 |
| T21 | 搜尋(只回可見專案) | ⚠️ | 🟢 | 非成員搜不到 private 專案 |
| T34 | 轉移擁有權(F16) | ⬜ | 🟢 | owner 可移交給成員;原 owner 降為 maintainer;非 owner 不可操作 |
| T35 | 最新版捷徑(F26) | ⬜ | 🟢 | `/projects/{slug}/releases/latest` 與其檔案下載網址固定不失效;以 `published_at` 判定,不用字串排序 |
| T36 | 標籤分類(F42) | ⬜ | 🟡 | 可加/移除標籤並依標籤篩選;migration 有 backward |

## M5 檔案

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T22 | 串流上傳 + SHA-256 + 配額 | ⚠️ | 🟡 | 記憶體不隨檔案大小成長;超限回 413;雜湊不符即作廢 |
| T23 | magic bytes 判型白名單 | ⚠️ | 🟢 | HTML/SVG 任何 kind 皆拒;判型不過不留物件 |
| T24 | 下載強制 attachment + nosniff + 雜湊標頭 | ⚠️ | 🟢 | 回應含 `Content-Disposition: attachment`、`nosniff`、SHA-256 |
| T33 | 容量上限調降為單檔 100 MB(F34) | ⬜ | 🟢 | `config.py` 與 `.env.example` 預設值改為 100 MB;超限測試仍綠 |
| T37 | 下載次數統計(F43) | ⬜ | 🟡 | 下載即累計;統計不記個資;migration 有 backward |

## M6 接入 Cats

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T25 | 掃毒決策(ClamAV 納不納入 MVP) | ✅ | 🟢 | 2026-07-26 定案:**不納入,先預留接點**;期間一律標 `not_scanned` |
| T38 | 稽核紀錄(F54) | ⬜ | 🟡 | 可查詢誰在何時做了什麼;**只存 user_id,不存 email/姓名**;migration 有 backward |
| T26 | 申請 SSO client(§5)並登記契約 §9 | ⬜ | 🟢 | 拿到 client_id/secret;redirect URI 含子路徑前綴 |
| T27 | 換版 runbook + 備份與**還原演練** | ⬜ | 🔴 | 還原實際演練過(沒還原過的備份等於沒有備份) |
| T28 | cats-portal 路由 PR + 驗收 | ⬜ | 🟢 | `/«PREFIX»/` 通;不影響既有 `/`、`/TMP_GEN/`、`/plm/` |
| T29 | 接入冒煙清單(SSO 契約 §7)全數通過 | ⬜ | 🟢 | 六項全綠;repo grep 零 Authentik、零 HS256 |

## M7 前端

> 範圍依 2026-07-26 裁示:主頁、導航列、專案頁面、專案歷史、最新版本至頂。
> ⏳ 技術選型未定(Q9),建議伺服器端模板;動工前需先定案。

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| ~~T30~~ | ~~最小可用網頁介面~~ | — | — | **由 T40–T45 取代**(範圍已細化) |
| T39 | 前端技術選型定案(Q9) | ⬜ | 🟢 | 書面決策:模板 vs SPA、靜態檔供應方式、子路徑前綴處理;**T40 動工前必須完成** |
| T40 | 導航列與版型骨架(F70) | ⬜ | 🟢 | 全站共用;子路徑下所有連結與靜態檔皆正確,無 404 |
| T41 | 主頁:專案列表 + 搜尋 + 標籤篩選(F71) | ⬜ | 🟢 | 只顯示當事人看得到的專案;分頁可用 |
| T42 | 專案頁:資訊 + **最新版本置頂**(F72) | ⬜ | 🟢 | 最新已發布版本的說明與下載按鈕在頁面最上方直接可見 |
| T43 | 專案歷史:版本列表(F73) | ⬜ | 🟢 | 依發布時間倒序;可展開看各版檔案並下載 |
| T44 | 上傳介面:建版本、傳檔、發布(F74) | ⬜ | 🟢 | 不需 curl 即可完成一輪上傳與發布 |
| T45 | 待開通頁與管理後台頁(F75、F76) | ⬜ | 🟢 | 未開通者看到指引頁而非裸 JSON;管理員可一鍵開通 |

## M8 第二期(不納入 MVP)

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T46 | 病毒掃描接上(F38) | ⬜ | 🟡 | 背景掃描 + 結果回寫;`infected` 的處置政策待 Q11 定案 |

---

## 待補的合規欠項(因事後補記而產生)

| # | 欠項 | 對應任務 |
|---|---|---|
| A1 | 測試從未實際執行——必須跑過並看到綠燈 | T06–T24 |
| A2 | T06–T24 缺動工前計畫段(已違反,不補寫,僅於日誌標示) | 全部 ⚠️ 項 |
| A3 | 缺 CI,無法滿足「M2 之後含 CI 綠燈」 | T12 |
| A4 | ~~設計文件尚未與實作對齊~~ 已於 T04 完成(設計_MVP v2.0) | T04 ✅ |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-07-25 15:53 | Claude(Benny 授權) | 初版:M1–M7 共 30 項任務,含狀態、資料影響分級與驗收標準;T06–T24 因事後補記標 ⚠️;另列四項合規欠項 |
| v1.1 | 2026-07-26 00:15 | Claude(Benny 授權) | 新增 T31 功能需求大綱(補列於 M1,✅);T04 設計文件升版與 T05 文件產生器由 🔵 改 ✅;欠項 A4 結案 |
| v1.2 | 2026-07-26 00:28 | Claude(Benny 裁示) | 依 Q1–Q8 定案新增 T32–T39(需求回寫、容量調降、轉移擁有權、最新版捷徑、標籤、下載統計、稽核紀錄、前端選型);T25 掃毒決策結案(不納入,先預留);M7 由單一 T30 細化為 T40–T45;新增 **M8 第二期**(T46 掃毒) |
