# upload-program 任務表

**專案:** upload-program
**建立日期:** 2026-07-25 15:53
**最後更新:** 2026-08-31 16:45
**版本:** v1.92

> 狀態:⬜ 未開始 / 🔵 進行中 / ✅ 完成 / ⚠️ 完成但有欠項
> 資料影響:🟢 不動資料 / 🟡 加欄位或加表 / 🔴 UPDATE 或刪除
> **完成定義(憲法第一條 3):** 驗收達成 + 測試全綠(實際跑過)+ 開發日誌已寫 + 文件已升版

---

## 目前進度(2026-08-31 16:45)

任務 128 項:**✅ 122 / 🔵 1 / ⬜ 5**(另 1 項 ~~T30~~ 已由 T40–T45 取代)。
測試 **653 passed**、`ruff check` 乾淨。

### 三個版本號現在各在哪裡

| 位置 | 版本 | 說明 |
|---|---|---|
| `app/version.py` | **0.3.0** | 🔵 合併 PR #33 後改的(它把 `publish` 從「發布」變成「送審」,那是 minor 版);依第八條 4 已在打 tag 前備好 |
| GitHub 最新 tag | **v0.2.5** | 指向 `31b4c20`,image 已上 GHCR。⚠ `v0.2.4` **作廢**(打在 `6dabe7d`,其常數是 `0.2.3`),依第八條 5 不刪不重推 |
| VM 執行中 | **v0.2.5** | 2026-08-31 換版,側欄實測 `upload-program v0.2.5`;migration 到 `0009_notify_email` |

### 尚未合併的工作

**T106–T128 一批**(我方 T106–T109 + 併入的 PR #33 = T110–T128)。
`main` = v0.2.5;本批合併後 `APP_VERSION` = **0.3.0**(#33 改變了既有行為:
`POST /v1/releases/{id}/publish` 由「發布」變成「送審」,那是 minor 版的定義)。

🔴 **有兩個 migration**:`0010_release_review`、`0011_project_comments`
(合併時讓號改鏈,見 [計畫_合併PR33](plans/計畫_合併PR33_重編號與migration改鏈.md))。
換版必須 `alembic upgrade head`,且 🔴 **退版會刪掉全部留言**(runbook §B)。

⚠ **PR #33 已被本分支取代** —— 合併後須在該 PR 留言說明並關閉,
不然下一個人會以為那還是待辦。

### 未完成的任務(6 項)

| # | 任務 | 卡在哪裡 |
|---|---|---|
| T130 ⬜ | 本機 CI 指令與 `ci.yml` 不得分岔 | T129 的遺留。同一句檢查指令同時存在於 `ci.yml` 與 runbook,這次分岔的方向是「文件比 CI 嚴格」→ 假警報;下次可能是「文件比 CI 寬鬆」→ 本機綠而 CI 紅。兩條路:**從 `ci.yml` 產生** runbook 那一節(比照 `render_docs.py`,較貴但根治)或**加測試比對兩邊關鍵字**(便宜,只擋得住「改了一邊」)|
| T105 ⬜ | **CI 守門:`APP_VERSION` 不得 ≤ GitHub 最新 tag** | 🔴 **不是建議,是下一個要做的任務**。三次卡在版本號(T94 文件寫錯最新 tag、T97 tag 沒推而 main 前進、**T104 時序交錯讓錯的 image 上了 GHCR**),前兩次的結論都只寫成「建議」而**建議不會執行自己**。本次事故它會直接擋下:`v0.2.4` 打在常數為 `0.2.3` 的 commit 上 → 那個 commit 的 CI 紅 → `Push GHCR` 不執行 → **錯的 image 不會存在**。⚠ 刻意不夾在發版 PR 裡做(守門本身要一輪 CI 驗證) |
| T27 🔵 | 換版 runbook + 備份**還原演練** | 文件已完成;演練待 staging 環境,回填證據表後才轉 ✅ |
| T29 ⬜ | 接入冒煙清單(SSO 契約 §7) | 六項餘 4 項待實跑;**T81 改了首頁行為,第 1 項的說明要一併更新**(現在首頁也 302,比當初報給 portal 的更貼近 §7) |
| T73 ⬜ | 顯示名稱改 Admin API 即時查詢 | **待 portal 裁示**;契約 v2.0 §4.2a L1 明文禁用 Admin API,已非授權問題而是**通則層級變更**,申請書需重寫 |
| T46 ⬜ | 病毒掃描接上(F38) | M8 第二期,不納入 MVP |

### 等外部回覆(不在本專案手上)

1. **SLO 聲明文**送 portal → 對方登錄 `frontchannel.logout.url` 後,才能重跑契約 §10 的 A→B 復現步驟(T74 端點已實作)。
2. **Admin API `view-users`** 申請(T73 前置)。

### 未結的收尾項目(v0.2.0 換版時未做完)

- [ ] `tools/purge_issues.py` 與 `purge_audit.py` 排進 VM cron(runbook §C.2)
      🔴 **2026-08-31 實測:cron 一行都沒掛,稽核清理從未執行過**;且 T109 之前掛了也會 `No such file`。**換到 v0.2.6 之後再掛,掛完當場手跑一次確認**
- [ ] runbook 的「不可逆操作」清單補上 `0007` / `0008` 的 `DROP TABLE`
- [ ] 🐛 **登入曾出現 401「授權碼交換失敗」**(trace `5983122e`):`app/oidc.py` 只記 status
      不記 IdP 的 `error`,無法一眼定位。**建議另立任務**補上(錯誤回應的 body 只有
      `error` / `error_description`,不含 token);裁示前不動工
- [ ] 🔴 **`requirements.txt` 仍是相容區間不是 lock 檔**——建置不可重現,同一個 tag 重建
      可能得到不同內容;T82 的 CVE 只是症狀,建議另立任務做 pip-compile

---

## M1 規則與計畫

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T01 | 採用開發憲法並在地化(CLAUDE.md v2.0) | ✅ | 🟢 | 七條齊備、位階界定清楚、有版本歷史 |
| T02 | 開發計畫書(里程碑、資料分級、回滾、§10 八問) | ✅ | 🟢 | M1–M7 皆有出場條件;八問全部有答案 |
| T03 | 任務表(本檔) | ✅ | 🟢 | 每項任務有驗收標準與資料分級 |
| T04 | 設計文件升版(領域模型、API、儲存、安全) | ✅ | 🟢 | 與現行程式碼一致;有版本歷史 |
| T05 | 文件產生器 `tools/render_docs.py` + HTML 版 | ✅ | 🟢 | 正式文件皆有 md + HTML;light 主題;架構圖內嵌 SVG;`--check` 可驗同步 |
| T31 | 功能需求大綱(補列於 M1) | ✅ | 🟢 | 七大模組功能項齊備,每項有編號/優先級/里程碑/實作狀態;範圍外與開放問題明列 |
| T32 | 開放問題 Q1–Q8 定案與需求回寫 | ✅ | 🟢 | 決定全數回寫大綱/計畫書/任務表並升版;受影響功能項的優先級與里程碑同步調整 |
| T48 | 容量級距定案與需求回寫(Q10) | ✅ | 🟢 | 兩級距數值明確;F17/F18 入大綱;T33 修正、T49 開立 |

## M2 骨架與規約落地

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T06 | 設定模組(全走 env、缺值 fail-fast) | ✅ | 🟢 | 缺必要變數即啟動失敗;`api_prefix` 無預設值 |
| T07 | JSON 單行 log + trace_id 中介層 | ✅ | 🟢 | 走 stdout;含 timestamp/level/service/message;不記 JWT |
| T08 | RFC 7807 錯誤格式(401/403 語意分離) | ✅ | 🟢 | 所有錯誤皆 `application/problem+json`;待開通有專屬 type |
| T09 | 資料模型與 alembic 初始 migration | ✅ | 🟡 | `users` 無 email/姓名/密碼欄;`downgrade()` 可回滾 |
| T10 | Dockerfile(multi-stage、non-root、8080、healthcheck) | ✅ | 🟢 | CI 實測:**image 254 MB**(≤300)、`Config.User='app'`、**UID 1000**、`/health` 2 秒綠、`/ready` 503、SIGTERM 優雅關閉 |
| T11 | docker-compose(cats-edge external+name、DB/MinIO 不曝 port) | ✅ | 🟢 | `docker compose config -q` 通過;三層網路正確 |
| T12 | CI(測試 + lint + build + Trivy + push GHCR) | ✅ | 🟢 | **Actions run #3 全綠**(前兩次紅燈已修:pytest pythonpath、trivy-action 版本);Trivy 0 findings |
| T50 | 補跑測試並修到全綠(清償欠項 A1) | ✅ | 🟢 | 實際跑過並看到綠燈;修掉的每個紅燈都有根因紀錄;lint 全過 |

> **2026-07-26 T12 更新:M2 全數 ✅。** CI(Actions run #3)全綠:
> 81 passed、lint 過、文件同步過、image **254 MB**、**UID 1000**、`/health` 2 秒綠、Trivy 0 findings。
> **2026-07-26 T16 更新:A6 亦已清償**,測試總數 **103 passed**。
> 除 A2 外所有欠項結案。
> A2(缺動工前計畫段)是**已發生的違規,不會消失**,僅由 T00 日誌如實記載。

## M3 身分接入

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T13 | OIDC discovery + PKCE 登入導向 | ✅ | 🟢 | 端點動態取;`response_type=code`、`S256`;禁 implicit |
| T14 | Token 驗證(RS256、JWKS 快取 1h、kid 輪替、±30s) | ✅ | 🟢 | HS256/`alg=none`/竄改/過期/別人的 aud 一律 401 |
| T15 | 首登建零角色 user + 403 待開通 | ✅ | 🟡 | DB 只有 sub;文案指引找管理員;`/v1/me` 待開通也看得到 |
| T16 | Single logout + session cookie(path 綁前綴) | ✅ | 🟢 | 22 條測試:Location 指向 IdP `end_session_endpoint`、帶 `id_token_hint`、清兩個 cookie、IdP 掛掉仍能登出;cookie HttpOnly/Secure/SameSite=Lax/path 綁前綴;開放轉址防護 |
| T17 | 管理後台:開通、停用、指派平台角色 | ✅ | 🟢 | 非 admin 一律 403;不得停用自己 |

## M4 核心業務

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T18 | 專案 CRUD + 可見性(internal / private) | ✅ | 🟡 | private 對非成員回 **404**(不洩漏存在) |
| T19 | 成員與角色(owner / maintainer / viewer) | ✅ | 🟡 | 權限矩陣測試全綠;maintainer 不能刪專案 |
| T20 | 版本(draft → published)與發布鎖定 | ✅ | 🟡 | 空版本不可發布;已發布不可換檔;重複發布冪等 |
| T21 | 搜尋(只回可見專案) | ✅ | 🟢 | 非成員搜不到 private 專案 |
| T34 | 轉移擁有權(F16) | ✅ | 🟢 | 9 條測試(**先紅後綠**):`PUT /projects/{slug}/owner`;原 owner 降 maintainer;冪等;maintainer/非成員 403;未開通者 409;**平台管理員可代為轉移** |
| T35 | 最新版捷徑(F26) | ✅ | 🟢 | 13 條測試(**先紅後綠**):`/releases/latest` 與 `/latest/artifacts/{filename}/download`;**關鍵測試刻意讓建立順序/版本號字串/發布時間三種排序給出不同答案**,只有用 `published_at` 才過 |
| T36 | 標籤分類(F42) | ✅ | 🟡 | 18 條測試(**先紅後綠**);`PUT /{slug}/tags` 整組取代、`?tag=` 篩選、`GET /v1/tags` 計數;正規化(小寫/去空白/去重);**`GET /v1/tags` 只計看得到的專案**;migration **up→down→up 演練通過** |

## M5 檔案

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T22 | 串流上傳 + SHA-256 + 配額 | ✅ | 🟡 | 記憶體不隨檔案大小成長;超限回 413;雜湊不符即作廢 |
| T23 | magic bytes 判型白名單 | ✅ | 🟢 | HTML/SVG 任何 kind 皆拒;判型不過不留物件 |
| T24 | 下載強制 attachment + nosniff + 雜湊標頭 | ✅ | 🟢 | 回應含 `Content-Disposition: attachment`、`nosniff`、SHA-256 |
| T33 | 容量上限調整(F34) | ✅ | 🟢 | 單檔 **100 MB**、專案 **2 GB**;另加**防漂移測試**確保 `.env.example` 與程式預設一致(分兩步改以實證其有效) |
| T49 | 專案容量級距與擴充(F17) | ✅ | 🟡 | `projects` 加 `quota_tier`(standard 2 GB / extended 10 GB);**只有平台管理員可調**;413 訊息含級距、上限、已用量、本次大小與依級距而異的指引;migration up→down→up 演練通過(**本專案第一個加欄位任務**) |
| T37 | 下載次數統計(F43) | ✅ | 🟡 | 9 條測試(**先紅後綠**);`artifacts.download_count` 原子 UPDATE 累計,版本計數為加總不另存;兩條下載路徑共用同一個計數點;失敗/無權限的下載不計數;**「不記個資」由結構保證**(無事件表、無下載者欄位)並有測試釘住;migration up→down→up 演練通過 |

## M6 接入 Cats

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T25 | 掃毒決策(ClamAV 納不納入 MVP) | ✅ | 🟢 | 2026-07-26 定案:**不納入,先預留接點**;期間一律標 `not_scanned` |
| T38 | 稽核紀錄(F54) | ✅ | 🟡 | 25 條測試(**先紅後綠**,24 failed 起步);15 個 action、15 個寫入點,全部經 `audit.record()` 單一入口(該函式同時寫 stdout log,收掉「log 記了但 audit 沒記」);🔴 **`target_label` 存快照而非只存外鍵**——東西刪掉後 UUID join 不回任何東西,而「誰刪掉了什麼」正是稽核最重要的用途;🔴 **與業務同一個 transaction**,保證「有紀錄 ⇔ 事情真的發生了」;🔴 **只有平台管理員可查**(T37「統計只到次數」那個個資決定的延續);`action` 刻意不下 DB CHECK(阻力會讓人選擇不記);保留期 365 天 + `tools/purge_audit.py`;migration up→down→up 演練通過 |
| T26 | 申請 SSO client(§5)並登記契約 §9 | ✅ | 🟢 | client 已建立並回讀驗證(confidential + PKCE S256);secret 已於 2026-07-29 安全交付並完成登入實測;**契約 §9 已登記本服務為平台第一個完成 SSO 端到端接入並上線的 App**(portal 2026-07-30 狀況對齊函)|
| T27 | 換版 runbook + 備份與**還原演練** | 🔵 | 🔴 | 還原實際演練過(沒還原過的備份等於沒有備份)。**文件部分已完成**(2026-07-29):[runbook_換版與備份還原.md](runbook_換版與備份還原.md)——換版含 🔴 通知 portal reload 的明文約定(施工單 §3.2 落地)、回滾含兩個不可逆 migration 的集中清單、備份「先物件後資料庫」、cron 含稽核清理(T38 遺留 #1 落地)、還原演練寫成**證據表**(毀掉再還原、SHA-256 驗功能、實測 RTO);⏳ 演練待 staging 環境,執行並回填證據表後才轉 ✅ |
| T28 | cats-portal 路由 PR + 驗收 | ✅ | 🟢 | 依施工單 §3.0 順序完成:我方容器 2026-07-29 先上線 → gateway 解析確認 → `/upload/` 三個 location 解除註解 + reload → 既有系統四條零差異;**portal 已將變更 commit 回權威 repo**(含 upstream 白名單 CI 斷言),見 2026-07-30 狀況對齊函 §2 |
| T74 | 單一登出:front-channel logout 端點(契約 v2.0 §10) | ✅ | 🟢 | SSO 契約 **v2.0** 新增 §10 SLO 義務,§10.6 列本服務「⏸ 待其聲明」;我方 session 是 HttpOnly cookie → §10.1 屬 **MUST 無豁免空間**;實作 `GET /oidc/frontchannel-logout`:**免認證**(iframe 不帶 token,要求認證等於永不生效)、冪等 204、**只刪 cookie**(不建 session/不寫業務/不寫稽核,有零副作用測試)、`iss`/`sid` 帶與不帶皆可、刪除的 `Path` 與種下時相同(有測試——Path 不符瀏覽器不刪,正是契約緣起段全平台的死法);聲明文 [聲明_SLO_session形態_給portal](plans/聲明_SLO_session形態_給portal.md) 待端點上線後送出請 portal 註冊;CLAUDE.md 契約對齊 v1.7→**v2.0**;**7 條紅→綠,全套 409** |
| T77 | 問題回報系統:回報 + 清單 + 狀態機(無附件) | ✅ | 🟡 | Benny 裁示:回報要有富文本、可貼照片、有清單、管理員可回覆、記錄進度與關閉狀態;設計文件 [設計_問題回報系統](plans/設計_問題回報系統.md) v1.0 + [施工計畫書](plans/施工計畫書_問題回報系統.md) v1.0(含五題**建議裁示**、DDL、權限矩陣、測試條數、三期發版 v0.2.0–v0.2.2)**待 Benny 逐條確認**;🔴 富文本**不採 HTML**(同源之下一次 XSS = 全平台帳號淪陷)改用 **Markdown + 白名單轉譯**;三張新表 + 狀態機(僅管理員可改,resolved/closed 分開讓回報者能說「還是不行」)+ 待開通者亦可回報(他們最可能遇到問題卻無處可說);自動帶入 `app_version` 與 `page_url`;**自己寫 `markdown_lite`(先 escape 再只產生白名單標籤)而不引入 markdown+消毒器兩個相依**;回傳 `Markup` 讓模板免用 `|safe`(禁令不開例外);入口導引依第二條 5 由第三期提前至本期(沒有入口的表單等於不存在);migration `0007_issues` up→down→up 演練通過;**36 條紅→綠,全套 445** |
| T78 | 問題回報:附件與貼圖 | ✅ | 🟡 | 🔴 **本專案第一次開「非 attachment」的下載路徑**——截圖要在頁面上看得到就需要 inline 顯示,故六條收窄:僅回報附件端點、僅 png/jpeg/gif(magic bytes 判定)、SVG 拒收、仍帶 nosniff、僅本人與管理員可讀、單張 5 MB 上限;貼上/拖曳走漸進增強(JS 壞掉表單仍可用);讀取時**再驗一次型別**(資料被動手腳也不 inline);附件區獨立顯示(貼上但未送出的圖不會憑空消失);⚠ 依第二條 5:改為「先送出回報→詳情頁附圖」(原設想在建立表單貼圖需要回報不存在時先有 id,得引入暫存區與孤兒清理);migration `0008` 演練通過;**13 條紅→綠,全套 463** |
| T79 | 問題回報:總覽待辦整合與清除工具 | ✅ | 🟢 | 管理總覽待辦加「未處理回報 n 件」(本平台無 email/推播,**不假裝會主動通知**);已關閉滿 365 天的回報與附件清除工具(比照 purge_audit 模式);`/help` 補回報流程與「不寄信」說明;`tools/purge_issues.py` **預設 dry-run**、物件先刪再刪列、`resolved` 不算結案;**5 條紅→綠,全套 463** |
| T80 | 品牌標誌改用入口網站官方 logo | ✅ | 🟢 | 使用者交辦「把網頁服務 logo 都換成入口網站的 logo」;原本導航列的標記是 `app.css` 的 `.nav-brand::before` 偽元素(藏青圓 + box-shadow 疊綠圓)——形狀比例對,但**畫不出**官方標誌兩橢圓間交錯的白鋸齒與 SPORTON LAB. 字樣,從 portal 首頁點進來會看到「很像但不一樣」的標記;改載入官方檔 `app/static/logo.png`(與 cats-portal landing、Keycloak 登入頁**同一支**,SHA-256 比對而非目視)+ 補上原本缺席的 favicon;🔴 偽元素整段移除(不是被圖蓋住——Keycloak theme 那次就是兩層各畫一個,本機預覽的 DOM 只有一層所以看不出來);🔴 `src`/`href` 都經 `url()`(既有 `test_web_layout` 逐一掃描把關)、自託管屬 CSP `'self'` **不放寬 CSP、不用 data: URI**;**5 條紅→綠,全套 468** |
| T81 | 首頁入口導流(未登入直上登入頁、未開通回入口) | ✅ | 🟢 | Benny 2026-08-04:「點擊卡片還是會進入登入頁面,不要進到這一頁。**沒權限就回入口,沒登入就進登入頁,有權限就進總覽**」——**推翻 T53「首頁留落地頁」的那一半**(深層頁 302 的那一半不變):從 portal 卡片進來的人已經知道這是什麼系統,再讀一次介紹只是擋路;🔴 **非瀏覽器(`Accept: */*`)維持 200 落地頁**——冒煙與監控拿首頁 200 當服務活著的判準(runbook §A.4),一起改掉會讓監控在換版當下集體變紅;🔴 **停用者在 callback 就擋下、不建 session**——`optional_identity` 把 disabled 的 403 吞成 None,網頁層看不出「停用」與「匿名」的差別,不擋就會 首頁→IdP→callback→首頁 無聲繞死;🔴 入口網址誤設成指回本服務時退回 `/pending`(迴圈防線);T64 靜默探測在首頁功成身退(互動式登入同樣達成免再按登入,差別只在沒 session 時);**F79 版本號隨之搬到共用版型**,否則匿名與待開通者再也看不到版本號;**8 條紅→綠 + 23 條既有測試改寫,全套 477** |
| T82 | 修 CI:cryptography CVE-2026-69247 | ✅ | 🟢 | PR #24 的 Trivy 步驟擋下建置——`cryptography` 是 `pyjwt[crypto]` 的**遞移相依**,requirements 用相容區間所以每次建置抓當下最新可解版,這次抓到帶洞的 49.0.0(HIGH,fixed in 50.0.0);**與 T81 無關,main 現在建置也會紅**;修法是加明確下限 `cryptography>=50.0.0`,🔴 **不用 `.trivyignore` 換綠燈**(本服務散布可執行檔,掃描是自我加嚴的一環,調鬆掃描是拿紅線換方便);🔴 lock 檔化(pip-compile)刻意**不順手做**——影響面遠大於一個 CVE,該有自己的計畫;升級後全站 **477 綠**(JWT 驗簽是實際會走到的路徑,不是裝了沒用) |
| T83 | 發版前置:`APP_VERSION` → 0.2.1 | ✅ | 🟢 | PR #24 合併後 main 的常數還停在 `0.2.0`,而 v0.2.0 tag 指的是 `c504b1c`——**程式碼自稱的版本與 tag 指的 commit 是兩件事**,正是憲法第八條 4 要防的「首頁對使用者說謊、換版驗證判錯」;選 0.2.1 是因為 T81 為行為調整、T82 為相依修補,**無新增功能面**;🔴 **不重發 v0.2.0**(第八條 5;該版已部署到 VM,移 tag 會讓「使用者手上那一版」與「tag 說的那一版」分家),比照 T69 沒搭上 v0.1.6 的處置;**不寫新測試**——既有版本號測試全部讀同一常數(T68 的單一真相),再寫 `assert == "0.2.1"` 只是把字面值抄第二遍、抓不到任何錯,比照第三條 4 純重構條款;全套 477 綠 |
| T84 | 稽核紀錄顯示使用者名稱 | ✅ | 🟢 | Benny:「稽核紀錄顯示使用者名稱」——原本兩排 UUID,認不出「誰把誰開通了」,而**認不出人的稽核紀錄等於沒有稽核紀錄**;來源是 T59 的 `display_name_cache`;🔴 **名字只上後台 HTML,不上 JSON API**(§4.2a L1 的例外僅涵蓋「管理後台顯示」,`AuditEventOut` docstring 亦寫明「稽核不是繞過個資紅線的後門」——API 帶名字等於開一條整批匯出姓名的路);🔴 **一次 `IN` 查完不逐列查**(設計文件 §2 查詢數固定;有測試以 `before_cursor_execute` 實際計數,1 列與 10 列相同);🔴 **UUID 不因為有名字就拿掉**(名字只是登入當下的快照,id 才是身分事實);模板過時註解依第二條 5 回寫(原文寫於 T59 之前,當時確實無名可用);🐛 過程中抓到**自己寫的假綠測試**——手動塞名字會被「每次登入覆寫」清成 NULL,導致「API 不含名字」在根本沒有名字時通過,已改由登入 claim 寫入並加前提斷言;**8 條紅→綠,全套 485** |
| T85 | 待開通清單顯示名稱與申請時間 | ✅ | 🟢 | Benny 實測回報「還是沒有申請者名字與身分」;根因是 **T59 只改了一半**——`admin_users.html` 兩個迴圈,名字只加在「其他帳號」,「待開通」仍只印 sub,**而漏掉的正好是最需要名字的那一半**(開通是本頁唯一有後果的操作,認不出是誰就不該按);頁首「所以下面只有識別碼、沒有名字」自 T59 起即不成立,依第二條 5 回寫;補**申請時間**當名字空白時的替代線索(時間非個資,不動紅線)——空白是常態不是缺陷(IdP 無 firstName/lastName,或最後登入早於 T59),真正解法是 T73;🔴 不加 email、不用 `preferred_username` 補位、表單仍以資料庫 id 為鍵(L1 第 8 條);🐛 順帶修掉 `test_管理後台不顯示個資欄位`——它整頁搜尋「姓名」,**T59 之後名字本來就會顯示**,它能繼續綠只是文案剛好沒用到那兩個字;改為以 `@` 掃全頁斷言「不得出現 email」(比原本更嚴),而非改文案繞過它;**6 條紅→綠,全套 491** |
| T86 | 上傳頁改三格卡片 + 取消上傳 | ✅ | 🟢 | Benny 實測:「1. 上傳錯誤 無法取消 2. 直接三成三格卡片 說明 程式碼 執行檔」;🐛 **無法取消的根因**是 `upload.js` 送出後停用唯一的按鈕、又**沒設 timeout**——連線卡住(不是斷線,`onerror` 不會觸發)時進度條停住、按鈕永遠停用,只能重整整頁;修法是取消鈕 + timeout + **`onabort`**(少了它取消後畫面停在「上傳中 47%」,比不能取消更像當掉);介面把 T65 的三類齊備表達成「檔案輸入 + 類型下拉」,**下拉有預設值,不選就是錯的那一類**(截圖正是 `files.zip` 配 `doc`),且「還缺什麼」在頁面最底、離「在哪裡補」隔了整頁——改成一格一類後需求本身就是介面;`_REQUIRED_KINDS` 併成 `REQUIRED_KINDS` 具名結構(卡片與缺項訊息同一份資料與順序);🔴 伺服器端 magic bytes / kind 比對一步未減(前端只是體驗不是防線)、仍是 raw body PUT、CSP 未放寬、已發布版本仍無任何上傳入口;**9 條紅→綠,全套 500** |
| T87 | OIDC 失敗記下 IdP 的 `error` 碼 | ✅ | 🟢 | 兩次登入故障排查時 log 只有 status,`invalid_grant`(code 用過/過期/PKCE/redirect_uri 不符)與 `invalid_client`(secret 錯)長得一模一樣,**只能靠猜**;原註解「不記(可能含 token)」過度保守——token 端點的**錯誤**回應依 RFC 6749 §5.2 只有 error / error_description / error_uri,不會有 token(有 token 的是 200 那條,本來就不記);🔴 **只記 `error`(列舉值)不記 `error_description`(自由字串,IdP 可能塞進使用者名稱或請求片段)**;🔴 body 非 JSON(gateway 回 HTML 錯誤頁)要安靜降級,記 log 不得變成新的例外;🔴 回應給呼叫端的文字一字不改(錯誤碼是維運資訊);`refresh` 原本**完全沒有 log**,一併補上;**6 條紅→綠,全套 506** |
| T88 | runbook 發版前補完(不可逆清單 + purge cron) | ✅ | 🟢 | 2026-08-02 明日待辦第 1 項(當時就寫明「發版前,不是發版後」)結案:§B 不可逆回滾清單補 `0007_issues` / `0008_issue_attachments`——🔴 與既有兩列**性質不同**(`0004` 下載數、`0005` 稽核失去的是系統紀錄;這兩支失去的是**使用者親手寫的內容**,無補救管道),故加註退版前須備份**並當場驗讀得回來**;§C.2 cron 補 `purge_issues.py` 並明寫 ⚠️ 旗標是 `--yes` **不是** `purge_audit` 的 `--apply`——抄錯會每天空跑而且**不會有任何錯誤訊息**(預設 dry-run 正常結束、正常寫 log);runbook v1.6 → **v1.7**;純文件,依第三條 4 不寫新測試 |
| T89 | 側欄「回到平台入口」移到最下面 | ✅ | 🟢 | Benny 實測回報(附側欄截圖):「把回到入口連結放到最下面」;T67 原本放**最上方**,理由是「離場動作與站內走動是不同層級」——**層級判斷沒錯,方向錯了**:同一份理由更支持放最後,第一眼該看到的是「在這個 App 裡能做什麼」,不是「怎麼離開」,把離場動作擺在視線起點等於每次進站都先被問要不要走;貼底用 `.side-exit { margin-top: auto }`(側欄本來就是 flex column)**而不是塞空白元素**——項目數隨管理權限而變,固定間距一定有一種身分看起來是壞的;🔴 **頂列(漢堡選單)刻意不動**:窄螢幕清單最後是使用者下拉與登入/登出,把離場連結插到登出後面會讓兩個「離開」互相干擾;T67 的模板註解依第二條 5 回寫(原文寫「放在最上方」,自本任務起不成立);**2 條紅→綠,全套 508** |
| T90 | 建立版本頁顯示歷史版本號 | ✅ | 🟢 | Benny 實測回報(附截圖):版本號欄位只有 placeholder `v1.0.0` 與「不強制 SemVer」,**要填的人手上沒有前一版是什麼**;🔴 這不只是方便——版本號有 UNIQUE 約束,撞號要送出之後才看得到錯誤,而正確值就是「上一版的下一個」;查詢共用 `queries.query_releases()`(與版本歷史頁、API 同一份排序,分岔的後果是同一專案在兩頁「最新版」不一樣而兩邊都不報錯),`include_drafts=True` 且**草稿必須標示**(不標的話會看到版本號已存在卻找不到它發布在哪);🐛 **既有測試當場抓到 MissingGreenlet(本 repo 第四次同型)**——撞號那條路徑先 `rollback()`,rollback 讓先前查出來的 ORM 物件全部過期,模板一讀就炸;修法是把清單查詢搬進 `_back()` 內(rollback 之後才查),而不是在失敗頁把清單省掉——**填錯的那一次最需要它**;測試以區塊而非整頁比對(整頁搜尋會撞到 placeholder `v1.0.0`,那種綠燈比紅燈危險)、並釘住不列別的專案的版本號;**5 條紅→綠,全套 513** |
| T91 | 使用教學頁加上頁面圖(視覺化教學) | ✅ | 🟢 | Benny:「教學加上 頁面圖 的 svg 檔 視覺化教學」;現況七步全是文字,而使用者卡住的地方通常是**空間問題**(「建立版本的按鈕在哪一頁」、「三個上傳格子是同一頁還是三頁」),文字回答不了空間問題;三張內嵌 SVG:①快速上手七步流程(標出唯一的閘門「三類齊備才亮起發布」)②**頁面圖**——側欄 + 首頁/專案頁/上傳頁三畫面與該按的位置(含 T89 之後移到最下面的入口連結)③三類齊備與發布鈕的狀態關係;🔴 **內嵌 SVG,不用 `<img>` 也不用 data: URI**(CSP `default-src 'self'` 擋 data:,內嵌是標記不是資源載入,離線可開)、🔴 顏色寫死十六進位對齊設計 token(presentation attribute 不吃 `var()`,而 `style="…"` 是 inline style 被 CSP 擋)、🔴 SVG 內不得有 `<script>`/`onload`(自家頁面比上傳檔更不該有可執行內容);🔴 **圖裡的七步與 `help-steps` 清單以測試對釘**——改一邊沒改另一邊會紅,**圖與文字互相矛盾比沒有圖更糟**;圖放獨立模板 include(三張圖的座標會把教學文字擠到看不見);**6 條紅→綠,全套 519** |
| T92 | 本機(WSL)驗證用開發伺服器 | ✅ | 🟢 | Benny:「本地 wsl 驗證」;**現況做不到**——側欄與建立版本頁只在「已開通登入者」的版型出現,而本專案紅線是不自建帳號系統,`docker compose up -d` 只給 PG + MinIO **沒有 IdP**,於是介面改動只能靠讀 HTML 或等部署到 VM(T85 / T86 都是這樣拖到實測才發現的);`tools/devserver.py` 用**測試已經在用的替身**(`FakeOidc` / `FakeStorage`)+ SQLite 檔把真的 app 跑起來,造一個管理員 + 示範專案 + 兩個版本(**刻意一個 published 一個 draft**,只有一種驗不到 T90 的草稿分支),並多掛 `/dev/login` 直接種 session cookie;🔴 **會偽造 session,所以安全性靠三層結構不靠自律**:①沒有 `DEV_UNSAFE_LOCAL=1` 就 `SystemExit`(拒絕啟動,不是印警告然後照跑)②只綁 `127.0.0.1`(對外綁定會讓本機用的假登入變成任何人都能用的假登入)③**image 裡根本沒有這支腳本**——Dockerfile 只 COPY `app/` 與 `alembic/`,而它 import `tests/`,兩層都不在 image 內,正式環境連 import 都會失敗(這層不靠設定);四條 + 「`app/` 不得提到 devserver」皆反向驗證;🐛 **計畫漏了一層:gateway 剝前綴**——第一次截圖時側欄 active 反白不見,根因是正式環境由 gateway 把 `/upload/` 剝掉才轉給我們,本機直接打則 `current_path == '/'` 永遠不成立,**畫面看起來像壞的而程式其實是對的**(與 T52「替身與真實行為的落差本身就是風險」同型),補 `strip_prefix()` ASGI 包裝並依第二條 5 回寫;**用 Chromium 實際截圖驗過 T89 / T90 / T91 三頁**;**8 條紅→綠,全套 527** |
| T93 | CI Trivy 紅燈:base image 的 util-linux CVE | ✅ | 🟢 | PR #28 的 CI:測試/lint 綠(527),**Build image + Trivy 紅**——`util-linux` / `mount` / `login` / `bsdutils` 的 CVE-2026-53612~53615(TOCTOU、SUID mount 繞過 nosuid、libblkid 整數溢位);🔴 **與本批改動無關**:PR 沒動 `Dockerfile` 也沒動 `requirements*.txt`,而**上一次 main 的 CI 是綠的**(08-11 run #173),期間 Debian 公布了這四支 —— 環境漂移,與 T82 同型;Trivy 帶 `--ignore-unfixed`,**能被列出就代表 Debian 已有修版**;🔴 **兩條看似顯然的修法在本專案行不通**:①Dockerfile 加 `apt-get upgrade` —— **VM 連不到 apt 套件庫**(2026-07-30 run #85 就是為此把 builder 的 apt 整段拿掉,Dockerfile 檔頭有紀錄),加回去等於在同一個地方再炸一次;②`.trivyignore` —— T82 已明文拒絕(散布可執行檔的服務,調鬆掃描是拿紅線換方便);故先做最便宜的一步:CI build 加 **`--pull`** —— runner 是**同一台 VM**、本機層快取長期存在,沒有 `--pull` 的話 `FROM python:3.12-slim` 會一直用第一次拉下來的那份,而 base image 走 dockerd 的 registry mirror,那條路是通的;**掃描門檻一字未改**;⚠ 若 `--pull` 後仍紅代表上游未重建 → 選項依序:等上游重建(期間不發版)/ 換已含修版的 base tag(另開任務)/ Benny 明示逐 CVE 帶到期日的一次性例外;**不寫新測試**(第三條 4):這個環境沒有 docker daemon 也沒有 Trivy,**驗證只能在 CI 上發生**,不是我宣稱它會過。**狀態 🔵:CI 轉綠才轉 ✅** |
| T94 | 發版前置:`APP_VERSION` → 0.2.2 | ✅ | 🟢 | 🔴 **這批不能用 v0.2.1——它早就發布過了**(tag 指向 `568014b`,即 T87/T88 合併後的 main);第八條 5 同版不重發,故走 **v0.2.2**,並依第八條 4 在打 tag 前隨 PR 改好常數;⚠ **本表的「三個版本號」表寫「最新 tag = v0.2.0、v0.2.1 待發」是過期記載**,發版規劃照它做,結果 VM 在 15:34 換到的 `v0.2.1` 是 T87/T88 那一版、**不含 T89–T93**(沒造成故障,但「照著文件做卻部署了不是預期內容的版本」正是第七條存在的理由),已依第二條 5 回寫該表;**不寫新測試**(第三條 4,同 T83):既有版本號測試全部讀同一常數,再抄一遍字面值抓不到任何錯;遺留建議:CI 加守門比對 `APP_VERSION` 與 GitHub 最新 tag(常數不得 ≤ 已發布最新 tag),另立任務不夾在發版裡做 |
| T95 | 版號位置改到側欄底部 | ✅ | 🟢 | Benny 實測回報(截圖:箭頭由右上指向左下);**現況那個位置不是設計是意外** —— `app.css` 從來沒有 `.app-version` 規則,那個 `<p>` 只是 `.layout`(flex)裡排在 `<main>` 之後的第三個項目,於是被擠到右上角;🔴 **不能整個搬進側欄**:側欄只有「已登入且已開通」看得到,而 T68 放版號的理由是**三種身分都要看得到**(回報問題報得出版本、換版驗證不必挑哨兵檔),T81 才剛為了保住這點把它搬到共用版型 —— 整個搬進去等於把 T68 悄悄廢掉;故拆成兩半:有側欄 → 側欄最底(入口連結下方)、無側欄 → 維持原位,**兩者以同一個條件互斥**(同頁印兩次,回報的人不知道該報哪一個);測試釘的是**順序**(`side-exit` < `side-version` < `</aside>`)而不是「頁面上有沒有」——後者在版號還在右上角時也會綠,那種測試不保護任何東西;⚠ 誠實標註:三條新測試裡只有位置那條是紅起步,另兩條釘的是「搬動時不要弄壞的東西」;devserver + Chromium 截圖驗過;**3 條新增(1 紅→綠),全套 530** |
| T96 | 專案短名(slug)自動產生 | ✅ | 🟢 | Benny:「專案短名(網址用) 改成自動幫專案產生」;原本表單第一個欄位就是**必填**的 slug —— 使用者要建專案,第一件事卻是先想一個網址用的英文短名,那是實作細節不是他要做的事;新增 `app/slugs.py`(`slugify` / `fallback_slug` / `unique_slug`),`My Cool Tool` → `my-cool-tool`;🔴 **產生的短名一律通過既有 `SLUG_RE`**(自動產生不是放寬規則的藉口,它進的是同一個欄位、同一條網址);🔴 **撞名自動加後綴** `-2`、`-3` 而不是把錯誤丟回使用者(欄位都拿掉了,叫他「換一個短名」是叫他改一個看不到的東西),併發仍靠 DB 的 UNIQUE 約束,web 端重試三次;🔴 **API 維持可指定 slug**(向下相容,腳本使用者要的正是可預測的短名),只是改為選填;⚠ **一條既有測試換了契約**(`test_slug重複時回到表單並顯示訊息` → `test_同名專案不再回錯誤而是自動換一個短名`)、另一條的**觸發方式**改了(逸出測試原本靠非法短名,那條路徑不再存在,改用空白名稱)——**不是放水**,依 CI 紅線在 dev-log 逐條說明;其餘 5 支只是把「表單送 slug」改成「表單送名稱」,**刻意沒讓端點偷偷接受畫面上不存在的欄位**(那會讓測試綠得毫無意義);🔴 **實測揭露的代價:中文名稱 → `p-1ded8d7a` 這種不可讀網址**,而本平台專案名多半是中文,**是常態不是邊緣案例** —— 三個選項(維持 / 引入 `pypinyin` 新相依 / 選填欄位)待裁示,不自行引入相依;**10 條新增(9 紅→綠),全套 540** |
| T97 | 發版前置:`APP_VERSION` → 0.2.3 | ✅ | 🟢 | v0.2.2 的常數在 T94 就備好,但 **tag 一直沒推**(本 session 憑證推 tag 回 403,需 Benny 手動;在那之前 T96 先合併進 main)→ main 的內容變成 T89–T95 **+ T96**;🔴 這時推 `v0.2.2` 會讓 tag 指到**含 T96** 的 commit 而常數與 Release 都說 0.2.2,**正是第八條 4 要防的「對使用者說謊」**;Benny 裁示發 **0.2.3 一版含 T89–T96**(少發一次版、VM 只換一次);⚠ **`v0.2.2` 就此跳過永遠不會存在** —— 它的內容沒有單獨出貨過,補發一個指向舊 commit 的 tag 只會多一份沒人跑過的版本(Release 需交代一句,否則看 tag 列表的人會找它);**不寫新測試**(第三條 4,同 T83/T94);🔴 **這是第二次因版本號記載卡住發版**(T94 是任務表寫錯最新 tag),守門建議(CI 比對常數與最新 tag)更該做 —— **兩次都是靠人臨場發現,那不是機制** |
| T98 | CI 建置遇失效層快取時自動重試 | ✅ | 🟢 | `v0.2.3` 推 tag 後 **`Build image` 第一步就死**(32 秒),Trivy 與 `Push GHCR` 全部 skipped ——**發版被擋在最後一哩**;根因不是程式:`failed to compute cache key … could not fetch content descriptor sha256:4857… from remote: not found`,即本機層快取指向**已經不存在於 registry 的 base image layer**(T93 加的 `--pull` 更新了 manifest,舊 blob 被回收 —— 這是那個正確決定的**已知副作用**,不是它的錯);修法:build 那步加 fallback,第一次失敗就以 `--no-cache` 重試一次(build 與 publish 兩處都加,**發版那一步失敗的代價最大**);🔴 **不會遮蓋真正的建置錯誤**——程式壞掉時第二次一樣失敗、job 照樣紅,只在第一次失敗時多花 1–2 分鐘;⚠ **本次的 v0.2.3 不會因此變綠**(重跑用的是 tag 那個 commit 的 workflow 檔),當下解法是 VM 上 `docker builder prune -f` 再重跑,本任務要的是**下一次不必靠人記得清快取**;**不寫新測試**(第三條 4):CI 建置環境無法在本機重現(無 docker daemon),宣稱測過會是假的 |
| T99 | 回報寄信通知管理員 + 後台入口 | ✅ | 🟡 | Benny:「有人回報問題會寄信給 ADMIN,然後 ADMIN 要有後台介面」,裁示三題(寄給**每位管理員** / **公司內部 relay** / **失敗只寫 log**);獨立計畫書 [設計_回報通知信與管理後台](plans/設計_回報通知信與管理後台.md) v1.1;🔴 **先盤點才實作**——管理員看全部回報 / 回覆 / 改狀態 T77 就有、待辦卡片 T79 就有,**缺的只是側欄常設入口**(待辦卡片沒有待辦時不會出現),當成「從零做後台」會做出第二套清單;寄信要知道管理員信箱,而本專案紅線是業務庫不存 email → 合法路徑是契約 **§4.2a L1b 通知用信箱**(2026-08-12 通則化,**明文不必申請**),逐條落地八項條件(只信 `email_verified=true`、**缺席不等於通過**、每次登入覆寫含成 NULL、不得顯示在任何頁面〔三頁掃描斷言〕、不進 log、不得作 join key/授權、nullable + `tools/purge_notify_email.py`、失敗不阻斷);`app/mailer.py` **只用標準庫 `smtplib`**(不新增相依,比照自寫 `markdown_lite`)、`MAIL_ENABLED` 預設 **false**(沒設好就完全不寄,而不是每次回報都噴錯)、`BackgroundTasks` 在**回應之後**才寄、**一人一封**(不用 To 塞多人,管理員彼此看得到信箱是沒必要的外洩面)、信件**不含回報全文**(信箱不是稽核紀錄,內容可能含敏感資訊,只放標題 + 直達連結);migration `0009` up→down→up 演練過;⚠ **施工中改正三件事**(依 CI 紅線說明):①「`/v1/me` 不得含 email」的斷言**是我寫錯的**(那是 IdP claims、呼叫者自己的信箱,L1b 管的是快取副本),照它改程式會拆掉合法功能 ②log 測試層級 DEBUG→INFO(DEBUG 會打開 aiosqlite 的 SQL 參數輸出)→ 🔴 **得出新結論:正式環境不得開 DEBUG,否則信箱隨 SQL 參數進 log**,已寫進 `.env.example` ③🐛 **截圖抓到側欄同時反白兩項**(回報問題與回報管理都是 `startswith('/issues')`),已收窄並加「任何頁面反白 ≤ 1」的測試;`CLAUDE.md` 紅線同步升 **v2.4**(L1/L1b 是**通則落地**不是個案例外,密碼不落地那條原封不動);🔴 **遺留:`MAIL_ENABLED` 仍 false,且沒人驗過那台 relay 連得到** —— 上線前要在 VM 實測;**14 條新增(10 紅→綠),全套 554** |
| T100 | 發布者設定查看權限 + ADMIN 都看得到 | ✅ | 🟢 | Benny:「發布者可以設定使用者查看權限」+「ADMIN 則都可以看到」,裁示**輸入名字搜尋** + **沿用三種角色**;獨立計畫書 [設計_專案查看權限管理](plans/設計_專案查看權限管理.md) v1.1;🔴 **盤點出兩件事決定了任務形狀**:①權限模型與 API 早就齊備(`visibility` / `ProjectMember` / `require_project_read` / 成員 API / 稽核字彙),**缺的只是介面** —— 從零設計會做出第二套規則而兩套遲早分岔,那是 private 專案外洩的起點,所以把邏輯抽成 `app/members.py` 讓 **API 與網頁共用一份**(API 那份重複的程式碼直接刪掉);②**「ADMIN 都看得到」施工前就成立但沒有任何測試**(只靠兩行 `is_admin`),使用者明確要求的行為沒有守門 = 下次有人「優化」那兩行不會有東西攔他,補 3 條;介面:專案頁「查看權限」區塊(可見性下拉 + 成員清單 + **純 HTML GET 搜尋表單**,零 JS 因 CSP 擋 inline script);🔴 **搜尋是唯一的新外洩面**,五條約束逐一有測試(空白 q 不列全部 —— 少了這條它就是「整份名單匯出」API、最多 5 筆、只回 active、**絕不回 email**〔L1b 第 14 條〕、只有 owner 能用);新增稽核 `project.set_visibility`(改「誰看得到」必須留痕,事後問「什麼時候變成公開的」沒紀錄就等於沒答案);🐛 **重構時字串定位吃掉了 `transfer_owner`**(`if payload.user_id == project.owner_id:` 在兩支處理器裡都有),既有測試 30 秒內抓到並還原 —— 值得記的不是我改錯,而是那 41 條測試就是為此存在;🟢 **無 migration**;**12 條新增(5 紅→綠 + 3 條補守門),全套 566** |
| T101 | 專案標題與簡介可編輯 + 修一條權限不一致 | ✅ | 🟢 | Benny:「專案標題與內容 發布者都可以編輯」;專案頁新增「編輯專案」區塊(標題 + 內容,**maintainer 以上**,與 API 同一條界線),驗證沿用 `ProjectUpdate`(介面不得比 schema 寬鬆);🔴 **本任務真正的價值是施工前讀 API 時發現的一條外洩路徑**:`PATCH /v1/projects/{slug}` 只要 maintainer,而 `ProjectUpdate` **含 `visibility`** —— T100 昨天把「誰看得到」定為 owner 專屬**但只在介面與 web 路由上**,於是 **maintainer 打一次 API 就能把 private 專案改成全公司可見**;T100 的計畫書寫過「介面比 API 寬鬆等於開第二套規則」,這次是**反過來:API 比介面寬鬆,而寬的那邊才是真正生效的那邊**;修法是 PATCH 收到 `visibility` 時要求 owner(**收窄**,唯一行為變化是那條路徑從成功變 403),三條測試(maintainer 403 / owner 200 / 只收窄 visibility 不連坐 name);新增稽核 `project.update`(業務庫只留最新值,「以前叫什麼」只有稽核答得出來);🔴 **短名刻意不可改**(在網址裡、平台無轉址,改了會讓已分享的連結死掉,T96 已載明),畫面明寫這句而不是讓人試了才發現;🔴 **可見性不放進這張表單**(混兩種權限遲早被一起放寬);⚠ 施工中兩個小坑都被抓到:`project_limit` 參數順序寫反(測試當場紅)、瀏覽器看不到新區塊其實是**舊 devserver 程序在跑舊程式碼**(下次先確認跑的是新版);**8 條新增(5 紅→綠,其中「maintainer 不能用 API 改可見性」一開始是紅的 —— 那條路徑當時真的是通的),全套 574** |
| T102 | base image 改全量升級 + 驗證無殘留 | ✅ | 🟢 | T101 那輪 CI:測試/lint/文件 ✅(574)、image/non-root/冒煙 ✅,**只有 Trivy 紅** —— `libssl3t64` / `openssl` / `openssl-provider-legacy` 的 **CVE-2026-14456**(OpenSSL QUIC server 記憶體無上限成長 → DoS),3.5.6-1~deb13u2 → 修版 3.5.7-1~deb13u2;🔴 **根因不是這支 CVE,是 T93 的白名單設計在收費**:T93 明列八個 util-linux 套件並在 dev-log 寫「日後其他來源的 CVE 就再開一個任務、再加一行」——**三天後就來了第二次**,而 base image 的 CVE 是**持續發生的環境漂移不是偶發事件**;每支新 CVE 都要改程式 + 一輪 CI + 一個任務編號,而 🔴 **常態紅燈的下一步是「先合併再說」,那才是真正的風險**;改為 `apt-get upgrade -y` 全量升級,並**保留 T93 最有價值的那一半:驗證不相信離開碼** —— 形式從「比對某個版本號」換成「**升級後不得還有可升級的套件**」(`apt-get -s upgrade` 的 `Inst` 計數必須為 0),不必知道有哪些 CVE、不必維護清單,同樣擋得住 T93 第五輪那個「apt 回 0 卻什麼都沒做」的無聲失敗;⚠ **誠實記下放棄的東西**:「diff 上看得見升了哪些套件」不再成立(要看 build log)——這是交換不是忽略;**不寫新測試**(第三條 4,同 T93/T98):CI 建置環境無法在本機重現,宣稱測過會是假的;**CI run #249 全綠**(574 passed + Trivy ✅,openssl 那 3 條消失;image 大小/non-root/冒煙同時維持綠),掃描門檻一字未改;⚠ 學到的事:T93 的白名單不是錯的決定而是**有到期日的決定** —— 判斷防線該不該換的訊號不是它壞了,而是**它要求人反覆做同一件事**,那種防線最後會被繞過 |
| T103 | 發版前置:`APP_VERSION` → 0.2.4 | ✅ | 🟢 | Benny:「發 0.2.4」;第八條 4 常數必須在**打 tag 之前**隨 PR 改好,本任務就是那一步;🔴 **為什麼是 0.2.4**:`v0.2.3` 已經發布過(tag 指向 `46d265f`,內容 T89–T96),第八條 5 同版不重發;**為什麼是 patch 而非 minor**:這一批沒有改變任何既有介面的契約 —— T99 是新增功能、T100/T101 是把既有權限模型接上介面、T98/T102 是建置面,唯一的行為變化是 T101 的**收窄**(`PATCH` 收到 `visibility` 要求 owner),那是修補外洩路徑不是使用者會依賴的介面;🔴 **本任務 🟢 不動資料,但這一版整體是 🟡** —— 同批 T99 含 migration `0009_notify_email`,**換版必須跑 `alembic upgrade head`**,已寫進 Release 的「部署注意」;**不寫新測試**(第三條 4,同 T83/T94/T97):既有版本號測試全部讀同一常數(T68 的單一真相),再抄一遍字面值抓不到任何錯;🔴 **守門仍不存在,而這是第三次提**(T94 任務表寫錯最新 tag、T97 tag 沒推而 main 前進、本次靠人記得 0.2.3 已用過)—— 建議 T104:CI 比對 `APP_VERSION` 與 GitHub 最新 tag,常數 ≤ 已發布最新 tag 就紅;⚠ 刻意**不夾在發版 PR 裡做**(同 T94 判斷):守門本身要一輪 CI 驗證,混進發版會讓「發版被自己的新守門擋住」變成可能 |
| T104 | 發版補救:`APP_VERSION` → 0.2.5 | ✅ | 🟢 | 🔴 **`v0.2.4` 的 tag 打在 `6dabe7d`**(PR #34 的合併,**PR #35 之前**),而該 commit 的 `APP_VERSION` 是 **`0.2.3`** —— tag 那輪 CI(run #256)success,所以 **`ghcr.io/…:v0.2.4` 已經推上 GHCR 而它的首頁會顯示 v0.2.3**,正是第八條 4 要防的「對使用者說謊」,而且換版驗證(看側欄版號)會判成換版失敗、實際上是版號本身錯的;🔴 **不刪 tag 重推**:①第八條 5 同版不重發(原文結論來自 v0.1.3 事故)②image 已流出,重推會讓**同一個 tag 在不同人手上是不同的東西**,比錯一次更難查 ③刪已發布的 tag/Release 是破壞性動作,換到的只是「版號數字漂亮」;故走既定解**發下一個 patch 版 v0.2.5**,內容與 v0.2.4 預定要發的完全相同(T98–T103),差別只有版號;⚠ `v0.2.4` 的 tag 與 Release **保留不刪但須標明作廢並指向 v0.2.5**(不標的話後來的人會照它部署);🔴 **根因不是誰不小心 —— 這是第三次卡在版本號上,而形狀一次比一次糟**(T94 文件寫錯最新 tag → 部署到非預期版本;T97 tag 沒推而 main 前進 → 版號與內容分家;**本次 PR 合併與 tag 的時序交錯 → 錯的 image 真的上了 GHCR**),而**前兩次的結論都只寫成「建議」,建議不會執行自己**,三次都靠人臨場發現(這次是靠截圖裡那行 `6dabe7d`);故守門升為**下一個要做的任務 T105**,不再是一句建議;**不寫新測試**(第三條 4)⚠ 但誠實講一句:那個慣例對「常數與 tag 對不上」**本來就無效**,它不是測試抓得到的東西,得由 CI 拿 GitHub 的 tag 來比 —— 那正是 T105 |
| T106 | 未上傳完成的檔案不顯示 | ✅ | 🟢 | Benny 截圖:專案頁出現 `下載 dashboard.html  binary · 0 bytes · 0 次下載`,按下去會 404 →「沒有上傳成功的 就不用顯示了」;🔴 **根因不是新判斷**:下載端點、三類齊備、配額計算**早就**只認 `upload_status is ready`,**只有網頁清單那一層沒有** —— 於是**畫面在承諾一件伺服器已經拒絕的事**,而使用者會以為是自己的網路壞掉;新增 `queries.ready_artifacts()` 一份共用(三個模板各寫一次的話,分岔方向是「某一頁又開始顯示壞掉的檔案」),專案頁與版本歷史頁改走它,**「N 個檔案」計數同步過濾**(寫 4 個卻只列 3 個,讀的人會以為頁面壞了);⚠ **上傳頁刻意不過濾**:那是擁有者的作業面,看不到失敗的檔就不知道要重傳,藏起來會讓殘留列變成沒有入口的幽靈 —— 有反向測試釘住;**4 條新增(2 紅起步 + 2 條守門防我過濾過頭),全套 578**;🔴 遺留 T107:殘留列本身沒清掉,且 `dashboard.html` 這個檔名代表 **HTML 拒收有生效但列先建了**,每次被拒收的上傳可能都留下一列 0 bytes 殘骸 |
| T107 | 被拒收的上傳不留殘骸 | ✅ | 🟢 | T106 的遺留,**根因在原始碼證實不是推測**:`artifacts.py` 的 PUT 是「建列 → commit → 收串流 → 判型」,而先建列是**必要的**(storage key 需要 `artifact.id`)—— 錯的是失敗後把列留下來;於是 `dashboard.html` 被 HTML 紅線擋下卻留下一列 `failed` / 0 bytes / SHA-256 空白,而且**每一次被擋下的上傳都會再留一列**;🔴 改成失敗即刪列(`_discard()`,五條失敗路徑共用),並 best-effort 刪掉可能寫到一半的物件 —— 代價誠實記下:**使用者無法事後在畫面上看到上次為什麼失敗**,判斷可接受的理由是失敗原因當下就以 RFC 7807 回去了(`rejected-file-type` 帶 `detected_type`)、log 也有一行,而**留著的那一列不會告訴任何人原因,它只顯示 0 bytes** —— 不帶資訊的殘骸不是紀錄是垃圾;補稽核 `artifact.upload_rejected`(`action` 欄位是 `String(64)` 故**零 migration**,`models.py:308` 的註解就寫著「刻意讓新增動作不需要 migration,因為阻力會讓人選擇不記」),於是「有人反覆試著傳被擋下的檔」變成查得到的訊號;既有殘骸靠新增的 `tools/purge_failed_artifacts.py` 清一次(**預設 dry-run**,`--apply` 才刪,只碰非 ready、條件寫死不接受參數放寬),runbook §C.3 明寫**不掛 cron**(不會再產生新殘骸,排程化只會讓一支平常不做事的腳本哪天大量刪東西);⚠ **一條測試我猜錯了**:以為「超過上限」會留列,實際上它在 `Content-Length` 階段就被擋、列根本還沒建 —— 先寫測試把「我以為的失敗路徑」與「實際的」分開了,該條留著當守門;**5 條新增(2 紅起步 + 3 守門),全套 583** |
| T108 | 後台使用者登入紀錄 | ✅ | 🟢 | Benny:「後台 新增使用者登入 紀錄」;🔴 **先盤點才做**:`users.last_login_at` 欄位、稽核表、後台稽核頁**都早就存在** —— 缺的是兩件事:①**登入沒有進稽核**(`last_login_at` 只答得出「最後一次」,查不到「這個月登入過幾次」)②**那個欄位存了兩個月,一個畫面都沒顯示過**;新增 `user.login`,以及 🔴 **`user.login_denied`(停用帳號嘗試登入)—— 這一條比成功登入更值得記**:契約 §1.1 明載「離職不會自動停用 IdP 帳號」、我方 deny-by-default 是第二道防線,而那道防線擋下了誰原本只留在**會滾掉的 stdout log** 裡;後台使用者頁顯示最後登入,沒有值時**明寫「從未登入」而不是留白**(空白與「沒顯示」在畫面上長得一樣,而「這個帳號從來沒人用過」是開通後最該被看見的事實);⚠ 待開通清單**刻意不改**(那裡已有「申請於」,而待開通者的建立時間就是首次登入時間,再加一欄只會出現兩個幾乎一樣的時間);🔴 **不記 IP 是刻意的**:稽核表目前一個網路識別欄位都沒有,為登入單獨加要 migration,而 IP 在內網幾乎等於指名道姓 —— 要記應該是全表一致的決定,不該夾在這裡開特例;⚠ **一條測試是假綠**(「稽核不含 email」在沒有登入事件時迴圈跑零次、斷言等於沒執行),加上功能後才真的在驗 —— 這種綠燈比紅燈危險,已寫進 dev-log;**7 條新增(6 條紅起步),全套 590**;🟢 零 migration(`action` 是 `String(64)`,`models.py:308` 早為此鋪路) |
| T109 | 維運腳本進 image(runbook 指令真的跑得起來) | ✅ | 🟢 | **撞到才發現的,不是 review 出來的**:Benny 照 runbook §C.3 清殘骸拿到 `can't open file '/app/tools/purge_failed_artifacts.py'`;第一層解釋是「那支工具太新」,但**根因是 `Dockerfile` 從來沒有 COPY `tools/`** —— 而 `test_devserver.py` 還有一條測試**明文禁止**它(T92 為了讓 `devserver.py` 進不了正式環境蓋的第三層防線);🔴 **於是 runbook §C.2 的兩行 cron 從寫下的那天起就不可能執行成功**,⚠ 實測 `crontab -l | grep purge` 一行都沒有 → 沒有每晚靜默失敗,但**稽核保留期清理從上線起一次都沒跑過**,而那是只增不減的個資表(後台寫的「保留期 N 天」是沒被執行的承諾);**Benny 裁示 A 案**:`COPY tools/purge_*.py`(不是整個 `tools/`),T92 那條收窄為「不得 COPY `devserver.py` / `tests/` / 整個 `tools/`」——🔴 **文字變了意圖一字未減**:T92 防的是「偽造 session 的腳本進正式環境」,關鍵是 devserver 與它 import 的 `tests/` 不在,不是「tools/ 整個不在」;維運腳本不偽造身分,把它們一起擋掉換到的不是安全,是**一份跑不起來的維運文件,而那等於那件事沒有人做**;🔴 **本任務真正的產出是 `tests/test_ops_scripts.py`**:掃 runbook 裡每一個 `python tools/xxx.py` 引用,逐一檢查 Dockerfile 的 COPY 是否涵蓋 —— 擋的不是這次(已知道了)是**下一次**;⚠ **我自己踩了一次同型的錯**:守門第一版寫成「整份 Dockerfile 不得出現 devserver」,結果我在註解裡寫理由時被自己的測試擋下 —— 已改為只檢查 COPY 指令,因為**逼人刪掉理由,下一個人就會順手把整個 tools/ 收進來**;**3 條新增(1 紅起步),全套 593**;runbook v1.9 標註「cron 從未掛」與「需 v0.2.6 以上」 |
| T110 | 測試 SOP 成文 | ✅ | 🟢 | Benny 指示以測試工程師視角寫測試 SOP;現況是紀律**存在但散落**(憲法第三條、conftest、43 個測試檔檔頭、各篇 dev-log 的教訓),新人要拼湊十幾份文件才知道規矩;新增 `docs/測試SOP.md`(照著做定位,比照 runbook):策略形狀(整合式為主體的理由)、三件套、TDD 五步、撰寫規範——🔴 **狀態走真實路徑寫入不得手動塞 DB**(T84 假綠)、**反向斷言必有前提斷言**、區塊斷言不整頁比對(T85/T59)、斷言 invariant 不斷言字面、成對逸出斷言、量化紅線用機制驗(查詢計數/SHA-256);紅線↔守護測試對照表 13 列、migration 演練規則、CI 判定(Trivy 紅不反射性怪最後 commit)、換版四組冒煙與「沒驗到不得記成驗過了」、**十條實踩陷阱清單**;每條規則標注出處任務;md + HTML 並存入 render_docs;純文件依第三條 4 不寫新測試,全套 506 維持全綠 |
| T111 | 測試項目清單(量身訂做的「測什麼」) | ✅ | 🟢 | Benny 對 T110 的回饋:SOP 是流程,他要的是**測試工程師會測的項目**;新增 `docs/測試項目清單.md`:依使用者旅程(登入SSO 13 / 開通 7 / 專案 10 / 版本 8 / 上傳 10 / 下載 6 / 回報 13)+ 四跨切面(後台 8 / 安全 9 / 維運 7 / 介面 6)共 **97 項**,逐項「條件→動作→預期」一行可執行;每項標 **✅ 自動**(CI 每次跑,人工抽查)/ **🖐 人工**(測試工程師主力:真實 Keycloak、貼上/拖放/取消、跨 App 登出、視覺、VM cron)與**冒煙 ✓**(15–20 分鐘子集,含執行順序);🔴 明訂登入不可用期間冒煙項記「未驗」不得略過;附測試地圖 SVG;每項皆從實作長出(F/T 編號的產物),不抄模板;SOP 升 v2.1 補互指(SOP=怎麼測、清單=測什麼);純文件,全套 506 維持全綠 |
| T112 | 冒煙腳本 `tools/smoke.sh`(一鍵換版驗證) | ✅ | 🟢 | Benny:「可以寫測試腳本驗證嗎?」——答案分層:清單 ✅ 項早有 506 條 pytest;**冒煙裡對正式站打 curl 的部分本任務腳本化**;真實 IdP 畫面/貼上拖放/跨 App 登出維持 🖐 人工。腳本逐項印 ✅/❌/⏭ 並以 exit code 回報,對應清單編號(J-01/J-02/K-06/A-01/A-12/I-01/I-02);🔴 **登入後項目刻意不自動化**——ROPC 是契約禁手,塞帳密給腳本多一份外洩面,一律明示 SKIP 指回清單,**跳過要看得見不得假裝驗過**;腳本自身依第三條測:pytest 守門(`bash -n` + 關鍵檢查字串)先紅後綠,再以測試設定起真 uvicorn 實跑——**正向全過、負向(錯誤版本)會咬人**;🐛 實跑抓到腳本 bug:/help 教學內文的範例版本號 v1.0.0 被裸抓撿走,哨兵改錨定 T68 頁尾固定格式 `upload-program vX.Y.Z`;🐛 另收一次 T12 同款(scratchpad 腳本目錄不在 sys.path);runbook §A.4 升 v1.8 加一鍵入口、清單升 v1.1 標注腳本化範圍;**2 條守門測試,全套 508** |
| T113 | 回覆 portal v2.0:時序更正 + session 觀測補強 | ✅ | 🟢 | portal 現場重現後把主線指向我方 session/refresh;查核我方程式後回覆:🔴 **「callback 同一秒發 refresh」是時序誤讀**——對照其 gateway log 逐行,那筆 `Token is not active` 屬於 authorize **之前**帶舊 cookie 的進站請求(`_session_token` 對過期 session 的預期行為),我方**無** callback 後 refresh 的程式路徑;§6.5-2 續期寫回由 `SessionRenewalMiddleware` 完成(有測試);§6.5-3 已於 v0.2.1 上線(T87);9 筆/120h 的無害解釋 = 各舊 session 過期後的首訪;**真正未解 = :44 踢回**,誠實列三種可能——其中「cookie 送達但無法解析」在 v0.2.1 仍**靜默**,本任務補上 warning(🔴 只記長度不記內容,cookie 內含 token;無 cookie 不記,匿名是常態);**2 條紅→綠,全套 510**;回覆文件同步撤回我方 v1.0 的主要嫌疑判定 |
| T114 | 憲法第九條:文件識別與跨專案文件規約 | ✅ | 🟢 | Benny 裁示:文件要標明專案與時間,交付其他專案的檔名要標明**發文與受文專案**。動工前盤點 `docs/plans/` 13 份揪出病灶:收發欄位**四種寫法**(發文方/收文方、受文方/發文方、申請方、英文 From/To)、**3 份完全沒有**、檔名只看得出對象看不出來源與時間;對方來文更慘(收到過 `catsportal____uploadprogram_____.md`,中文被傳輸吃光只剩底線,連是誰發的都認不出)。第九條定:所有文件抬頭加 `專案`;跨專案文件再加 `發文專案 / 受文專案 / 發文時間`(**發文時間不隨改版變動**,供雙方對帳,與 `最後更新` 分工);🔴 檔名 `<發文專案>_致_<受文專案>_<類別>_<主題>_<YYYYMMDD-HHmm>.md`(類別限六種);他方來文原樣存 `docs/plans/inbox/` 不改內容;一律登記進**跨專案文件索引**(新增,一頁看完發過什麼/給誰/何時/對方回了沒/卡在對方三項)。⚠️ **界線:只對生效後新建文件有強制力,既有 8 份不改名**——舊檔被開發日誌引用 40+ 處,為求整齊回頭改寫日誌等於**拿新規則竄改舊歷史**(第六條精神反對),改以索引為橋、腳本統一補抬頭(手改 8 次必然漏,那正是本條要消滅的東西);規約由 `tests/test_doc_conventions.py` **5 條**守門(規約沒有測試,三個月後就是一句沒人記得的話)。順帶完成 T113 回覆文件 v1.1:Benny 撈回 48h VM log——正面 08:18:**21.226** `invalid_grant` 與對方同秒且在前段,佐證時序主張,`授權碼交換失敗` **0 筆**(中斷已結束);🔴 **負面更有價值:08:18:44 無任何紀錄 → 排除「續期失敗導致踢回」**,剩 cookie 無法解析 / 未帶 cookie 兩種,已請對方比對毫秒序 UA 來源 IP。**5 條紅→綠,全套 515** |
| T115 | 🐛 中文文字檔被判成 binary(判型截斷補救砍過頭) | ✅ | 🟢 | Benny 實測:「說明」卡片上傳 `客戶規格書.md` 回 422「檔案實際型別為 application/octet-stream」——**而卡片自己寫著收 PDF / Markdown / 文字檔**,是我方 bug 不是使用者用錯。根因:判型只看前 4096 bytes,尾端幾乎必然切在字元中間,舊補救寫「decode 失敗就砍尾端 3 bytes 再試」——對 ASCII 沒事,**對中文是錯的**:中文一字 3 bytes,殘尾若是 1 或 2 bytes,砍 3 bytes 會把**前一個完整的字也砍破**,製造出新的不完整字元 → 仍失敗 → 判成 binary → 白名單擋下。實測同一份內容只差一個 ASCII 前綴就翻盤(**三種對齊兩種失敗**),所以症狀是「有時可以有時不行」。改用 UTF-8 **增量解碼器**(`final=False` 語意正是「後面還有,尾端不完整不算錯」,非法位元組照樣拋錯)——把「被截斷」與「不是文字」分開,舊寫法混為一談。影響面比截圖大:`kind=source` 也吃 `text/plain`,**帶中文註釋超過 4KB 的 `.py`/`.txt` 同樣被擋**,公司內部開發者中文註釋是常態;英文文件不受影響,此 bug **專打中文**。🔴 護欄不動:不把 octet-stream 加進白名單、不看副檔名、HTML/SVG 仍一律拒收、attachment+nosniff 兜底未碰,皆有測試。活這麼久的原因是測試資料太乾淨(小檔 + 英文),新測試刻意用 >4096 中文並把三種對齊全測;🔴 **要換版才生效**,建議併入 v0.2.2。**5 條紅→綠,全套 520** |
| T116 | 發版前置:`APP_VERSION` → 0.2.2 | ✅ | 🟢 | T115 的修正要換版才生效(正式站仍跑 v0.2.1,中文大檔依舊傳不上去),Benny 裁示發下一版。依第八條 4 在打 tag **之前**改好常數。本版帶 6 個任務,其中**只有兩個動到執行中的服務**:T115 中文判型修正(主角)與 T113 壞 cookie 觀測;T110–T112、T114 是文件、測試與工具。**為何 0.2.2 不是 0.3.0**:無新功能面,一個 bug 修正 + 一個觀測補強,patch 語意正確;**為何不重發 v0.2.1**:第八條 5,該 tag 已發布且已部署,移動 tag 會讓「使用者手上那版」與「tag 說的那版」分家。依第三條 4 不寫新測試——版本號三個載體(頁尾/OpenAPI/問題回報附記)都讀同一常數,既有測試自動驗新值,再抄一次字面值只會製造「改版本要改兩處」的摩擦。🟢 **無 migration**,VM 換版不需 `alembic upgrade head`;換版驗證除哨兵外,🖐 **人工必驗超過 4KB 的中文 `.md` 與帶中文註釋的 `.py` 能上傳**(本版主角,腳本構不到),並反向確認 `.exe` 改名成 `.md` 仍被擋。全套 520 |
| T117 | 🐛 CI Trivy 紅燈:base image 被本機快取釘住 | ✅ | 🟢 | PR #33 的 Trivy job 紅燈,列出的全是 **base image 的 OS 套件**(`util-linux` / `mount` / `login` / `libmount1` / `libblkid1` / `libsmartcols1` / `libuuid1`,CVE-2026-53612~53615),與本 PR 的 diff 無關——Dockerfile 的 runtime stage 從頭到尾沒有 `apt-get`。根因:建置指令沒有 `--pull`,而 runner 是 **self-hosted**,`python:3.12-slim` 這個 tag 一旦拉過就躺在本機層快取,上游把同一個 tag 重建、把 Debian 安全更新併進去,我們永遠取不到 → **時間到了 CI 自己會紅,而且 main 一起紅,看起來卻像某個 PR 弄壞的**。🔴 這不是放寬紅線:tag 仍是版本 tag 不是 `latest`,`--pull` 只影響「同一個 tag 取哪一份」;沒用 `.trivyignore`、沒放寬 `--severity`(T82 立過的界線);`--ignore-unfixed` 維持原狀——它本來就在,代表這批 CVE **上游有修好的版本**,我們只是拿不到。若 `--pull` 後仍紅代表上游尚未重建,屆時再於 runtime stage 明確 `apt-get upgrade`,**先不做**免得上游修好後留下長期贅生物 |
| T118 | 專案頁顯示擁有者(過渡 sub 前 8 碼)+ 申請擴大名稱快取用途 | ✅ | 🟢 | Benny 看正式站:「沒有顯示作者的名字」——查證屬實,`專案資訊` 只有容量與時間,**完全沒有擁有者欄位**。對程式分享平台是核心缺口:要下載一支執行檔,「這是誰放的」是判斷信不信任它的第一個依據,**尤其掃毒還沒接上**(`not_scanned`),發布者是誰是使用者僅剩的依據。🔴 **但名字放不上去**:名字唯一來源 `display_name_cache` 受契約 §4.2a L1 管,而那條「僅管理後台顯示、**不出現在一般使用者可見頁面**」是**我方自己寫進申請書的**——當初為求好過把範圍縮到最小,現在反過來咬到自己;偏離平台契約的權限不在本專案(〈位階〉),只能送申請不能自行放寬。Benny 裁示**兩個都做**:(1)過渡顯示 **sub 前 8 碼**+誠實標註「顯示名稱待平台核准」(sub 是不透明識別碼非個資,業務庫本來就只存它;截斷比照 T59「僅供人眼辨識」;與 `not_scanned` 同一個態度);(2)申請書送 portal,**只動「用途」一欄**,明列不申請的四件事(Admin API / 進 API 回應 / 新欄位 / email),附契約增修建議稿與「若不同意」的替代方向詢問。🐛 測試自己先寫錯一次:三條都讓**擁有者自己**看自己的專案,於是「不得洩漏完整 sub」當場紅——**卻不是功能有問題**,而是導覽列本來就會顯示檢視者自己的 sub;「名字不得出現」那條更是永遠不可能過。改成**由別人來看**才是真正要問的問題。教訓:寫反向斷言前要先確認頁面沒有**另一個合法來源**會產生同一個字串。**3 條紅→綠(功能紅、兩條護欄綠),全套 523** |
| T119 | 本地 WSL 開發環境 + 本機跑 CI | ✅ | 🟢 | Benny:「部屬 本地 WSL 跑 CI」。盤點發現 **README 現行指示是錯的**——「最省事的方式是 `docker compose up -d`」在 WSL 必然失敗三次:`cats-edge` 外部網路本機沒有、`ghcr.io` image 要登入且拉到的是**舊版而非你的程式碼**、CA 憑證路徑是 VM 上的。根目錄 compose 是**正式機形狀**,本來就不該當本機環境,依第二條 5 回寫(README v2.1)。好消息是 CI 主力本機全跑得動:`conftest` 用 **SQLite + 假 S3**,528 條測試**不需要 DB / MinIO / 網路**;真正需要相依的只有「用瀏覽器實際點」。新增 `docker-compose.dev.yml`(只起 PG + MinIO、綁 `127.0.0.1`、無 restart)與 `docs/runbook_本地開發與CI.md`(七節:WSL 準備→venv→起相依→本機跑 CI 三個 job→做不到的事→常見卡關→一頁速查)。🔴 **最大風險是 dev compose 被誤用到正式機**:它為了本機能連**必須發布 port**,而那正是平台鐵則禁止的——三層防線:檔名**不叫** `docker-compose.override.yml`(那名字會被自動載入,在 VM 上打一次就把 DB 開到主機且無警告)、刻意不設 restart(差異本身是標記)、`tests/test_dev_compose.py` **兩邊一起釘**(dev 不得有 cats-edge/ghcr/restart;正式檔仍須有 cats-edge 且 db/minio 不得有 ports)。`docker compose config` **實跑通過**,不只是讀過覺得對。查證後修掉兩個本來會寫錯的地方:`PUBLIC_BASE_URL` **不必改**(頁面連結一律 root-relative,絕對網址只用於 OIDC)、**啟動 log 的 discovery error 是正常的**(刻意不擋啟動)並附 `/health` ✅ `/ready` ❌ 的本機預期表。🔴 本機**登入不能用**且刻意不解決——不會為了開發方便要求 portal 在正式 client 加 localhost redirect URI。**5 條紅→綠,全套 528** |
| T120 | 憲法第十條:指令必須標明「在哪台機器、在哪個目錄」 | ✅ | 🟢 | Benny:「給指令要說明在哪一個平台執行 以及執行的檔案路徑」。不是龜毛——**已經出過事**:2026-08-11 換版第一次失敗,根因是「改 `docker-compose.yml` 的 image tag」那一步**是編輯檔案,卻被當成 shell 指令貼進終端機**,結果 pull 到舊版而容器顯示 Running。文件裡同時躺著**三種機器**的指令(WSL 開發機 / Windows Docker Desktop / Cats VM),長得一模一樣,而貼錯的代價**不對稱**:在開發機跑到 VM 指令頂多報錯,**在 VM 跑到開發機指令會出事**(dev compose 一貼就把 DB 的 port 開到主機)。第十條定:每個可貼區塊正上方標 `🖥️ 在哪執行`(機器 + 工作目錄)或 `📄 編輯哪個檔`;工作目錄一律絕對路徑或 `~` 起頭,不寫「專案根目錄」這種要讀者換算的話;聊天回覆指令同樣適用。兩份 runbook **23 個區塊全標**(本地 16 → v2.0、換版 7 → v1.9)。🔎 **標記這個動作逼出三個坑**(不是順便發現,是「要寫在哪執行就得先回答這是執行還是編輯」逼出來的):§A.1 打 tag 其實在**本機 repo** 卻夾在兩段 VM 指令中間、§C.2 的 cron 是**編輯 crontab** 不是貼進終端機(08-11 同一個分界換個地方等著)、本地 §3.2 的 `.env` 區塊**看起來就像四條指令**(照貼會得到四個 command not found 且不知錯在哪)。⚠️ 誠實記下第 4 項(聊天回覆)**測試守不到**——只掃 repo 內文件,靠自律。**3 條紅→綠,全套 531** |
| T121 | 🐛 草稿版本回不去(版本歷史頁沒有編輯入口) | ✅ | 🟢 | Benny 實測:「草稿版本就不能再發布與編輯了」——他的專案裡躺著 **4 個 0 檔案的草稿**,每個只有一枚徽章、旁邊什麼都沒有。根因**不是功能沒做**:上傳與發布都在且有測試(三類齊備、已發布回 409、maintainer 把關);缺的是**入口**——上傳頁 `/releases/{id}/upload` 只在「建立版本」送出後被 `_redirect` 導向**一次**,使用者一離開就再也回不去;版本歷史頁只印徽章不給連結,專案頁又只顯示最新**已發布**版本。草稿因此變成孤兒:**看得見、點不進去**,每次建版沒一次做完就多一個殼。修法是版本歷史頁的草稿列加「繼續編輯」。🔴 兩條護欄與功能同等重要:(a)**viewer 看得見草稿但改不動**(發布要 maintainer 以上),給他一個按下去必然 403 的連結**比不給更糟**——新增 `security.may_manage_releases()` 與 `require_project_role` **共用 `_ROLE_RANK`**,只是不拋例外(真正把關仍在 API,這裡少判只會少一個入口);(b)**已發布的版本不得出現該入口**,已發布不可變更,給連結等於誘導撞牆。🐛 順手修掉 `upload.html` 的 `**已發布**`——Markdown 寫在 HTML 模板裡不會變粗體,**會原樣印出星號**,已發布頁面一直掛著兩對星號沒人回報。📌 記一筆免得日後誤讀規模:「功能做完了」與「使用者找得到」是兩件事,而本例的症狀聽起來完全像前者。**3 條紅→綠(功能紅、護欄綠),全套 534** |
| T122 | 管理員權限:網頁指派入口 | ✅ | 🟢 | 依[施工計畫書](plans/施工計畫書_審核與回饋_T122-T125.md) §1 施工。盤點結果:**後端九成已存在**——`platform_role` 欄位、`PATCH /v1/admin/users/{id}` 可改角色且有稽核、後台已顯示「管理員」徽章、`BOOTSTRAP_ADMIN_SUBS` 解決雞生蛋;**唯一缺網頁按鈕**(與 T121 同一形狀:功能在、入口沒有)。🔴 三條防呆:不能取消自己的管理員(手滑就沒人管得動,平台無 root 後門)、不能把未開通者設為管理員(繞過自己的門禁)、稽核照記。🔴 **必須排在 T123 之前**——審核制度要有可指派的管理員,否則全卡在 `.env` 那一個 bootstrap 帳號。防呆做**兩層**:畫面不給機會按(自己那列無按鈕、未開通者不顯示「設為管理員」),**伺服器端 409 才是把關**(表單可以偽造)。刻意**不做**「最後一個管理員不能被降級」:那要數管理員人數,而計數在並行下不可靠(兩人同時降對方,兩邊都讀到還有 2 個),要做對得上資料庫鎖;防呆 1 不需計數、只看「你是不是你」,永遠正確且已涵蓋最常見的手滑。🐛 測試踩到 local import 慣例導致 4 條以 `NameError` 紅——**紅的原因不是功能有問題**,這種紅要先看錯誤型別再動程式。**5 條紅→綠,全套 539** |
| T123 | 發布審核:管理員核准後才可下載 | ✅ | 🟡 | 依裁示施工:狀態機由兩態變三態 `draft →(送審)→ pending_review →(核准)→ published`,退回帶理由回到 draft。🎯 **關鍵設計:`published` 的語意刻意不變**(一直都是「可下載」)——既有已發布的版本因此**一列都不用改**,裁示第 2 條不是靠資料遷移達成而是靠設計,資料影響從 🔴 UPDATE 降為 🟡 純加欄位(實測 `alembic upgrade --sql` 產出的 PostgreSQL DDL **沒有任何一行 UPDATE**)。可見性沿用草稿既有的 404 規則(`is draft` → `is not published`),不發明新規則——兩套可見性遲早分岔,分岔的那邊就是漏洞。🔴 順手補上一道洞:**待審中不得再改檔案**(原本只擋 published),否則管理員核准的不是他看過的那一份,審核會變成能繞過的形式。🐛 **up→down→up 演練當場抓到兩個 migration bug,而 548 條全綠也看不到**(pytest 用 `create_all` 根本不跑 migration):SQLite 不支援 `ALTER COLUMN TYPE`(改用 `batch_alter_table`)、batch 模式外鍵必須具名。🔴 另有一個 **SQLite 永遠測不到**的坑:`native_enum=False` 存 VARCHAR、長度取最長值 → 原本 `VARCHAR(9)`,而 `pending_review` 有 14 字元,PostgreSQL 會拒寫而 SQLite 不檢查——靠讀 `_enum` 實作推出來的,已用 `--sql` 逐行確認放寬語句在裡面。既有 **21 條測試依新行為改寫**(需求變更的預期後果,非回歸):新增 `conftest.publish_and_approve()` 走真實 API 而非塞 DB(T84 假綠的教訓),並把 app 掛在 client 上讓幾十個 helper 的**呼叫點一行未動**。✅ **PostgreSQL up→down→up 演練已完成**(2026-08-13,Benny 在 WSL 以 dev compose 的 PG 15.8 執行,三步全過)——⚠️ 誠實註明那是**空庫**演練,證明的是 DDL 在 PostgreSQL 上跑得動(即本任務的風險:`ALTER COLUMN TYPE` 與 VARCHAR 長度),不是資料搬遷邏輯。通知缺口仍在(已知取捨)。**9 條紅→綠,全套 548** |
| T124 | 專案回饋(專案留言板) | ✅ | 🟡 | 依裁示做**專案留言板**(計畫書 §3、§6-4)。現有的問題回報是**平台層**(回報網站問題,🔴 僅本人與管理員可見、404 不洩漏存在),與專案層公開回饋的可見性設計**完全相反**,不能沿用。三種解讀工作量差很多:(a) 專案留言板 (b) 版本層回饋 (c) 評分;建議 (a)——文字對「這支程式好不好用」的價值遠高於分數,且結構近似 `IssueComment` 可沿用逸出與長度限制。三件必須面對的事:可見性跟隨專案(private 的回饋絕不能外洩)、留言者身分又撞契約 §4.2a L1(獲准前只能顯示 sub 前 8 碼,一串 UUID 的留言板體驗很差)、保留與清理是否比照 365 天。**實作結果**:新表 `project_comments`(專案 CASCADE、使用者 RESTRICT、複合索引);可見性沿用 `require_project_read` 不另立一套(private 非成員 404 不是 403);**能讀就能寫**,viewer 也能留言——只有 maintainer 講得了話的留言板不叫 feedback。🔴 **最重要的界線:專案擁有者不能刪別人的留言**,只有留言者本人與平台管理員可以——擁有者若能刪掉評語,留言板就只會剩下好話,而**一個只留得住讚美的回饋區比沒有回饋區更糟**(它讓人誤以為「沒有負評」= 「沒有問題」);想回應批評的方式是再留一則言。**刻意不做自動清理**(與 issues 不同:回報是事務、處理完就沒價值,留言是**內容**,一年後仍有用)。🐛 第一版有一條**為錯的理由而綠**的假綠:「private 非成員 404」在端點根本不存在時本來就會過,而且**永遠不會紅**;補前提斷言(擁有者先拿到 200)後才真的紅——反向斷言必須先證明正向那條路是通的(T84 同一課換個面貌)。**7 條紅→綠,全套 559**;migration up→down→up(SQLite)通過 |
| T125 | 版本作者屬名 | ✅ | 🟢 | 依裁示施工(計畫書 §4、§6-3:採**建版者**)。好消息:**資料早就在**——`Release.created_by_id` 存在且必填,「初始版本 jack、v2 由 benny」資料庫現在就答得出來,只是沒顯示在任何地方。🔴 壞消息與 T118 同一個:顯示**名字**受契約 §4.2a L1 擋住(版本歷史頁是一般使用者可見頁面),申請書已送出且**已把「上傳者」寫進請求範圍**,獲准後一次補齊。分兩段:現在先顯示 `sub` 前 8 碼(格式與 T118 專案頁一致,🟢 不動資料)、獲准後換成名字(一行)。已裁示採**建版的人**(🟢 不動資料)。🔴 **一次 `IN` 查完不逐列查**——版本歷史頁一次列 20 版,逐列查就是 20 次查詢;比照 T84 有一條測試以 `before_cursor_execute` **實際計數**,1 版與 10 版必須相同。那條護欄**實作前就綠**,它真正的價值在**實作後仍然綠**:那才證明批次有效,而不是「有沒有做」。🐛 四條測試一開始都漏了上傳執行檔(`complete_kinds` 只補 source+doc),害兩條護欄以 422 誤紅——**紅的原因不是功能**,與 T122 的 `NameError` 同一課:先看清楚紅的理由再決定動不動程式。格式與 T118 專案頁擁有者**刻意一致**(同一件事不該長成兩種東西)。**4 條紅→綠,全套 552** |
| T126 | 發版前置:`APP_VERSION` → 0.3.0 | ✅ | 🟢 | 常數原為 0.2.2(T116 決定),但**這一版後來長大了**:累積了四項新功能(T122 管理員指派 / T123 審核制度 / T124 留言板 / T125 屬名)與**兩個 migration**。SemVer 在 0.x 的慣例是新功能與行為變更都升 minor,而且這一版有一件比新功能更重的事——🔴 **`POST /v1/releases/{id}/publish` 的語意改了**:原本「發布,馬上可下載」,現在是「送審,等核准」;同一條端點、同樣回 200,**結果完全不同**。用 0.2.2 發等於對看 Release 頁的人說「這只是小修正」,而他下一步就會發現自己發布的東西沒人下載得到——**版本號會說謊**。0.2.2 從未打成 tag、只在分支上當過幾小時的常數值,**跳號不補**(SemVer 不要求連號,為了整齊而發一版是本末倒置)。依第三條 4 不寫新測試(三個載體讀同一常數)。🔴 本版**有兩個 migration**,VM 換版必須跑 `alembic upgrade head`;換版前先清空審核佇列(0009 的 downgrade 會把待審當回草稿) |
| T127 | 🐛 CI Trivy 紅燈:base image 的 openssl 落後安全更新 | ⚠️ | 🟢 | PR #33(v0.3.0)的 `Build image + Trivy 掃描` 紅:`libssl3t64` / `openssl` / `openssl-provider-legacy` 同一個 **CVE-2026-14456**(HIGH ×3,`3.5.6-1~deb13u2` → `3.5.7-1~deb13u2`)。**與本 PR 的 diff 無關**——runtime stage 沒有任何 `apt-get`,OS 套件原封不動來自 base image,main 現在建也會紅。🔍 **這次不是 T117 的快取問題**:`--pull` 確實生效了(掃到的就是 Debian 13.6 的當期版本),真正的原因是**上游 `python:3.12-slim` 尚未重建**——`--pull` 只能保證取到上游最新的那份,**上游沒動就沒得取**。處置正是 T117 日誌〈如果 `--pull` 之後還是紅〉那一節**預先寫好**的方案:runtime stage 只升 openssl 家族(不做整包 `apt-get upgrade -y`——那會讓「這個 image 裝了什麼」隨建置時間漂移,是 T82 遺留問題的 OS 版本)。🔴 **仍然不用 `.trivyignore`**,而且這次誘惑比前兩次都大:CVE-2026-14456 是 OpenSSL **QUIC 伺服器**的記憶體無限成長,本服務沒有任何 QUIC 伺服器路徑(TLS 在 gateway 終結,OpenSSL 只被 Python 用在對外 client:JWKS、MinIO),**幾乎確定不適用**——但「我判斷用不到」不是關掉掃描的理由:判斷可能錯,而 ignore 檔留下的只有一行 CVE 編號、留不下今天這段推理;既然上游**已經有修好的版本**(`--ignore-unfixed` 一直開著,它出現就代表有得修),拿到它才是正解。⚠️ **誠實記下一個風險**:Dockerfile 第 12–16 行的歷史正是「apt 套件庫從 CI 網路不可達」(run #85)才把 apt 拿掉的,本次把 apt 加回來**可能撞同一堵牆**;若撞牆會**大聲失敗**(🔴 刻意不加 `\|\| true`——吞掉失敗等於發布一個自己以為修好的 image),❌ **實測失敗,已回退**——而且失敗的形狀與預期相反:牆還在(三個 suite 全部 connection timed out),但 🔴 **`apt-get update` 抓不到索引時只印 `W:`、exit code 仍是 0**,`--only-upgrade` 拿 base image 的**舊索引**比對後回報「已經是最新版」,整條 `&&` 鏈**成功**,建置產出一個**看起來已修好、實際一個位元組都沒動**的 image。那條「刻意不加 `\|\| true`」的護欄立意正確但**守錯了地方**:我防的是「指令回傳非 0 而我吞掉」,而 apt 根本不回傳非 0——**抓到這件事的是 Trivy,不是我寫的任何一道保險**。📌 可帶走的一課:**`&&` 鏈只證明「每個指令都自稱成功」,不證明「事情做到了」**;對「取得外部資源」這類步驟必須自己驗結果,不能把 exit code 當成事實——與 T84 / T124 的假綠同病,只是載體從測試換成了 shell。🔴 **結論:我方修不掉,三條路都不在本 repo**——A 等上游重建(ETA 未知)/ **B 開通 CI 容器網路的 apt 或指向內部鏡像(建議,一勞永逸:這是第三次被 base image 的 OS 套件擋)**/ C 放寬掃描(🔴 不建議,已立過兩次界線)。⚠️ **此紅燈是 v0.3.0 的硬阻塞**:`publish` job `needs: build`,Trivy 紅則 GHCR 推不上去,**tag 打了也不會有 image**。待 Benny 裁示 |
| T128 | 狀態總覽頁 + 🐛 補上 HTML 覆蓋守門 | ✅ | 🟢 | Benny:「目前狀態 寫成一份文件」。狀態原本散在**四個地方**——任務表(160+ 列,太細)、開發日誌(26 篇,一篇一件事拼不出全局)、跨專案索引(只涵蓋對外那一面)、GitHub PR(不含 staging 欠項與營運限制);四個都是對的,但**沒有一頁看得完**,於是「現在到底能不能發版」每次都要重查一遍。新增 `docs/狀態總覽.md` 七節,重點在按**阻塞源**重編的〈誰的球〉(平台網路 / cats-portal / staging),讓「**唯一擋住發版的是 #11**」變成一眼可見的事實而不是要推理出來的結論。🔴 開頭明示本頁是**摘要不是真相來源**——若讀者把摘要當權威,它就會變成第五份會漂移的真相,比沒有它更糟。🐛 **彙整時順手抓到 `--check` 的盲點**:它只掃 `render_docs.py` 自己的 TARGETS 清單,**漏掉的東西不在清單裡,清單當然檢查不出來**——前一天新建的第 11 份申請書因此沒有 HTML 版,而 CI 照樣綠。與 T117 / T127 同一形狀:**檢查器只證明它檢查過的東西是對的,不證明該檢查的都檢查了**;差別是這次的漏網之魚是文件,症狀更難察覺(沒有 HTML 版不會有任何錯誤訊息,只有對方收到 md 卻打不開時才發現,而那時已經寄出去了)。依第九條 7「規約若沒有測試,三個月後就是一句沒人記得的話」補守門第 9 條,🔴 **判準綁索引不綁 glob**(索引是「哪些算跨專案文件」的唯一登記處,綁 glob 等於讓規則散成兩份)。⚠️ 日誌計畫段為**事後補記**,已依第二條 2 明示。⚠️ 遺留:**過期的狀態頁比沒有狀態頁更危險**(看起來很權威),而測試查得出檔案在不在、抬頭齊不齊,**查不出內容是否過期**——建議每次發版與卡關項變動時更新。**1 條紅→綠,全套 560** |
| T129 | 修掉會誤報的本機 CI 指令 | ✅ | 🟢 | Benny 照 `runbook_本地開發與CI.md` §4.1 實跑,得到「❌ 踩到 SSO 紅線」,而同一個 commit 的 CI 那一步**是綠的**;根因:runbook 上的是**簡化版**,`ci.yml` 實際有三層排除而它一層都沒有 —— ①`--exclude='test_sso_contract.py'`(**那支測試自己就是掃描器,必須寫得出被禁的字**)②HS256 **只掃 `app/`**(測試會刻意產生 HS256 token 來驗「必須被拒絕」,那是正確用法)③排除 `不接受/一律拒/禁` 這類**說明**用法的行,並以 `--include` 避開 `__pycache__` 的 binary 命中;🔴 **與 T109 同一類(文件寫得出來、照做卻得到錯的結果)但更陰險**:T109 是 `No such file`,當場知道有問題;這一支給的是**看起來很嚴重的假警報**,後果是有人去找一個不存在的問題,或更糟 —— **學會忽略紅字,而下次是真的**;已改為與 `ci.yml` 逐字相同並列出每個排除的理由(不寫理由,下一個人會覺得排除很可疑而順手拿掉);**不寫新測試**(第三條 4):`test_sso_contract.py` 已在驗這件事本身,⚠ 但遺留 T130 —— **同一句指令出現在兩個地方遲早分岔**,這次是文件比 CI 嚴格(假警報),下次可能是文件比 CI 寬鬆(本機綠、CI 紅)|
| T131 | 憲法第十一條入憲(彩色標示 + 佔位符紅線) | ✅ | 🟢 | Benny 指示把三層指令標示規則入憲;盤點後**第一、二層已經在第十條裡**,真正缺的是第三層與佔位符規則 → 新增**第十一條**(七項:圖例先給、對映整條線不變且已完成標 ✅ 並**明說不會再叫你進去**、🟩 固定保留給 GitHub 網頁、步驟標題=**三連**色塊+目標名+路徑+執行處、佔位符紅線、顏色本身不帶語意、治理範圍限對話與 runbook);🔴 **放在第十條之後不插進去**:第十條已被測試與多篇 dev-log 逐條引用(「第十條 2」),插隊會讓引用悄悄指到別的東西;🔴 **第十一條 5 有守門測試不是一句期許** —— `test_可貼的指令裡不得有尖括號佔位符` 掃 runbook 的 shell/cron 區塊,**會被執行的行**出現 `<…>` 就紅,起因是 2026-08-31 實踩 `<拿新token>` 整段被貼上、**`<` 被 bash 當重導向**而炸掉(尖括號是文件慣例,而**那個慣例在 shell 裡會產生語法**);⚠ 兩個刻意邊界:註解豁免(`# /opt/<服務名>` 貼上去 bash 根本不看,要擋的是會被執行的那行)、行尾註解只切「空白+#」不切任何 `#`(**寧可偏嚴不偏鬆**);**反向驗證做過**(塞佔位符→紅、還原→綠,沒這步新測試可能只是裝飾);同步 cats-portal 憲法 v1.22(依它自己「往後加」的規矩寫成附則最後一節);⚠ **`workOrder` repo 不在本 session,那本沒改也沒假裝改**;**1 條新增,全套 648** |
| T132 | 專案/版本/留言顯示真名(契約 v3.4) | ✅ | 🟢 | Benny 連續三次回報「使用者還是看不到作者」;盤點是**兩件不同的事**:①線上是 v0.2.5,T118/T125 的程式碼還沒部署 ②🔴 **就算部署了也不是名字是 `sub` 前 8 碼** —— 契約 §4.2a L1 第 3 條「僅供管理後台顯示」擋著,而我方 2026-08-12 的《申請:擴大名稱快取用途》**躺了 19 天沒被裁決**;2026-08-31 Benny 裁決「**准**」,cats-portal 契約升 **v3.4**(裁決書已依第九條 4 原樣存進 `docs/plans/inbox/`,索引第 10 份 ⏳→✅);實作:`_subs_by_id()` → **`_labels_by_id()`**(一次 `IN` 查 `sub` + `display_name_cache`,查詢數與版本數無關的效能守門維持綠),標籤 = 有名字用名字、沒有就退回 8 碼 + `…`(🔴 **fallback 不是貼心設計,是契約第 6 項的要求**);🔴 **放寬的只有「顯示在哪」** —— 名字仍不進 API(新增守門測試掃四個端點)、仍不得作授權依據、仍不得進 log、🔴 契約新增「不得再散布至本服務以外」;⚠ **改了兩條既有測試並在 dev-log 逐條說明**(原斷言「名字不得出現在一般使用者頁面」釘的是 v3.3,照舊斷言改程式等於讓一條已被放寬的限制繼續生效)—— **反向斷言沒刪掉,改釘 L1b 仍不得顯示在任何頁面**,並補上帶 `email_verified=true` 的登入確保信箱真的進了快取才斷言它不出現(否則又是假綠);憲法紅線同步升 v2.8(**紅線與實作打架比沒有紅線更危險**);**5 條新增(3 紅起步)+ 2 條改寫,全套 653** |
| T76 | README 更新(v2.0) | ✅ | 🟢 | Benny:「更新 README.md」;原本只有標題 + 一句描述,改寫成新人打開 repo 就能自己動起來的門面:定位與線上狀態、功能一覽(只寫已實作,不寫願望)、技術棧與**路徑拓撲圖**(gateway 剝前綴最常被誤解)、本地開發指令、部署三條必記紅線(細節指向 runbook,不做第二份會漂移的說明)、契約 v2.0 對齊、憲法八條摘要與紅線、文件索引;🐛 初稿的 `pip install -e ".[dev]"` 是錯的(相依以 requirements*.txt 為準),已改正——寫指令就要確認它真的能跑;依第七條補日期/版本/版本歷史 |
| T75 | 「回報問題」獨立入口 | ✅ | 🟢 | Benny:「是不是少了網站問題回報?」——T66 把回報做成 `/help` 頁內一節,沒有入口等於不存在;加 `id="report"` 錨點 + 側欄(對話框圖示)與頂列各一條「回報問題」,**不分登入狀態都顯示**(網站壞掉時最需要回報的往往是還沒登入成功的人);不另立頁面(內容只有一段,拆開會斷開教學與回報的關係並多一份會漂移的文案);**3 條紅→綠,全套 409** |
| T73 | 顯示名稱改 Admin API 即時查詢(帳號不落地) | ⬜ | 🔴 | Benny 2026-07-31 裁示:「用 Keycloak Admin REST API 即時查詢,只顯示不落地」;🔴 **需 portal 授予 `view-users` service account**,本專案無權自行取得——申請書 [申請_AdminAPI即時查詢_給portal](plans/申請_AdminAPI即時查詢_給portal.md) 已成文,並**明示推翻** 2026-07-29 申請書 §3 的評估(當時主張 Admin API 授權面過大,portal 據此核准落地快取);承諾:只顯示不落地/只單筆查不列舉/2 秒逾時失敗回退顯示 sub/請求內去重不跨請求快取/上線後移除 `display_name_cache` 欄位並請撤銷 §6.1 特例;三階段 A 客戶端、B 後台改用、**C 刪欄位獨立 PR**(本專案第一個刪資料 migration);**裁示前不動工**(T59 立下的慣例),過渡期維持現行快取 |
| T70 | 管理後台總覽:KPI + 待辦清單 | ✅ | 🟢 | 依設計文件《管理員後台與數據面板》v1.1(Benny「可以 開工」,門檻全採預設);新增 `app/dashboard.py` 為聚合查詢單一出處(路由不寫 SQL),`/admin` 顯示 KPI 六卡 + **需要處理**(待開通/逼近配額 ≥80%/停滯 draft >14 天/未掃描/擁有者已停用→提示 F16 轉移)+ 系統資訊;分頁列抽成 `_admin_tabs.html` 共用 partial(第三頁一加必漂移);模板數值帶 `data-metric`,測試斷言**數字等於實際值**而非「有出現數字」;待辦未觸發時不出現;配額總和按級距加總(非專案數×標準級距);時間界線在 Python 端算(SQLite/PG 間隔語法不同)+ `_aware()` 補時區防 500;🔴 唯讀、只聚合不按人拆解、不放 T72 的懸空連結(有測試釘住);**13 條紅→綠(11 紅起步),全套 399** |
| T69 | 更名「AI 小程式分享平台」 | ✅ | 🟢 | Benny 裁示:「把程式分享的名稱改成 AI 小程式分享平台」;原字串散在五處(品牌/側欄副標/首頁大標/教學頁/OpenAPI summary),順手收成**單一來源** `app/branding.py`,並以**掃描所有模板**的測試確保沒有人再硬編碼;🔴 技術識別名 `upload-program`(平台服務名、`/upload/` 路徑、GHCR image、compose 服務名、log service 欄)**不動**——改它要 portal 重做路由;分頁標題維持 `— upload-program`;**5 條紅→綠(4 紅起步),全套 386** |
| T68 | 首頁版本號 + 憲法第八條〈發版儀式〉 | ✅ | 🟢 | Benny 裁示:「加入 CLAUDE.md 每次發布 Release 提供 title 跟 content,並且在服務的首頁加入版本號」;新增 `app/version.py` 為**單一真相**(不吃 env 覆寫——版本是事實不是部署參數),首頁頁尾三種狀態皆顯示,`main.py` 寫死的 `0.1.0`(脫節五版)改讀同一常數並以**防漂移測試**釘住;CLAUDE.md 升 **v2.2** 新增第八條:Release 必附 title 與 content(content 四段:本版內容/資料影響/部署注意/測試)、🔴 `APP_VERSION` 須在打 tag 前隨 PR 改好、同版不重發、發版前驗 migration 鏈;**4 條紅→綠,全套 381** |
| T67 | 回到平台入口連結 | ✅ | 🟢 | Benny 裁示(附側欄截圖):「增加一個回到入口的連結」——從 portal 卡片進到 `/upload/` 之後沒有回頭路;側欄最上方獨立一段 + 頂列(已開通桌面版 `d-lg-none` 防重覆);🔴 **不經 `url()`**(加前綴會變成 `/upload/` 自己),設定化 `PORTAL_HOME_URL` 預設 `/`,與契約 §2.1 `/account` 同類具名例外;白名單納入 `/` 會鬆動「連結一律帶前綴」那條紅線,**同 commit 補上具名標記斷言**(每條 `/` 必須帶 `nav-exit`/`side-exit`);🐛 過程發現排序測試撞上 SVG 路徑裡的 `v9`,圖示與測試兩邊都修;**6 條紅→綠(5 紅起步),全套 377** |
| T66 | 使用教學頁 + 回報問題/功能需求管道 | ✅ | 🟢 | Benny 裁示:加「說明網站使用教學」頁與「回報網站問題或提交功能需求」;`/help` **匿名可看**(待開通者最需要):快速上手七步、三類齊備規則(T65)與格式白名單(依 filetypes.py 改寫成人話)、檔案安全規則(magic bytes/HTML+SVG 拒收/SHA-256/not_scanned)、回報專節(找平台管理員,附重現步驟/預期行為/時間網址);入口=頂列(所有人;已開通桌面版 d-lg-none 防與側欄重覆)+ 側欄(行內 SVG 問號圖示);**6 條紅→綠(5 紅起步),全套 371** |
| T65 | 發布強制三類齊備(doc + binary + source) | ✅ | 🟢 | Benny 裁示:每一版都要上傳三個——更新文件、執行檔、source code 包(99% Python 開發);發布條件由「至少一檔」改為 **三類各至少一個 ready** 才可發布;規則本體只有一份(`releases.missing_required_kinds`),API 422(`release-missing-kinds`,明列缺項)與網頁表單(`?error=missing-kinds`)同一條;上傳頁列缺項並停用發布鈕(提示性,伺服器端兜底);既有已發布版本不回溯;**4 條紅→綠(3 紅起步),全套 365** |
| T64 | 靜默 SSO(portal 進站免再按登入) | ✅ | 🟢 | Benny 裁示:IdP 已登入還要再按登入「太蠢了」——首頁對「瀏覽器(Accept 含 text/html,沿 T47 內容協商)+ 5 分鐘內未探測」的匿名訪客發起 **`prompt=none`** 靜默授權:有 IdP session 無聲登入,沒有則 `login_required` 無聲退回落地頁;探測 cookie 防迴圈;流程仍 code+PKCE(§4.1 不變);curl/冒煙行為零改變;**8 條紅→綠,全套 361** |
| T63 | T2.0 `groups` claim 通知對齊(只讀不判) | ✅ | 🟢 | portal 2026-07-31 通知:claim 已就緒但**無人被指派**——🔴 現在不得以 `/svc/upload` 判開通(會鎖住所有人且症狀像 SSO 壞掉),維持後台開通;新增 4 條防禦測試釘住「帶 groups 行為不變、空陣列/不存在都承受、User 無 groups 欄位(§4.2a L2 不得快取,結構保證)」;啟用時機等 portal 通知 |
| T62 | 導覽改版:PLM 式「頂列 + 左側欄」 | ✅ | 🟢 | Benny 裁示參考 PLM:左側欄(專案總覽/建立專案/管理二項,行內 SVG 圖示、active 藏青反白,僅已開通者、僅 lg+)、頂列瘦身(桌面連結走側欄,`d-lg-none` 留給漢堡不重覆);`render()` 注入 `current_path` 做 active;**344 測試零放寬全綠** |
| T61 | Bootstrap 版面優化:麵包屑 + 專案頁兩欄 | ✅ | 🟢 | Benny 指示「用 bootstrap 優化」:三頁補 **breadcrumb**(T56 遺留 #2 結案;連結一律 `url()`)、專案頁 **grid 兩欄**(lg 左最新版本/右專案資訊,窄幅疊回單欄;🔴 DOM 仍下載在前,Q8 測試不動);**344 測試零放寬全綠** |
| T60 | 施工單 v1.3 對齊:OIDC 伺服器端呼叫改內部端點 | ✅ | 🟢 | §4.4(sporton_core 實測;我方上線當天踩過同一坑、當時以 hairpin 解):新增三個**可覆寫**設定 `OIDC_DISCOVERY_URL`/`OIDC_TOKEN_URL`/`OIDC_JWKS_URL`(預設空=行為不變);🔴 瀏覽器端點與 `iss` 永遠對外(字串比對非連線)、issuer 檢查不因內部抓取放寬——**7 條紅→綠**,全套 344;VM 換版時 .env 追加三行後 hairpin 配件降級為備援 |
| T59 | 管理後台顯示名稱快取(§4.2a L1) | ✅ | 🟡 | 營運實測:管理後台只有 sub,開通後的日常管理無法認人,且會催生管理員私記的影子對照表;已依 §4.2 紅線流程提出**具名特例申請**(比照 PLM §6.1):[申請_姓名快取特例_給portal](plans/申請_姓名快取特例_給portal.md)——僅 `name` claim、登入時覆寫、僅管理後台顯示、不進 log、可整批清除、不呼叫 Admin API;獲准後才開工(migration 加 nullable 欄位,backward 為 drop column) |
| T58 | 導航列 Bootstrap 5 化(解擁擠) | ✅ | 🟢 | Benny 裁示採用 Bootstrap 5;**自託管 vendor**(CSP 禁外部資源,CDN 不可能):5.3.8 官包原檔 + LICENSE 進 `static/vendor/`;登入後導航七項收為四群(使用者名/帳號設定/登出進**下拉**),窄螢幕收**漢堡**;🔴 CSP 逐字不動——漢堡用字元「☰」自繪(Bootstrap 圖示是 `data:` URI 會被擋)、下拉觸發用 `<button>` 不用 `href="#"`(連結一律 `url()` 紅線);vendor 先載 `app.css` 後載,同名類我方覆蓋(框架進場、設計語言不變);**337 測試零放寬全綠** |
| T57 | UI 打磨:動效與質感第二輪 | ✅ | 🟢 | 在 T56 設計語言之內(不改色盤/字階/模板)補齊質感:雙層陰影、導航列毛玻璃、輸入框「藏青框+綠光圈」focus、`<details>` 摺疊箭頭(可供性缺口非裝飾)、404 漸層字、pager/tabs hover 回饋;**動效全部包在 reduced-motion 媒體查詢內**(與被禁的深色配色查詢是兩回事);唯一異動檔 `app.css`,**337 條測試零放寬全綠** |
| T56 | UI 改版:套用設計稿 | ✅ | 🟢 | 設計師依[介面導覽](介面導覽_給UI設計.md)交付 11 畫面 mockup;`app.css` 全面重寫(設計 token 進 `:root`:藏青/綠/膠囊/卡片漸層條),模板只加 class 不動文字與連結;卡片漸層條與雙圓 logo 用**偽元素零標記**;容量條用原生 `<progress>`(CSP 禁 inline style);待開通頁複製鈕以**漸進增強**外部 JS 實作(推翻 T45「不值得」的判斷——設計師把它列為開通流程第一步);**337 條既有測試零放寬全綠** |
| T55 | 首次上線實測修正(bootstrap 升級 + 部署缺陷) | ✅ | 🟢 | 8 條測試(**先紅後綠**);🐛 **bootstrap 只在建號時生效**——T45 自助流程在結構上保證帳號先以 pending 存在,清單永遠升不了級,第一個管理員靠 SQL 解鎖(2026-07-29 實際發生);修為 pending 在清單內登入即升級 + 寫稽核(actor 為系統);🔴 disabled 不升(不是繞過停權的後門)、active 成員不升權(派角色是後台職權);另修 `ensure_bucket` 冪等、`backup.sh` v2(minio 無 tar)、compose 補 extra_hosts/CA |
| T54 | portal 裁決與施工單技術對齊 | ✅ | 🟢 | 逐項核對施工單九項技術規格,**拿掉重複的 `X-Frame-Options`**(gateway 已送,語意不支援多值合併,行為未定義)、compose 補 `container_name: upload-program`(施工單 §3 三項硬性約束之一)、DB 命名改為 `upload_program_db`/`upload_program_user`(平台規約命名表,尚未上線現在改);回覆施工單 §9 五項見 [plans/回覆_portal施工單.md](plans/回覆_portal施工單.md) |
| T51 | SSO 契約 v1.7 對齊(設定值 / §2.1 帳號設定連結 / §4.8 文案 / §4.10 測試化) | ✅ | 🟢 | `OIDC_ISSUER` 改為 D2″ 的子路徑形式;導航列有指向 `/account` 的帳號設定連結;待開通文案能指引找對人;**§4.10 五項同源義務逐條有測試**(cookie Path、CSP、base path、輸出消毒、禁 localStorage) |
| T52 | 網頁 session 自動續期(access token 300s) | ✅ | 🟢 | 🐛 access token 5 分鐘過期但網頁零 JS 不會呼叫 `/auth/refresh`,登入 5 分鐘後全站靜默退回未登入;伺服器端在驗證失敗時以 refresh token 自動換發並更新 cookie;**測試要能模擬 token 過期**(FakeOidc 永不過期,抓不到) |
| T53 | 深層頁未登入導向 IdP(§7 冒煙第 1 項) | ✅ | 🟢 | 專案頁/歷史頁等未登入時 302 到 `/auth/login` 並帶 `next` 導回原頁;**首頁保留落地提示頁**(Benny 2026-07-28 裁示);🔴 302 不得因此洩漏專案是否存在 |
| T29 | 接入冒煙清單(SSO 契約 §7)全數通過 | ⬜ | 🟢 | 六項全綠;repo grep 零 Authentik、零 HS256;第 1 項依 T53 的裁示(深層頁 302、首頁落地頁)與 cats-portal 說明並取得認可 |

## M7 前端

> 範圍依 2026-07-26 裁示:主頁、導航列、專案頁面、專案歷史、最新版本至頂。
> 技術選型已定案:**Jinja2 + 原生 JS**,見 [決策_前端技術選型.md](決策_前端技術選型.md)。
> **執行順序:T47 → T40 → T41–T45。**

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| ~~T30~~ | ~~最小可用網頁介面~~ | — | — | **由 T40–T45 取代**(範圍已細化) |
| T39 | 前端技術選型定案(Q9) | ✅ | 🟢 | 決策文件含選項比較、代價、實作綱要與推翻條件;靜態檔剝前綴寫法可直接進路由 PR |
| T47 | 錯誤回應內容協商(HTML vs problem+json) | ✅ | 🟢 | 13 條測試(**先紅後綠**);規則為**路徑 AND Accept 兩個條件**——`/v1/*` 與機器端點一律 problem+json,`*/*` 不視為瀏覽器;Jinja2 autoescape 防 XSS(以關掉 autoescape 實測非空測);`Vary: Accept` + `nosniff`;錯誤頁 light 主題、零外部資源 |
| T40 | 導航列與版型骨架(F70) | ✅ | 🟢 | 16 條測試(**先紅後綠**);`base.html` + `app.css` + `web.py`,錯誤頁改為繼承;**解析 HTML 逐一檢查所有 href/src/action 都帶前綴且無絕對網址**;🐛 抓到 `root_path` 與 Mount 二次剝前綴導致靜態檔全站 404,改用一般路由;新增全站 CSP;`optional_identity` 讓網頁不因未登入而 401 |
| T41 | 主頁:專案列表 + 搜尋 + 標籤篩選(F71) | ✅ | 🟢 | 13 條測試(**先紅後綠**);查詢抽成 `queries.query_projects()` **與 API 共用**,並有一條測試直接比對網頁與 API 看到的專案集合;標籤可點、分頁帶著篩選條件;URL 編碼在 Python 端做(autoescape 不管 URL 語意);匿名/待開通顯示提示而非錯誤,且不漏出任何專案 |
| T42 | 專案頁:資訊 + **最新版本置頂**(F72) | ✅ | 🟢 | 16 條測試(**先紅後綠**);下載按鈕排在專案資訊之前;沿用 `latest_published_release()`(draft 不算、以 published_at 判定);🔴 **匿名一律不查詢**,使存在與不存在的回應完全相同(以「先查再判身分」實測其非空測);private 非成員 404;誠實顯示 `not_scanned` 與 SHA-256;附 F26 固定連結 |
| T43 | 專案歷史:版本列表(F73) | ✅ | 🟢 | 16 條測試(**先紅後綠**);🐛 **修正既有缺陷**——`GET /v1/projects/{slug}/releases` 原本依 `created_at` 倒序,與 `/latest` 的 `published_at` 判定矛盾,列表第一筆可能與 latest 指向不同版本且兩邊都不報錯;查詢抽成 `queries.query_releases()` 與 API 共用;draft NULLS FIRST 且非成員看不到;`<details>` 展開(不用 JS,CSP 禁 inline) |
| T44 | 上傳介面:建版本、傳檔、發布(F74) | ✅ | 🟢 | 不需 curl 即可完成一輪上傳與發布;**XHR PUT 並顯示上傳進度**與失敗提示(HTML 表單發不出 raw body PUT);**導航列補上「建立專案」入口**(T40 刻意延後,避免懸空連結);JS 須為外部檔案(CSP 禁 inline) |
| T45 | 待開通頁與管理後台頁(F75、F76) | ✅ | 🟢 | 18 條測試(**先紅後綠**,16 failed 起步);🔴 **待開通頁顯示使用者自己的 `sub`**——業務庫只存 sub,不顯示的話使用者與管理員之間沒有任何對照鍵,開通流程結構上走不通;管理後台於頁面上寫明「這裡只有 sub」的原因(而非加備註欄位落地個資);未登入 302、非管理員 403 兩種回應分流,權限判斷仍轉呼叫 API 的 `require_admin()`;不得停用自己;各頁待開通提示連到 `/pending`;導航列補管理入口 |

## M8 第二期(不納入 MVP)

| # | 任務 | 狀態 | 資料 | 驗收標準 |
|---|---|---|---|---|
| T46 | 病毒掃描接上(F38) | ⬜ | 🟡 | 背景掃描 + 結果回寫;`infected` 的處置政策待 Q11 定案 |

---

## 待補的合規欠項(因事後補記而產生)

| # | 欠項 | 對應任務 |
|---|---|---|
| A1 | ~~測試從未實際執行~~ **已清償**:81 passed、lint 全過、migration 雙向演練 | T50 ✅ |
| A2 | T06–T24 缺動工前計畫段(**已發生的違規,不會消失**;不補寫,僅由 T00 日誌記載) | 永久紀錄 |
| A3 | ~~缺 CI~~ **已清償**:Actions run #3 全綠(test + build + Trivy) | T12 ✅ |
| A4 | ~~設計文件尚未與實作對齊~~ 已於 T04 完成(設計_MVP v2.0) | T04 ✅ |
| A5 | ~~Dockerfile 未建置驗證~~ **已清償**:CI 建置成功,254 MB / UID 1000 / Trivy 0 findings | T12 ✅ |
| A6 | ~~single logout 無自動化測試~~ **已清償**:22 條測試釘住契約 §4.5(測試總數 103) | T16 ✅ |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.92 | 2026-08-31 16:45 | Claude(Benny 裁決 A 案) | **T132 顯示真名**:契約 §4.2a L1 第 3 條經 Benny 裁決放寬(cats-portal 契約 v3.4),專案頁擁有者 / 版本建立者 / 留言作者由 `sub` 前 8 碼改為顯示名稱,無名字者退回 8 碼。🔴 放寬的只有「顯示在哪」——名字仍不進 API(有守門)。憲法紅線同步 v2.8;裁決書依第九條 4 原樣落地 inbox。**653 passed**(648 → 653)|
| v1.91 | 2026-08-31 15:25 | Claude(Benny 指示入憲) | **T131 憲法第十一條入憲**:多目標工作線的彩色標示 + 🔴 佔位符不得混進可貼指令(附守門測試與反向驗證)。CLAUDE.md 升 v2.7、cats-portal 憲法升 v1.22。⚠ `workOrder` repo 不在本 session,那本未改 |
| v1.90 | 2026-08-31 14:45 | Claude(Benny 實跑撞到) | **T129**:`runbook_本地開發與CI.md` §4.1 的 SSO 紅線檢查是簡化版,少了 CI 的三層排除 → 照做必得假警報「❌ 踩到 SSO 紅線」而 CI 是綠的。已改為與 `ci.yml` 逐字相同並附每個排除的理由。⚠ 新增待辦 **T130**(兩邊指令不得分岔)|
| v1.89 | 2026-08-31 14:10 | Claude(Benny:「依你推薦執行」) | **合併 PR #33**(29 commits / 106 檔)。🔴 兩個真問題都處理了:①**19 個任務編號整段撞號**(T89–T107 兩邊意義完全不同)→ 已發布的不動,#33 整段平移 **+21**(T89→T110 … T107→T128),可驗算不必查對照表;②**alembic 雙 head**(兩個 `0009` 接同一個父節點,而 `0009_notify_email` 已在正式機跑過)→ #33 讓號改鏈為 `0010_release_review` / `0011_project_comments`,實測 `alembic heads` 只有一個。⚠ 重編號**刻意不用整檔 sed**:#33 從 v0.2.3 切出,它的分支上同時有我方已發布的 T89–T96,整檔 sed 會改掉 Release 說明引用的編號 —— 只改「#33 新增的那些行」(程式碼側 67 行 + 19 篇 dev-log + 1 份計畫書 + 40 列任務表)。**647 passed**、ruff 乾淨 |
| v1.88 | 2026-08-31 12:20 | Claude(Benny 裁示 A 案) | **T109 完成**:`Dockerfile` 加 `COPY tools/purge_*.py`,T92 那條測試收窄為「不得 COPY `devserver.py` / `tests/` / 整個 `tools/`」(文字變、意圖未減)。🔴 揭露兩件事實並回寫:**runbook §C.2 的 cron 指令從寫下起就不可能成功**(`tools/` 不在 image),而且**那些 cron 根本沒掛** → 稽核保留期清理從上線起一次都沒跑過。新增 `tests/test_ops_scripts.py` 守住「文件引用的腳本必須在 image 內」。**593 passed**(590 → 593) |
| v1.87 | 2026-08-31 11:25 | Claude(Benny:「後台 新增使用者登入 紀錄」) | **T108 完成**:新增稽核動作 `user.login` 與 `user.login_denied`(停用帳號被擋 —— 契約 §1.1 的第二道防線擋下了誰,原本只在會滾掉的 log 裡),後台使用者頁顯示最後登入 / 「從未登入」。🟢 零 migration。**590 passed**(583 → 590)。⚠ 不記 IP 是刻意的(要記應是全表一致的決定);遺留建議:`artifact.upload_rejected` 與 `user.login_denied` 是同一類「被擋下的嘗試」,應一起做一個異常訊號區塊,分開做會變成兩個半套 |
| v1.86 | 2026-08-31 10:35 | Claude | **T107 完成**:被拒收 / 中斷的上傳不再留下 0 bytes 殘骸(五條失敗路徑改走 `_discard()`,刪列 + best-effort 刪物件 + 新增稽核 `artifact.upload_rejected`,零 migration)。既有殘骸以 `tools/purge_failed_artifacts.py` 手動清一次(預設 dry-run),runbook 升 v1.8 加 §C.3 並明寫不掛 cron。**583 passed**(578 → 583) |
| v1.85 | 2026-08-31 09:50 | Claude(Benny:「沒有上傳成功的 就不用顯示了」) | **T106 完成**:檢視面(專案頁、版本歷史頁)只列 `upload_status is ready` 的檔案,計數同步;上傳頁刻意維持全列(擁有者的作業面)。新增 `queries.ready_artifacts()` 一份共用。**578 passed**(574 → 578)。⚠ 新增待辦 **T107**(被拒收/中斷的上傳不留殘骸)。同時「三個版本號」表回寫實況:**v0.2.5 已發布並已部署到 VM**(側欄實測),`v0.2.4` 標記作廢 |
| v1.84 | 2026-08-27 08:50 | Claude(Benny:「做 T104」) | **T104 發版補救:`APP_VERSION` → 0.2.5**。🔴 `v0.2.4` 的 tag 打在 `6dabe7d`(PR #35 之前),該 commit 的常數是 `0.2.3`,而 tag 那輪 CI success → **錯的 image 已在 GHCR**。依第八條 5 不刪不重推(image 已流出),改發 v0.2.5;`v0.2.4` 須在 Release 說明標明作廢。「三個版本號」表據實記入此事並標注 **VM 不要換到 v0.2.4**。🔴 第三次卡在版本號,且前兩次的結論都只寫成建議 —— 守門升為 **T105**(待辦),不再是建議 |
| v1.83 | 2026-08-27 07:55 | Claude | **「尚未合併的工作」據實更正:PR #34 已於 07:16 合併(T98–T102),而 T103 沒進去** —— 常數改動是在合併之後才推的。🔴 於是 `main` 現在的內容是 T98–T102 而 `APP_VERSION` 仍是 `0.2.3`(已發布過),**T103 合併前不得打 tag**(第八條 4)。⚠ 上一列(v1.82)寫的「PR #34 含 T98–T103」在寫的那一刻是對的預期,但它**已經被實際發生的合併時序推翻**,依第二條 5 立即回寫 —— 版本號記載已經三次卡住發版,這一條不能等 |
| v1.82 | 2026-08-27 07:40 | Claude(Benny:「發 0.2.4」) | **T103 發版前置:`APP_VERSION` → 0.2.4**(T98–T102 一版發出)。依第八條 4 在打 tag 之前改好常數;`v0.2.3` 已發布過故不重發(第八條 5),走 patch 是因為這一批沒有改變任何既有介面契約。🔴 **「三個版本號」表依第二條 5 回寫為實況**(原記載仍寫「最新 tag = v0.2.1、VM 跑 v0.2.1、等 v0.2.2 image」,而 v0.2.3 早已推出並已部署)——**這正是第三次因版本號記載而卡住發版的同一個病灶**,故連「尚未合併的工作」那節一起更正為 PR #34。🔴 明載這一版整體是 🟡:T99 的 migration `0009`,**換版必須先跑 `alembic upgrade head`** |
| v1.81 | 2026-08-27 07:20 | Claude | **T102 轉 ✅ —— CI run #249 全綠**(574 + Trivy ✅,openssl 3 條 HIGH 消失)。全量升級 + 「升級後不得還有可升級套件」的驗證有效,掃描門檻一字未改。⚠ 學到:T93 的白名單是**有到期日的決定**,而該換防線的訊號是「它要求人反覆做同一件事」 |
| v1.80 | 2026-08-27 07:05 | Claude(CI 紅) | **新開 T102(🔵)**:Trivy 又紅 —— openssl CVE-2026-14456。🔴 根因是 **T93 的白名單設計**:三天內第二次「為一支新 CVE 改一行 + 跑一輪 CI + 開一個任務」,而 base image 的 CVE 是持續漂移。改為全量 `apt-get upgrade` 並把驗證從「比對版本號」換成「**升級後不得還有可升級套件**」;⚠ 放棄的是「diff 上看得見升了什麼」,這是交換不是忽略 |
| v1.79 | 2026-08-25 11:30 | Claude(Benny 回報) | **T101 專案標題與簡介可編輯**(全套 574,🟢 無 migration)+ 🔴 **修一條真的外洩路徑**:`PATCH` 只要 maintainer 卻能改 `visibility`,而 T100 剛把可見性定為 owner 專屬 —— **API 比介面寬鬆,而寬的那邊才是生效的那邊**。收窄後 maintainer 改可見性 403、owner 不受影響。短名刻意不可改(網址裡沒有轉址) |
| v1.78 | 2026-08-25 10:40 | Claude(Benny 裁示兩題) | **T100 發布者設定查看權限**(全套 566,🟢 無 migration)。權限模型與 API 早就齊備 —— 本任務是**補介面**並把邏輯抽成 `app/members.py` 讓兩邊共用(不留第二套規則)。🔴 補上 3 條「ADMIN 都看得到」的守門測試:那條行為施工前只靠兩行 `is_admin` 活著。搜尋(唯一的新外洩面)五項約束逐條有測試。🐛 重構時吃掉 `transfer_owner`,既有測試 30 秒抓到 |
| v1.77 | 2026-08-25 09:15 | Claude(Benny 裁示三題) | **T99 回報通知信 + 後台入口**(全套 554,🟡 加欄位 migration 0009)。合法路徑是契約 §4.2a **L1b 通則**(不必申請),八項條件逐條落地並以反向測試釘住;`smtplib` 標準庫、`MAIL_ENABLED` 預設 false、失敗只寫 log。⚠ 施工中改正三件事,其中兩件是**我的測試寫錯**(`/v1/me` 的 email 來自 IdP claims、log 測試用 DEBUG 會抓到驅動層 SQL 參數)——後者反而得出一條新結論:**正式環境不得開 DEBUG**。🐛 截圖抓到側欄同時反白兩項。`CLAUDE.md` 升 v2.4 |
| v1.76 | 2026-08-25 06:15 | Claude(發版受阻) | **T98:CI 建置遇失效層快取時自動重試**。`v0.2.3` 的 tag 推上後 build 第一步就死(層快取指向已從 registry 消失的 base image layer),Trivy 與 GHCR 推送全被 skip ——**發版被環境問題擋死**。build 與 publish 兩處加 `--no-cache` fallback;🔴 不遮蓋真正的建置錯誤。⚠ 本次 v0.2.3 需先在 VM `docker builder prune -f` 再重跑(重跑用的是 tag 那個 commit 的 workflow 檔) |
| v1.75 | 2026-08-24 17:40 | Claude(Benny 裁示「0.2.3」) | **T97:`APP_VERSION` → 0.2.3**,T89–T96 一版發出。🔴 v0.2.2 的 tag 始終沒推,而 T96 已進 main —— 再推 v0.2.2 會讓 tag 指到含 T96 的 commit 而版號說 0.2.2;**v0.2.2 就此跳過**。⚠ 第二次因版本號記載卡住發版(T94 是第一次),CI 守門(常數 vs 最新 tag)更該做 |
| v1.74 | 2026-08-24 17:15 | Claude(Benny 回報) | **T96 專案短名自動產生**(全套 540):表單移除必填的 slug 欄位,由名稱產生;撞名自動加後綴而不是把錯誤丟回使用者;API 維持可指定(向下相容)。⚠ 一條既有測試**換了契約**、一條**換了觸發方式**,依 CI 紅線在 dev-log 逐條說明(不是放水)。🔴 實測揭露:中文名稱會得到 `p-xxxxxxxx` 這種不可讀網址,而本平台專案名多半是中文 —— **常態而非例外**,三個選項待裁示 |
| v1.73 | 2026-08-24 16:20 | Claude(Benny 實測回報) | **T95 版號移到側欄底部**(全套 530)。🔴 關鍵不是移動而是**不能整個移**:側欄只有已開通者看得到,而 T68 要求三種身分都看得到版號 —— 拆成「有側欄 → 側欄最底 / 無側欄 → 維持原位」且兩者互斥。順帶記錄一個發現:版號原本在右上角是因為 `app.css` **從來沒有** `.app-version` 規則,它只是 flex 容器裡的第三個項目 —— 那個位置沒有人決定過 |
| v1.72 | 2026-08-24 15:45 | Claude(發版前置) | **T94:`APP_VERSION` → 0.2.2**,並更正本表過期的版本號記載。🔴 `v0.2.1` **早就發布過**(指向 `568014b`),而本表寫「最新 tag = v0.2.0、v0.2.1 待發」——發版照它規劃,導致 VM 在 15:34 換到的 v0.2.1 是 T87/T88 那一版、不含 T89–T93。沒造成故障,但這正是第七條要防的事:**過期文件看起來很權威**。依第八條 5(同版不重發)本批走 v0.2.2 |
| v1.71 | 2026-08-24 15:40 | Claude(發版前置) | **T93 轉 ✅ —— Trivy 兩個 run 全綠**(五輪:`--pull` → 診斷 → 指定套件升級 → 當場驗證 + `--no-cache` → 重試以版本為判準)。留下的資產不只綠燈:Dockerfile 那層現在**自己會擋住無聲失敗**(apt 回 0 卻沒升級 = 建置當場失敗),而它在加上的第一天就擋下一次真的 CDN 索引不一致。發版繼續:合併 → tag v0.2.1 → Release |
| v1.70 | 2026-08-24 15:35 | Claude(發版前置) | **T93 第五輪**:同一個 commit 的兩個 run **結果相反**(接續執行、相隔 4 秒)—— push 那個被**我第四輪加的版本驗證擋下**(util-linux 沒升級),pull_request 那個升級成功、Trivy 綠。🔴 根因是 **`deb.debian.org` 為 CDN,節點間 `Packages` 索引不一致**:抽到未同步 security 更新的節點,`apt-get install --only-upgrade` **回 0 而什麼都不做**。⚠ **沒有那行驗證,今天就會有一個未修補的 image 被推上 GHCR 而 CI 全綠** —— 驗證在加上的第一天就擋下一次真的無聲失敗。修法:重試迴圈以**版本**為判準(`dpkg --compare-versions ge`,不寫死等於某一版,免得日後新修版被誤判)、最多三次、失敗仍硬失敗;`--no-cache` 收回(那層自帶驗證,檢查沒過就不會產生層,所以快取到的必然是成功那次) |
| v1.69 | 2026-08-24 15:20 | Claude(發版前置) | **T93 第四輪**:診斷 2 給出互相矛盾的兩個數字 —— **image 裡 `util-linux=2.41-5`,而新開容器裡 `Candidate: 2.41.5-0+deb13u1`(來源 `trixie-security` 有設定)**。`apt-get` 回 0、build 一路過關,而**什麼都沒升級**,由 Trivy 在下一步才告發 —— 看起來像掃描的問題,其實是這裡的無聲失敗。修法兩件:①Dockerfile 加**當場驗證**(`case` 比對版本,不符即 `exit 1` 並印出實際版本)——不能只相信離開碼;②CI build 暫時加 **`--no-cache`**(`--pull` 只更新 base image,**不影響我們自己那層 `RUN apt-get`**,而 runner 是同一台 VM、層快取長期存在)。診斷 2 步驟已移除。**發版仍暫停** |
| v1.68 | 2026-08-24 15:05 | Claude(發版前置) | **T93 修法定案**:runtime 階段升級 Trivy 表列的八個 util-linux 套件(2.41-5 → 2.41.5-0+deb13u1)。三輪 CI 換到三個事實:`--pull` 無效(base image digest 沒變,上游自 **2026-07-14** 未重建)、**`apt` 實測 APT_OK**。🔴 **計畫錯在把 `apt-get upgrade` 當成行不通** —— 依據是 Dockerfile 檔頭 2026-07-30 的紀錄,而**那則紀錄已經過期**;教訓是「某條路不通」的註解記的是當時環境,要先問它哪一天寫的、能不能當場重驗。Dockerfile 檔頭已加更正,診斷步驟已移除,掃描門檻一字未改 |
| v1.67 | 2026-08-24 14:40 | Claude(發版前置) | **新開 T93(🔵)**:PR #28 的 CI `Build image + Trivy` 紅——base image `python:3.12-slim` 的 util-linux CVE-2026-53612~53615。🔴 **與本批改動無關**(沒動 Dockerfile / requirements,08-11 的 main 是綠的);🔴 修法不能用 `apt-get upgrade`(VM 連不到 apt 套件庫,run #85 的教訓)也不能用 `.trivyignore`(T82 紅線),先加 `docker build --pull`(runner 同一台 VM,舊 base image 快取長期存在)。**發版暫停到 Trivy 轉綠**——不繞過掃描發版 |
| v1.66 | 2026-08-24 14:10 | Claude(Benny:本地 wsl 驗證) | **T92 本機驗證用開發伺服器**(8 條紅→綠,全套 **527**):本機沒有 IdP 就看不到登入後版型,devserver 用測試替身 + SQLite 把真的 app 跑起來;安全性靠三層結構(旗標拒絕啟動 / 只綁 127.0.0.1 / image 內沒有 `tools/` 與 `tests/`)而非自律;🐛 過程抓到替身落差——少了 gateway 剝前綴那層,側欄 active 反白會**看起來像壞的而程式其實是對的**;T89/T90/T91 三頁已用 Chromium 實際截圖驗過。README v2.1 |
| v1.65 | 2026-08-24 11:55 | Claude(Benny 實測回報) | **T89 側欄入口連結移到最下面 + T90 建立版本頁顯示歷史版本號 + T91 教學頁三張視覺化 SVG**(13 條紅→綠,全套 **519**):T89 回寫 T67「放最上方」的理由(層級判斷沒錯、方向錯了);T90 過程被既有測試抓到 **MissingGreenlet 第四次同型**(rollback 後才能查清單),修法是搬查詢而不是在失敗頁省掉清單;T91 圖與文字步驟以測試對釘,避免圖說謊 |
| v1.0 | 2026-07-25 15:53 | Claude(Benny 授權) | 初版:M1–M7 共 30 項任務,含狀態、資料影響分級與驗收標準;T06–T24 因事後補記標 ⚠️;另列四項合規欠項 |
| v1.1 | 2026-07-26 00:15 | Claude(Benny 授權) | 新增 T31 功能需求大綱(補列於 M1,✅);T04 設計文件升版與 T05 文件產生器由 🔵 改 ✅;欠項 A4 結案 |
| v1.2 | 2026-07-26 00:28 | Claude(Benny 裁示) | 依 Q1–Q8 定案新增 T32–T39(需求回寫、容量調降、轉移擁有權、最新版捷徑、標籤、下載統計、稽核紀錄、前端選型);T25 掃毒決策結案(不納入,先預留);M7 由單一 T30 細化為 T40–T45;新增 **M8 第二期**(T46 掃毒) |
| v1.3 | 2026-07-26 00:41 | Claude(Benny 裁示) | Q9 定案:T39 結案(Jinja2 + 原生 JS);**新增 T47 錯誤回應內容協商並列為 T40 前置**;T44 補上 XHR 上傳進度的驗收標準;T28 補上靜態檔剝前綴寫法與 `client_max_body_size` |
| v1.4 | 2026-07-26 00:53 | Claude(Benny 裁示) | Q10 定案:新增 T48(需求回寫,✅)與 **T49 容量級距實作**(🟡 加欄位);T33 驗收標準補上專案預設 2 GB |
| v1.5 | 2026-07-26 01:12 | Claude(Benny 授權) | **T50 清償欠項 A1**:測試實際執行 81 passed、lint 全過、migration up→down→up 演練通過;T06–T09、T11、T13–T15、T17–T24 由 ⚠️ 改 ✅;新增 T50;A1 結案,另立 **A5**(Dockerfile 未建置驗證)與 **A6**(single logout 無測試)兩項新欠項 |
| v1.6 | 2026-07-26 02:41 | Claude(Benny 授權) | **T12 CI 建置**:新增 `.github/workflows/ci.yml`(lint/測試/文件同步/git 紅線/SSO 紅線 → build + image 大小 + non-root + /health 冒煙 + Trivy → 僅版本 tag 推 GHCR 且不覆蓋同版);T12 標 ⚠️(尚未在 Actions 實跑);T10 補上本機實測結果;A3、A5 依實況改寫 |
| v1.7 | 2026-07-26 02:47 | Claude(Benny 授權) | **CI 首次全綠(run #3)**:T12、T10 由 ⚠️ 改 ✅;**A3、A5 結案**;M2 全數完成。實測數字:image 254 MB、UID 1000、/health 2 秒、Trivy 0 findings |
| v1.8 | 2026-07-26 07:10 | Claude(Benny 授權) | **T16 完成、A6 結案**:新增 22 條 single logout / session cookie 契約測試(總數 81→103),未測出缺陷;除 A2 外所有欠項皆已清償 |
| v1.9 | 2026-07-26 07:45 | Claude(Benny 授權) | **T34 轉移擁有權完成**——本專案第一個走完整 TDD(紅→綠)的任務;測試 103→112 |
| v1.10 | 2026-07-26 08:20 | Claude(Benny 授權) | **T35 最新版捷徑完成**(TDD 紅→綠);測試 112→122;下載回應建構抽成共用函式,避免捷徑路徑的安全標頭與主路徑不一致 |
| v1.11 | 2026-07-26 23:30 | Claude(Benny 授權) | **T33 容量調值完成**:單檔 100 MB / 專案 2 GB;新增 `.env.example` 與程式預設的防漂移測試;測試 122→125 |
| v1.12 | 2026-07-27 05:50 | Claude(Benny 授權) | **T36 標籤分類完成,M4 全數 ✅**;本專案第一個 🟡 加表任務,migration up→down→up 雙向演練通過;測試 125→143 |
| v1.78 | 2026-08-12 22:20 | Claude(Benny 提出四項需求) | **依第一條 4 先立計畫不動程式**:新增 [施工計畫書 T122–T125](plans/施工計畫書_審核與回饋_T122-T125.md)。盤點發現四項規模懸殊——T122 後端九成已存在只缺按鈕、T125 資料早就有只缺顯示(名字被契約擋住)、T124 不能沿用問題回報(可見性設計相反)、**T123 最大**(兩態變三態 + migration,且最大風險是「沒有通知管道」的營運現實)。四題待裁示、T122 必先於 T123 |
| v1.79 | 2026-08-12 22:55 | Claude(Benny 四項需求之一) | **T122 管理員指派入口**:後端九成已存在(角色欄位、PATCH API、稽核、bootstrap),只缺網頁按鈕——與 T121 同一形狀。三條防呆做**兩層**(畫面不給機會按、伺服器端 409 才是把關);刻意不做「最後一個管理員不能被降級」(要計數,並行下不可靠)。全套 539 |
| v1.80 | 2026-08-12 23:45 | Claude(Benny 四項需求之二) | **T123 發布審核**:兩態變三態,🎯 `published` 語意刻意不變,既有已發布版本**一列都不用改**(裁示第 2 條靠設計達成而非資料遷移,🔴 降為 🟡)。🐛 up→down→up 演練當場抓到兩個 **548 條全綠也看不到**的 migration bug(pytest 用 `create_all` 不跑 migration)。既有 21 條依新行為改寫。全套 548 |
| v1.81 | 2026-08-13 00:05 | Claude(Benny 執行演練) | **T123 欠項清除**:0009 的 PostgreSQL up→down→up 三步全過,`information_schema` 量到 `status` 已是 VARCHAR(20);⚠️ 誠實註明那是**空庫**演練,證明 DDL 跑得動,不是資料搬遷邏輯 |
| v1.82 | 2026-08-13 00:45 | Claude(Benny 四項需求之四) | **T125 版本作者屬名**:資料早就在(`created_by_id`),只缺顯示;名字仍卡在契約 §4.2a L1,先顯示 sub 前 8 碼。🔴 一次 `IN` 查完不逐列查,護欄**實作前就綠**——它真正的價值在**實作後仍然綠**。全套 552 |
| v1.83 | 2026-08-13 01:25 | Claude(Benny 四項需求之三) | **T124 專案留言板,四項需求全部完成**:不可沿用問題回報(可見性設計相反)。🔴 **專案擁有者不能刪別人的留言**——只留得住讚美的回饋區比沒有回饋區更糟。🐛 一條**為錯的理由而綠**的假綠(端點不存在時 404 也會過,且永遠不會紅),補前提斷言後才真的紅。全套 559 |
| v1.84 | 2026-08-13 01:50 | Claude(發版前置) | **T126 `APP_VERSION` → 0.3.0**:這一版累積四項新功能與兩個 migration,且 🔴 `publish` 的**語意改了**(發布→送審)——用 patch 版發等於版本號說謊。0.2.2 從未打成 tag,**跳號不補** |
| v1.85 | 2026-08-26 07:15 | Claude(CI 紅燈觸發) | **T127 openssl CVE 擋下建置**:PR #33 的 Trivy job 紅,三筆 HIGH 是同一個 CVE-2026-14456。**不是 T117 的快取**——`--pull` 已生效,是**上游 image 尚未重建**;執行 T117 預先寫好的處置(runtime stage 只升 openssl 家族)。🔴 第三次重申不用 `.trivyignore`,即使本 CVE 對我方幾乎確定不適用。📌 順手補齊 **v1.79–v1.84 六列漏記的版本歷史**——那六次只改了抬頭沒補歷史列,正是第七條 4「缺一不可」要防的事 |
| v1.86 | 2026-08-26 07:40 | Claude(CI 實測回報) | **T127 方案失敗並回退**:deb.debian.org 從 CI 容器網路仍不可達。🔴 真正的發現是第二個——**`apt-get update` 抓不到索引時 exit code 仍是 0**,建置因此產出一個「看起來已修好、實際沒動過」的 image;護欄立意正確卻守錯地方,**抓到的是 Trivy 不是護欄**。一課:`&&` 鏈只證明每個指令自稱成功,不證明事情做到了。結論:我方修不掉,建議開通 apt(第三次被 base image 的 OS 套件擋);⚠️ 此紅燈是 **v0.3.0 的硬阻塞** |
| v1.87 | 2026-08-26 08:35 | Claude(Benny:「目前狀態 寫成一份文件」) | **T128 狀態總覽頁**:狀態原本散在四處、沒有一頁看得完;重點在按阻塞源重編的〈誰的球〉,🔴 明示是摘要不是真相來源。🐛 順手抓到 **`--check` 的盲點**——只掃 TARGETS 清單,漏掉的檔案不在清單裡就檢查不出來,第 11 份申請書因此沒有 HTML 版而 CI 照樣綠;補守門第 9 條(判準綁索引)。全套 560 |
| v1.77 | 2026-08-12 22:05 | Claude(Benny 實測回報) | **T121 草稿版本回不去**:上傳頁只在建立版本後導向一次,離開就找不到路,草稿成孤兒;版本歷史頁草稿列加「繼續編輯」,判準與發布同一條(viewer 不給必然 403 的連結、已發布不給入口);🐛 順手修掉 HTML 模板裡的 Markdown 星號;全套 534 |
| v1.76 | 2026-08-12 21:15 | Claude(Benny 裁示) | **T120 憲法第十條成文**:指令要標明機器與工作目錄,🖥️ 執行與 📄 編輯必須分清楚(08-11 換版失敗的根因);兩份 runbook 23 個區塊全標;標記動作逼出三個坑(打 tag 在本機、cron 是編輯、`.env` 區塊看起來像指令);全套 531 |
| v1.75 | 2026-08-12 20:30 | Claude(Benny:部屬本地 WSL 跑 CI) | **T119 本地開發環境**:新增 `docker-compose.dev.yml` 與本地 runbook;🐛 回寫 README 那句在 WSL 必敗的錯誤指示;dev/正式兩份 compose 由 5 條測試兩邊一起釘,防止互相污染;全套 528 |
| v1.74 | 2026-08-12 19:45 | Claude(Benny 裁示) | **T117 CI `--pull`**(base image 被本機快取釘住,Trivy 對著沒改過的東西紅燈)+ **T118 專案頁擁有者欄位**(契約 §4.2a L1 擋住名字,過渡顯示 sub 前 8 碼並送申請擴大用途;🐛 測試視角寫錯的教訓);全套 523 |
| v1.73 | 2026-08-12 18:50 | Claude(Benny:好) | **T116 `APP_VERSION` → 0.2.2**:依第八條 4 在打 tag 前改好;本版主角是 T115 中文文字檔判型修正(修正需換版才生效),另含 T113 觀測補強與 T110–T112/T114 文件工具;🟢 無 migration;換版驗證加一項🖐 人工必驗中文大檔上傳 |
| v1.72 | 2026-08-12 18:30 | Claude(Benny 實測回報) | **T115 修好中文文字檔上傳**:判型的 UTF-8 截斷補救「砍尾端 3 bytes」會砍破前一個中文字,導致 >4KB 的中文 `.md`/`.py` 被判成 binary 擋下(三種對齊兩種失敗,故症狀時好時壞);改用增量解碼器分開「被截斷」與「不是文字」;白名單與 HTML/SVG 拒收未動;🔴 要換版才生效,建議 v0.2.2;5 條紅→綠,全套 520 |
| v1.71 | 2026-08-12 17:30 | Claude(Benny 裁示) | **T114 憲法第九條成文**:文件抬頭加 `專案`,跨專案文件加 `發文專案 / 受文專案 / 發文時間` 並以 `<發文專案>_致_<受文專案>_<類別>_<主題>_<時間>` 命名;新增跨專案文件索引(9 份登記在案)與 5 條守門測試;既有檔案不改名(不以新規則竄改舊日誌);T113 回覆文件升 v1.1 補 VM log 證據——**:44 無紀錄排除可能(a)**;全套 515 |
| v1.70 | 2026-08-12 11:40 | Claude(portal v2.0 回函) | **T113 完成**:時序誤讀更正(refresh 在 authorize 之前不是 callback 之後)、§6.5 三問正面回答、壞 cookie 觀測補強(2 條紅→綠,全套 510);回覆文件《回覆_portal_session時序》成文;:44 踢回待雙方 log 比對 |
| v1.69 | 2026-08-12 01:20 | Claude(Benny:可否寫測試腳本) | **T112 冒煙腳本完成**(守門 2 條紅→綠,全套 508):可腳本化者一鍵化、登入後項明示 SKIP;本機實跑抓到哨兵裸抓撿錯範例版本號的 bug,改錨定頁尾固定格式 |
| v1.68 | 2026-08-12 00:30 | Claude(Benny:量身訂做測試項目) | **T111 測試項目清單**:11 區 97 項、自動/人工分工、冒煙子集與執行順序、測試地圖 SVG;SOP v2.1 補互指 |
| v1.67 | 2026-08-11 23:55 | Claude(Benny:搭配 SVG、說明簡單一點) | **測試 SOP 升 v2.0**:精簡約四成 + 四張內嵌 SVG(層級/TDD 流程/假綠對照/換版冒煙),首度使用第四條 5–6 的 `<!--SVG:名稱-->` 機制於 architecture 以外的圖;規則內容不變 |
| v1.66 | 2026-08-11 23:30 | Claude(Benny 指示) | **T110 測試 SOP 成文**:把散落的測試紀律收攏為 `docs/測試SOP.md`(照著做定位)——TDD 五步、撰寫規範、紅線對照表 13 列、十條實踩陷阱;每條規則標注出處任務編號 |
| v1.65 | 2026-08-11 22:35 | Claude(換版實測回寫) | **v0.2.1 已上線**:VM 換版完成,三個版本號(程式碼 / tag / VM)首度一致;🐛 換版第一次失敗——`docker-compose.yml` 的 image tag **沒改到**(那一步是編輯檔案,被當成 shell 指令貼了),`pull` 拉的仍是 v0.2.0、容器顯示 `Running` 而非 `Started`;**哨兵當場抓到**(這正是 T68 版本號 + T81 搬到共用版型的價值:登入壞掉時仍能匿名驗版本)。換版指令改用 `sed -i` + `grep` 覆核,不再要求人工編輯 |
| v1.64 | 2026-08-11 11:40 | Claude(Benny:先做 1 和 2) | **T87 OIDC 錯誤碼入 log**(6 條紅→綠,全套 506)+ **T88 runbook 發版前補完**(v1.7):前者讓下次登入故障能一眼定位、後者把 `DROP TABLE` 的兩支 migration 與 purge cron 補進 runbook;另新增給 portal 的**故障通報文件**(redirect_uri 400 導致登入中斷) |
| v1.63 | 2026-08-05 13:40 | Claude(Benny 回報) | **T86 上傳三格卡片 + 取消上傳完成**(9 條紅→綠,全套 500):修掉「上傳中無退出路徑」(無 abort、無 timeout)與「下拉預設值即錯配」;三類的單一真相併成 `REQUIRED_KINDS`,卡片與缺項訊息同序 |
| v1.62 | 2026-08-05 12:10 | Claude(Benny 回報) | **T85 待開通清單顯示名稱與申請時間完成**(6 條紅→綠,全套 491):T59 只改一半的漏網之魚,漏掉的是有後果的那一半;順帶修掉一條守的東西已與字面脫節的個資測試(改為以 `@` 掃全頁,比原本更嚴) |
| v1.61 | 2026-08-05 11:00 | Claude(Benny 裁示) | **T84 稽核紀錄顯示使用者名稱完成**(8 條紅→綠,全套 485):名字只上後台不上 API、一次 IN 查完並以查詢計數釘住、查不到退回 UUID;記下一個**自己寫的假綠安全測試**及其修法 |
| v1.60 | 2026-08-05 09:40 | Claude(Benny:開 PR) | **T83 `APP_VERSION` → 0.2.1**:PR #24 合併後常數與 tag 不一致,依第八條 4 在打 tag 前先改好;不重發 v0.2.0(第八條 5);〈目前進度〉更新為 PR #24 已合併、下一版 v0.2.1 待發 |
| v1.59 | 2026-08-04 11:30 | Claude(CI 紅燈觸發) | **T82 修 CI:cryptography CVE-2026-69247**(HIGH):遞移相依 + 相容區間 = 建置抓到帶洞版本;加明確下限而非 ignore CVE、也不順手改 lock 檔;遺留:requirements 仍非 lock 檔,建置不可重現,建議另立任務 |
| v1.58 | 2026-08-04 10:30 | Claude(Benny 裁示) | **T81 首頁入口導流完成**(8 條紅→綠,既有 23 條依新行為改寫,全套 477):推翻 T53「首頁留落地頁」的那一半;落地頁只剩非瀏覽器看得到(冒煙判準不動);停用者的迴圈在 callback 擋下;F79 版本號搬到共用版型;〈目前進度〉一節同步更新(PR #22 已合併、v0.2.0 已發並已上 VM) |
| v1.57 | 2026-08-03 16:30 | Claude(使用者交辦) | **T80 品牌標誌改用入口網站官方 logo 完成**(5 條紅→綠,全套 468):導航列由偽元素近似標記改為載入官方檔,並補上原本缺席的 favicon;斷言比對 **SHA-256** 而不是「有一張圖」——「看起來很像」正是這次要修的問題本身,目視驗收會把它驗過;偽元素規則整段移除並留下根因註解,回歸斷言先剝 CSS 註解再檢查(否則會逼人刪掉那段防改回去的歷史) |
| v1.56 | 2026-08-01 03:20 | Claude(Benny:完成能完成的項目,前提是先寫開發計畫書) | **T78 附件與貼圖 + T79 收尾整合完成**(18 條紅→綠,全套 463):開了本專案第一條 inline 下載路徑並以六條收窄 + 回歸測試守住舊路徑;總覽待辦加「未處理的回報」;清除工具預設 dry-run;兩份動工前計畫皆先成文 |
| v1.55 | 2026-08-01 02:10 | Claude(Benny 裁示「照建議做」) | **T77 問題回報第一期完成**(36 條紅→綠,全套 445):自家 Markdown 轉譯器、兩張表 + migration 演練、狀態機與權限(非本人非管理員 404)、待開通者可回報、入口提前接上;施工計畫書回寫 v1.1 |
| v1.54 | 2026-08-01 01:00 | Claude(Benny 指示) | **新增問題回報系統施工計畫書**:五題建議裁示(inline 圖片建議接受、Markdown 白名單、僅本人與管理員、365 天、先交付 T77)、兩支 migration DDL、端點與權限矩陣、安全設計逐條、各期測試條數、三期發版 v0.2.0–v0.2.2、風險與回滾、驗收清單 |
| v1.53 | 2026-08-01 00:20 | Claude(Benny 裁示) | **開立 T77–T79 問題回報系統**:設計文件成文(富文本走 Markdown 不走 HTML、貼圖需開 inline 路徑並以六條收窄、三表資料模型、狀態機與權限、無 email 不假裝通知、保存期 365 天);**待 Benny 裁示五個問題後動工** |
| v1.52 | 2026-07-31 23:45 | Claude(Benny 指示) | **T76 README 更新完成**(v2.0:功能一覽、路徑拓撲圖、開發與部署指引、文件索引)|
| v1.51 | 2026-07-31 23:00 | Claude(Benny 授權) | **T74 front-channel logout 完成**(契約 v2.0 §10 義務,7 條紅→綠)、**T75 回報問題獨立入口完成**(3 條紅→綠);全套 **409**;CLAUDE.md 升 v2.3(契約對齊 v2.0)|
| v1.50 | 2026-07-31 21:40 | Claude(Benny 裁示) | **開立 T73**(顯示名稱改 Admin API 即時查詢、帳號不落地):申請書已成文送 portal,明示推翻 T59 的評估立場;裁示前不動工,過渡期維持現行快取 |
| v1.49 | 2026-07-31 21:00 | Claude(Benny 裁示) | **T70 管理後台總覽完成**(13 條紅→綠,全套 399);功能需求大綱新增 F81(v3.13);T71/T72 待續 |
| v1.48 | 2026-07-31 18:50 | Claude(Benny 裁示) | **T69 更名「AI 小程式分享平台」完成**(5 條紅→綠,全套 386);功能需求大綱新增 F80(v3.12) |
| v1.47 | 2026-07-31 18:00 | Claude(Benny 裁示) | **T68 首頁版本號 + 憲法第八條〈發版儀式〉完成**(4 條紅→綠,全套 381);功能需求大綱新增 F79(v3.11);CLAUDE.md 升 v2.2 |
| v1.46 | 2026-07-31 17:10 | Claude(Benny 裁示) | **T67 回到平台入口連結完成**(6 條紅→綠,全套 377);功能需求大綱新增 F78(v3.10) |
| v1.45 | 2026-07-31 16:00 | Claude(Benny 裁示) | **T66 使用教學頁 + 回報管道完成**(6 條紅→綠,全套 371);功能需求大綱新增 F77(v3.9) |
| v1.44 | 2026-07-31 15:00 | Claude(Benny 裁示) | **T65 發布強制三類齊備完成**(4 條紅→綠,全套 365);F22 同步加嚴(功能需求大綱 v3.8) |
| v1.43 | 2026-07-31 01:10 | Claude(Benny 授權) | **T64 靜默 SSO 完成**(8 條紅→綠,全套 361);另:v0.1.4 已於 2026-07-30 14:51Z 完成換版(T57–T63+T59 全數上線),事故鏈見 dev-logs/2026-07-30_v013v014發版事故 |
| v1.42 | 2026-07-31 11:00 | Claude(Benny 授權) | 收掉兩筆過時狀態:**T26、T28 轉 ✅**(上線日即已完成,portal 2026-07-30 對齊函確認 commit 回權威 repo 與契約 §9 登記);M6 僅餘 T27 演練 |
| v1.41 | 2026-07-31 09:00 | Claude(Benny 授權) | **T63**:groups claim 通知對齊——只讀不判(現況本就不讀),4 條防禦測試防止提早接;全套 353 |
| v1.40 | 2026-07-30 09:30 | Claude(Benny 授權) | **T59 完成**:portal 2026-07-30 裁決**准**(升格契約通則 §4.2a L1)——migration 0006 加 `display_name_cache`(nullable)、每次登入以 `name` claim 覆寫(無 claim → NULL,不採 preferred_username)、後台顯示「名字+截斷 sub(title 帶完整值)」、/v1/me 不輸出、`tools/purge_name_cache.py` 整批清除(必然涵蓋孤兒);**5 條紅→綠,全套 349** |
| v1.39 | 2026-07-29 18:00 | Claude(Benny 授權) | **T62 完成**:導覽改 PLM 式頂列+左側欄(active 反白、行內 SVG 圖示、窄幅漢堡);344 全綠 |
| v1.38 | 2026-07-29 17:20 | Claude(Benny 授權) | **T61 完成**:Bootstrap 版面元件進內容區——麵包屑三頁(T56 遺留 #2 結案)+ 專案頁響應式兩欄;344 全綠 |
| v1.37 | 2026-07-29 16:30 | Claude(Benny 授權) | **T60 完成**:portal 施工單 v1.3 §4.4 對齊——OIDC 伺服器端呼叫改可覆寫內部端點(7 條紅→綠,全套 344);runbook v1.5 |
| v1.36 | 2026-07-29 15:30 | Claude(Benny 授權) | 補記:**v0.1.2 發版中斷**——tag 已建、CI 兩次「秒殺無 runner」(帳號層,疑 Actions 額度),T57/T58 尚未上線,線上維持 v0.1.1;中斷點與續辦見 [dev-logs/2026-07-29_v012發版中斷_CI無runner.md](dev-logs/2026-07-29_v012發版中斷_CI無runner.md);runbook 升 v1.4(版本哨兵/秒殺診斷);T59 申請日誌補齊 |
| v1.35 | 2026-07-29 15:00 | Claude(Benny 授權) | 新開 **T59 顯示名稱快取**(⬜ 待 Platform 裁示):Benny 裁示需要認人;依 §4.2 紅線走**具名特例申請**而非直接實作,申請書見 plans/;裁示前不動任何欄位 |
| v1.34 | 2026-07-29 14:30 | Claude(Benny 授權) | **T58 導航列 Bootstrap 5 化完成**(Benny:「導航太擁擠,採用 bootstrap 5」):自託管 vendor、七項收四群(使用者下拉+漢堡);CSP 與 337 條測試逐字不動——`data:` 圖示與 `href="#"` 兩個框架慣例都因紅線改走替代做法 |
| v1.33 | 2026-07-29 13:35 | Claude(Benny 授權) | **T57 UI 打磨完成**(Benny:「再美化一下介面」):同一套設計語言內補動效與質感——雙層陰影、毛玻璃導航列、focus 光圈、摺疊箭頭、404 漸層字;動效收在 reduced-motion 查詢內尊重無障礙;純 `app.css` 異動,337 測試全綠,隨下一個 tag 發布 |
| v1.32 | 2026-07-29 11:40 | Claude(Benny 授權) | **T56 UI 改版完成**:套用設計稿(藏青+綠、膠囊按鈕、卡片漸層條、amber 警示徽章、未登入 hero、管理分頁列、複製識別碼鈕)。落地紀律:337 條既有測試**一條都不放寬**全綠——裝飾一律偽元素、動態寬度用原生 `<progress>`、測試釘住的文字與連結全數保留。測試數不變(純前端) |
| v1.31 | 2026-07-29 10:20 | Claude(Benny 授權) | **🎉 v0.1.0 正式上線**(2026-07-29,第一個管理員已開通,既有系統零影響)。新開並完成 **T55 首次上線實測修正**:四個真缺陷——bootstrap 只在建號時生效(與 T45 自助流程結構性矛盾,兩份文件互打且兩邊都測過綠,因為沒有測試涵蓋「先登入後設清單」的順序)、backup.sh 假設 minio 有 tar、ensure_bucket 把已存在當錯誤、compose 缺 hairpin/CA。上線過程另抓到兩個 **portal 側缺陷**(gateway 憑證是舊自簽 sportonhr1cb、cats-edge 別名掛在 keycloak),已修正/回饋,見 [狀況同步](plans/狀況同步_給portal_20260729上線.md)。新增 [介面導覽_給UI設計](介面導覽_給UI設計.md)。測試 329→337 |
| v1.30 | 2026-07-29 07:30 | Claude(Benny 授權) | **依第二條 5 回寫 portal 的兩項更正 + 記錄 client 建立完成**。(1)施工單 v1.1:Keycloak 自 2026-07-27 即已上線,先前所有「卡 P0-A」的記載不成立——本案沒有外部阻擋;(2)施工單 v1.2 §3.0 施工順序反轉:我方容器**先**上線,portal 路由註解中等我方(nginx 對不存在上游是 [emerg] 全份載入失敗,順序弄反 = 全平台中斷);(3)portal 已執行 client 腳本,回讀驗證通過,secret 待安全管道交付。漏抓補正:**全平台統一 PG15**(施工單 §3),compose 16.4→15.8;runbook 新增 §A0 首次上線八步 |
| v1.29 | 2026-07-29 04:25 | Claude(Benny 授權) | **T27 文件部分完成,⬜→🔵(刻意不轉 ✅——驗收標準是「還原實際演練過」,文件寫完不降標)**。新增 [runbook_換版與備份還原.md](runbook_換版與備份還原.md):換版/回滾/備份/還原演練四段 SOP;兩件懸著的事就此落地——施工單 §3.2 的 reload 約定寫成換版第 A.3 步、T38 遺留 #1 的稽核清理 cron 寫進 §C.2;還原演練設計成 11 步證據表,含「毀掉再還原」與 SHA-256 驗功能(列數對了只代表 metadata 回來,檔案抓得回、雜湊對得上才代表系統活著) |
| v1.28 | 2026-07-29 03:20 | Claude(Benny 授權) | **收到 portal 裁決函與施工單,T26/T28 由 ⬜ 轉 🔵、新開 T54 並完成**。路徑前綴 `/upload/`、redirect URI、`client_max_body_size`、gateway 路由**全數定案**,portal 側程式碼已就緒,**唯一阻擋項是 Keycloak 正式環境部署**(Platform P0-A)。T54 逐項核對施工單技術規格,抓到一項真的要修的地方:`SecurityHeadersMiddleware` 自行送 `X-Frame-Options` 與 gateway 重複(語意不支援多值合併,行為未定義),已拿掉並補測試釘住;compose 補 `container_name` 求字面精確;DB 命名對齊平台規約(尚未上線,現在改)。回覆文件見 [plans/回覆_portal施工單.md](plans/回覆_portal施工單.md);測試 328→329 |
| v1.27 | 2026-07-28 08:00 | Claude(Benny 授權) | T26 更新:[SSO接入申請_給portal.md](plans/SSO接入申請_給portal.md) 升版至 v1.1(反映 T44/T45/T38 完工——測試數 249→328、§4.10 已導入外部 JS 後的守門方式、新增稽核紀錄個資邊界聲明、§4.3 雞生蛋問題已有自助解法),**申請書就緒可送出** |
| v1.26 | 2026-07-28 07:30 | Claude(Benny 授權) | **T38 稽核紀錄完成,MVP 可自主的部分全數結束**(剩下 T26–T29 全等平台方)。本任務的本質是**兌現 T37 開的支票**:T37 不做下載事件表時說稽核「有自己的保存期限與存取權限」,T38 讓那句話有落點。🐛 **T37 的紅測試立刻抓到 T38**——「一次下載不在任何表新增列」在 F54 之後不再成立,但處理方式是改成**具名例外**(只允許 `audit_events` 增加恰好一列,比原本更嚴),不是放寬斷言;`設計_MVP.md` §3.5 依第二條 5 回寫範圍。核心設計:快照而非外鍵、同交易不留假紀錄、action 不下 DB CHECK。測試 303→328 |
| v1.25 | 2026-07-28 06:40 | Claude(Benny 授權) | **T45 完成,M7 全數 ✅**。核心發現:「業務庫只存 sub」這條紅線的代價是使用者與管理員之間缺一條對照鍵——**待開通頁把 `sub` 交到使用者手上**才是這一頁真正的功能,不是安撫文案。計畫寫「直接用 `AdminUser` 依賴」在執行中證明是錯的(它對未登入者回 401,與 T53 的深層頁 302 相衝),已依第二條 5 回寫修正;權限判斷仍轉呼叫 API 同一個 `require_admin()`。「不得停用自己」以拿掉判斷實測——管理員停用自己後連轉址頁都載不進來,是這條規則的實地演示。測試 285→303 |
| v1.24 | 2026-07-28 05:35 | Claude(Benny 授權) | **T44 上傳介面完成,M7 出場條件達成**(不需 curl 即可完成一輪上傳與下載)。抓到兩個真 bug:路由順序被 `/projects/{slug}` 吃掉、`rollback()` 讓 ORM 物件過期導致 MissingGreenlet 第三次變形。CSRF 依決策文件 §6.5 完成評估:不加 token,理由是 `SameSite=Lax`,**條件已寫成測試**。導航列「建立專案」入口補上(T40 遺留結案);測試 267→285 |
| v1.23 | 2026-07-28 04:05 | Claude(Benny 授權) | **T51、T53 完成,SSO 的 S0(可自主部分)全數就緒**。T51:`/account` 連結撞到「所有連結帶前綴」紅線,以**具名例外**處理而非放寬斷言,例外本身有測試;§4.10 五項同源義務逐條測試化(含 `localStorage` 的原始碼守門,T44 導入 JS 後會真的擋人)。T53:深層頁 302、首頁留落地頁(Benny 裁示),**同一個「不洩漏存在」陷阱第二次出現**並再次以改壞實作實測。新增 [plans/待portal提供資訊.md](plans/待portal提供資訊.md) 交付清單。測試 249→267 |
| v1.22 | 2026-07-28 02:25 | Claude(Benny 授權) | 依第二條 3 新增 **[docs/plans/SSO接入計畫.md](plans/SSO接入計畫.md)**(跨 T26/T28/T29/T51/T52/T53 的大工程,先立獨立計畫);T26 掛上申請內容指標;`tools/render_docs.py` 的 TARGETS 納入 `plans/` |
| v1.21 | 2026-07-28 02:20 | Claude(Benny 授權) | **T52 完成**:網頁 session 在 access token 過期時由伺服器端向 IdP 續期。**先補測試替身(FakeOidc 原本 token 永不過期)才看得見這個缺陷**——240 條測試一條都抓不到;續期真的去問 IdP,不自行延長,收權即時性不受損;測試 240→249 |
| v1.20 | 2026-07-28 02:06 | Claude(Benny 授權) | **收到 SSO 契約 v1.7 並逐條對照**(原對齊 v1.0,期間有 v1.3 token 壽命定案、v1.5 同源義務、v1.6 註冊政策三次實質變更)。依第一條 4「需求變更先更新計畫」新增 **T51 契約對齊**、**T52 網頁 session 自動續期**(🐛 真缺陷)、**T53 深層頁未登入 302**;T29 補上第 1 項的處置方式 |
| v1.19 | 2026-07-28 01:45 | Claude(Benny 授權) | **T43 專案歷史完成**;寫計畫時發現 `list_releases` 的排序缺陷(`created_at` vs `published_at`)並一併修正——**這改變了既有 API 的回應順序**,已在日誌與設計文件記錄;測試 224→240 |
| v1.18 | 2026-07-27 15:22 | Claude(Benny 授權) | **T42 專案頁完成**;匿名訪客的回應以「不查詢」達成結構性不洩漏(先查再判身分的寫法會讓存在與否從狀態碼看得出來,已實測測試抓得到);首頁卡片接上連結,T41 遺留 #1 結案;測試 208→224 |
| v1.17 | 2026-07-27 15:08 | Claude(Benny 授權) | **T41 主頁完成**;可見性查詢抽成共用函式(API 與網頁各寫一份的話,分岔的後果是 private 專案外洩),並以「拿掉可見性過濾 → 測試變紅」實測其非空測;待開通文案取自 `problems.pending_activation()` 的同一來源;測試 195→208 |
| v1.16 | 2026-07-27 14:52 | Claude(Benny 授權) | **T40 導航列與版型骨架完成**;實作中抓到子路徑的真實缺陷(`root_path` + `Mount` 二次剝前綴 → 靜態檔正式環境全站 404),已改用一般路由並留下根因註解;順帶導入全站 CSP 並收掉 T47 的兩項遺留;F70 的「建立專案」入口刻意延後到 T44(避免懸空連結)並已寫進其驗收標準;測試 179→195 |
| v1.15 | 2026-07-27 14:35 | Claude(Benny 授權) | **T47 錯誤回應內容協商完成**(M7 第一項,T40 前置解除);導入 jinja2 相依與 `app/templates/`;協商規則刻意加上「路徑」條件,避免呼叫端用一個 Accept 標頭就改掉整個 API 表面的錯誤格式;測試 166→179 |
| v1.14 | 2026-07-27 13:55 | Claude(Benny 授權) | **T37 下載次數統計完成,M5 全數 ✅**;採計數欄位而非事件表,「統計不記個資」成為結構保證;併發測試以 naive `+=` 實測會掉數(1≠5)證明非空測;測試 157→166 |
| v1.13 | 2026-07-27 06:05 | Claude(Benny 授權) | **T49 容量級距完成**(TDD 紅→綠,13 條全紅起步);本專案第一個 🟡 **加欄位**任務,演練確認回滾只掉 `quota_tier`、其他欄位與資料列不變;`ProjectOut` 的組裝抽成 `security.project_out()`(原散在 8 處);測試 143→157 |
