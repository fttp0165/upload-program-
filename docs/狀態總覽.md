# upload-program 狀態總覽

**建立日期:** 2026-08-26 15:58
**最後更新:** 2026-08-26 15:58
**版本:** v1.0

> 本文是專案現況的**一頁總覽快照**,給新加入者與回頭查看的人。
> 🔴 **權威來源是 [任務表.md](任務表.md) 與 [dev-logs/](dev-logs/)**——兩邊不一致時以任務表為準;
> 本文新鮮度以檔頭「最後更新」判斷。本次快照對齊任務表 **v1.75(2026-08-24 17:40)**。

---

## 1. 系統定位與部署現況

- **系統:** upload-program——公司內部**開發者程式分享平台**(「AI 小程式分享平台」,T69 更名),
  簡易型 GitHub:登入後建立專案,上傳**原始碼、執行檔、程式文件**,以版本為單位發布、瀏覽、搜尋、下載。
- **技術:** Python 3.12 + FastAPI + PostgreSQL + MinIO(物件儲存)。
- **部署:** Cats 共用 VM,經 `portal-gateway` 掛載於子路徑
  `catsapp.sporton.com.tw/upload/`,**✅ 已上線**(cats-portal 系統清單)。
- **MVP 不做:** git 協定(clone/push)、PR / code review、issue、線上編輯、CI 建置、外網公開。
  一句話界線:**有版本管理的程式發布倉庫,不是分散式版本控制系統。**

## 2. 版本現況(三個版本號各在哪裡)

| 位置 | 版本 | 說明 |
|---|---|---|
| `app/version.py` | **0.2.3** | T97 已改好(憲法第八條 4:tag 前先改),**待發**;⚠ `v0.2.2` 跳過不發(其內容未單獨出貨,見 T97) |
| GitHub 最新 tag | **v0.2.1** | 指向 `568014b`(T87/T88) |
| VM 執行中 | **v0.2.1** | 2026-08-24 15:34 換版(T87/T88,**不含 T89–T93**);待 v0.2.3 image 推出後再換,🟢 無 migration |

## 3. 里程碑與任務進度

任務 95 項:**✅ 90 / 🔵 1 / ⬜ 3**(另 ~~T30~~ 已由 T40–T45 取代)。

| 里程碑 | 狀態 |
|---|---|
| M1 規則與計畫 | ✅ 完成(8/8) |
| M2 骨架與規約落地 | ✅ 完成(8/8) |
| M3 身分接入 | ✅ 完成(5/5) |
| M4 核心業務 | ✅ 完成(7/7) |
| M5 檔案 | ✅ 完成(6/6) |
| M6 接入 Cats | 🔵 進行中(餘 T27 / T29 / T73) |
| M7 前端 | ✅ 完成(8/8) |
| M8 第二期(不納入 MVP) | ⬜ 未開始(T46 病毒掃描) |

## 4. 未完成任務與卡點

| # | 任務 | 卡在哪裡 |
|---|---|---|
| T27 🔵 | 換版 runbook + 備份**還原演練** | 文件已完成;演練待 staging 環境,回填證據表後才轉 ✅ |
| T29 ⬜ | 接入冒煙清單(SSO 契約 §7) | 六項餘 4 項待實跑;T81 改了首頁行為(首頁也 302),第 1 項說明要一併更新 |
| T73 ⬜ | 顯示名稱改 Admin API 即時查詢 | **待 portal 裁示**;契約 v2.0 §4.2a L1 明文禁用 Admin API,屬**通則層級變更**,申請書需重寫 |
| T46 ⬜ | 病毒掃描接上(F38) | M8 第二期,不納入 MVP;現以 `scan_status=not_scanned` 誠實標示 |

## 5. 平台整合狀態(SSO / SLO / gateway)

- **SSO 契約:已對齊 v2.0(2026-07-31)**——RS256/JWKS、業務庫只存 `sub`、
  首登「待開通」deny-by-default、PKCE;IdP 與各 App **同源**,§4.10 額外義務對本專案有強制力。
- **單一登出(SLO,契約 §10):** front-channel logout 端點**已實作(T74)**;
  SLO 聲明文已送 portal,**等對方登錄 `frontchannel.logout.url`** 後才能重跑 §10 的 A→B 復現步驟。
- **等外部回覆(不在本專案手上):** ① 上述 SLO 登錄;② Admin API `view-users` 申請(T73 前置)。
- gateway / 部署變更依 cats-portal 的《App服務對齊指南》;平台文件**只引用、不複製維護**。

## 6. 測試與品質

- 測試 **527 passed**、`ruff check` 乾淨(2026-08-24)。
- CI(GitHub Actions)push/PR 必跑;Trivy base image CVE 紅燈已於 T93 處理。
- 文件 md/HTML 同步由 `python tools/render_docs.py --check` 把關。
- 安全底線(自我加嚴):上傳驗 magic bytes、HTML/SVG 拒收、下載強制
  `Content-Disposition: attachment` + `nosniff`、每檔保存 SHA-256。

## 7. 遺留問題與後續建議

- [ ] `tools/purge_issues.py` 排進 VM cron(runbook §C.2)
- [ ] runbook「不可逆操作」清單補上 `0007` / `0008` 的 `DROP TABLE`
- [ ] 🐛 登入曾出現 401「授權碼交換失敗」(trace `5983122e`):`app/oidc.py` 只記 status
      不記 IdP 的 `error`;建議另立任務,**裁示前不動工**
- [ ] 🔴 `requirements.txt` 仍是相容區間不是 lock 檔——建置不可重現,建議另立任務做 pip-compile
- 發版待辦:v0.2.3(T89–T96 一版發出)依憲法第八條發版儀式補 Release title/content 後換版

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.0 | 2026-08-26 | Claude(Benny 指示) | 初版:七節總覽,對齊任務表 v1.75 |
