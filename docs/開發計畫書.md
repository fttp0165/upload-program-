# upload-program 開發計畫書

**建立日期:** 2026-07-25 15:53
**最後更新:** 2026-07-31 13:00
**版本:** v1.5

> 本文是 upload-program 的開發藍圖:目標、範圍、里程碑、資料影響分級與回滾原則。
> 任務層級的追蹤見 [任務表.md](任務表.md);技術設計見 [設計_MVP.md](設計_MVP.md)。
> 需求變更**不直接改程式**——先改本文與任務表,再依新任務執行(憲法第一條 4)。

---

## 1. 目標與範圍

### 1.1 產品目標

公司內部的**開發者程式分享平台**——「簡易型 GitHub」。開發者登入後:

1. **建立專案**(一個程式/工具 = 一個專案)
2. **開啟專案**後上傳三類內容:**原始碼**、**執行檔**、**程式文件**
3. 以**版本**為單位發布,他人可瀏覽、搜尋、下載

### 1.2 明確不做(MVP 範圍界線)

| 不做 | 理由 |
|---|---|
| git 協定(`git clone` / `push`)、commit 圖、diff | GitHub 的核心難點;本專案以「版本化的檔案集」取代 |
| Pull Request / code review / issue 追蹤 | 第二期再議 |
| 線上編輯、CI 建置 | 上傳既有產物即可 |
| 外網公開 | 內部平台,登入才可見 |

> 一句話界線:**做的是「有版本管理的程式發布倉庫」,不是分散式版本控制系統。**

### 1.3 非功能目標

| 面向 | 目標 |
|---|---|
| 安全 | 散布可執行檔的平台——判型、雜湊、強制 attachment 下載為不可退讓項 |
| 容量 | 單檔上限 **100 MB**;專案總容量兩級距——標準 **2 GB**(預設)/ 擴充 **10 GB**(需申請) |
| 可用 | `/health` 60 秒內綠;換版不中斷既有請求(SIGTERM 30 秒優雅關閉) |
| 規約 | 通過 Cats 接入指南 §11 的 Definition of Done 全部檢查項 |
| 可維運 | 備份**且演練過還原**;migration 可雙向回滾 |

---

## 2. 系統架構

```
                        內網使用者
                            │ HTTPS 443
                  ┌─────────▼──────────┐
                  │   portal-gateway   │  全 VM 唯一持 80/443,依路徑前綴分流
                  └─────────┬──────────┘
                    external network: cats-edge
                            │  /«PREFIX»/(前綴會被剝掉)
        ┌───────────────────▼────────────────────┐
        │  upload-program stack                  │
        │  ┌──────────────────────────────────┐  │
        │  │ svc:8080(對外容器,FastAPI)     │  │  ← 只有它上 cats-edge
        │  └────────┬──────────────┬──────────┘  │
        │           │ backend      │ backend     │
        │  ┌────────▼───────┐ ┌────▼──────────┐  │
        │  │ db(PostgreSQL)│ │ minio(S3 相容)│  │  ← 不曝 port、不上 cats-edge
        │  │ volume: pgdata │ │ volume: data  │  │
        │  └────────────────┘ └───────────────┘  │
        └────────────────────────────────────────┘
                            │
                    ┌───────▼────────┐
                    │ Keycloak(IdP) │  只驗 token,不自建帳號
                    └────────────────┘
```
<!--SVG:architecture-->

**兩個由拓撲推導出的關鍵決定:**

1. **瀏覽器不直連 MinIO**——它在 `backend` 網路、不曝 port,且全 VM 只有 gateway 持 443。
   因此**上傳與下載一律由 svc 串流轉送**,不用 presigned 直傳。
2. **服務掛在子路徑且前綴被剝掉**——路由註冊在根路徑,只用 `root_path` 讓 OpenAPI 正確;
   對外絕對網址(OIDC redirect、cookie path)一律從設定組出,不從 request 推導。

---

## 3. 里程碑

| 里程碑 | 目標 | 出場條件(Exit Criteria) |
|---|---|---|
| **M1 規則與計畫** | 憲法、計畫書、任務表、設計文件到位 | 四份文件皆有版本歷史;md + HTML 並存 |
| **M2 骨架與規約落地** | 設定 / log / 錯誤格式 / DB / 模型 / 容器 / CI | `/health` 綠、CI 全綠、Trivy 無 high|critical |
| **M3 身分接入** | OIDC PKCE、JWKS、待開通、single logout | 冒煙清單前 5 項通過(用測試帳號) |
| **M4 核心業務** | 專案 / 版本 / 成員 / 搜尋 | 權限矩陣測試全綠 |
| **M5 檔案** | 串流上傳、magic bytes、下載、配額 | 惡意檔案測試全綠;大檔實測通過 |
| **M6 接入 Cats** | 申請 client、路由 PR、驗收、備份還原演練、稽核紀錄 | 接入指南 §11 DoD 全數打勾;SSO 部分見 [plans/SSO接入計畫.md](plans/SSO接入計畫.md) |
| **M7 前端** | 主頁、導航列、專案頁(最新版置頂)、專案歷史、上傳與管理介面 | 使用者不需 curl 即可完成一輪上傳與下載 |
| **M8 第二期** | 病毒掃描接上;其餘 P2 項目 | 另行規劃,不納入 MVP |

**目前位置(2026-07-31):** M1–M5、M7 **全數完成**;M6 僅餘 T27 還原演練(待 staging)。
**系統已於 2026-07-29 正式上線**(v0.1.0,同日 v0.1.1),為平台第一個完成 SSO
端到端接入並上線的 App(契約 §9 登記);v0.1.3(T57–T63 + T59)已備妥待發。
早期「程式碼先行違反第二、三條」的欠項已全數清償(T50,詳見 T00 日誌)。

---

## 4. 對現有資料的影響分級

**2026-07-29 上線後本表已生效**:任務表每項任務都標分級;🔴 級動手前必先備份(backup.sh 已上線)。

| 分級 | 定義 | 要求 |
|---|---|---|
| 🟢 不動資料 | 只加程式 / 設定 / 文件 | 一般流程 |
| 🟡 加欄位、加表 | 結構新增,既有列不變 | migration 要 backward;staging 演練 |
| 🔴 UPDATE / 刪除 / 型別變更 | 既有資料會被改寫 | **動手前備份** + staging 雙向演練 + 離峰執行 + 估時 |

---

## 5. 回滾原則

| 層級 | 回滾方式 |
|---|---|
| 程式 | 改 compose 的 image tag 回舊版 → `docker compose up -d` → **reload portal-gateway** |
| 資料 | migration 的 `downgrade()`;🔴 級任務另需備份還原程序 |
| 路由 | 回退 cats-portal 的 gateway 設定 PR → `nginx -t` → `nginx -s reload` |
| 文件 | git revert;版本歷史保留錯誤紀錄,不抹除 |

**換版不動 DB**:正常升版只換 image tag,資料活在 named volume 裡。

---

## 6. 待平台方分配(不得自選)

| 項目 | 值 | 狀態 |
|---|---|---|
| 服務短名 `«NAME»` | `upload-program` | ✅ 定案(施工單 §3 容器名硬性約束)|
| 路徑前綴 `«PREFIX»` | `/upload/` | ✅ **portal 裁決函(2026-07-29)定案** |
| cats-edge 別名 | `upload-program` | ✅ 已與路徑前綴一併定案 |
| GHCR image | `ghcr.io/fttp0165/upload-program` | ✅ 使用中(v0.1.0 / v0.1.1 已發布)|
| 資料庫 / 使用者 | `upload_program_db` / `upload_program_user` | ✅ 依平台規約命名表自行對齊(尚未上線,已改) |
| SSO client_id / secret | `upload-program` | ✅ 已建立並交付,2026-07-29 正式環境登入實測通過 |
| gateway `client_max_body_size` | **≥ 128 MB**(單檔上限 100 MB + 餘裕) | ✅ portal 已設 128MB 併入權威設定 |
| 🔴 第一個管理員的 `sub` | Benny(已開通) | ✅ 2026-07-29 解決;T55 修正後 bootstrap 清單對既有 pending 帳號也生效,後續管理員可走清單或後台指派 |

> repo 名 `upload-program-`(尾端多一個連字號)不符命名規約的 `service-<name>`,
> 是否改名由 Platform 決定,本專案不自行更名(portal 裁決函與施工單皆確認此為
> **非 portal 權責**,已轉交 Platform)。

---

## 7. 接入前的八個問題(接入指南 §10)

| # | 問題 | 本專案的答案 |
|---|---|---|
| 1 | 語言 / 框架? | Python 3.12 + FastAPI(gunicorn + UvicornWorker) |
| 2 | 容器內部 port? | **8080**,綁 `0.0.0.0` |
| 3 | 要不要自己的 DB?資料量級? | 要:PostgreSQL **15**(全平台統一 PG15,施工單 §3;原規劃 16,2026-07-29 對齊)。metadata 量小(千筆級);檔案在 MinIO,初估數十 GB |
| 4 | 健康檢查路徑? | `/health`(liveness,不查 DB)+ `/ready`(查 DB / MinIO / JWKS) |
| 5 | **能不能在子路徑下運作?** | **能**。FastAPI `root_path` + 對外網址由設定組出;gateway 可**剝前綴** |
| 6 | 靜態檔由誰供應? | svc 自己供應 `/static/`(M7 起:app.css、upload.js、copy.js、自託管 Bootstrap vendor);gateway 有獨立 `/upload/static/` location |
| 7 | 要存哪些使用者相關欄位? | `sub` 為唯一身分欄位;無 email / 密碼。2026-07-30 起依契約 **§4.2a L1** 增顯示名稱快取(僅 `name` claim、僅後台顯示、可清除,T59)|
| 8 | 路徑前綴偏好? | `/upload/`(最終由平台方定) |

> 第 5 題有一處要向 portal 確認:SSO 契約 §4.1 寫 redirect URI 固定為
> `https://<hostname>/oidc/callback/`,但在子路徑部署下實際會是
> `https://catsapp.sporton.com.tw/«PREFIX»/oidc/callback/`。申請 client 時需一併登記後者。

---

## 8. 已知風險

| 風險 | 影響 | 對策 |
|---|---|---|
| **未接掃毒** | 平台可能成為惡意執行檔散布管道 | 2026-07-26 定案:**不納入 MVP,先預留接點**(F38 / M8)。期間 `scan_status` 一律誠實標 `not_scanned` 並隨下載回傳 |
| 大檔經 svc 串流 | 佔用 worker 與 gateway 逾時 | 單檔上限降為 100 MB 後風險大幅降低;仍維持 multipart 串流(記憶體固定)並調 gateway `client_max_body_size` 與逾時 |
| MVP 範圍因 Q1–Q8 定案而擴大 | M4–M7 工作量增加(新增 F16、F26、F42、F43、F54 + 前端 7 項) | 已排入任務表 T33–T45;若時程吃緊,F42 標籤與 F43 統計是最容易延後的兩項 |
| 換版後 502 | 服務中斷 | 換版 runbook 強制 `disconnect → up → reload gateway`(T22) |
| MinIO 單點 | 檔案不可用 | 資料在 named volume;備份與還原演練列為 M6 出場條件(T23) |
| 磁碟隨時間線性成長 | 共用 VM 空間被吃光,影響 portal / PLM / AES_KEY | 標準級距壓在 2 GB;⏳ **舊版本清理策略尚未定案(Q13)——上限只是延後爆炸,清理才是控制手段**;另需磁碟監控告警(M6) |
| ~~事後補記的程式碼未經 TDD~~ | — | ✅ 已清償(T50);其後任務均先紅後綠 |
| 共用 VM 上的 self-hosted CI runner | build/Trivy 與正式服務搶資源;runner 目錄吃磁碟 | 單 runner 天生序列化;首次全量低峰;磁碟納入監控;詳見 2026-07-31 CI 日誌 |

---

## 版本歷史

| 版本 | 日期 | 修改人 | 摘要 |
|---|---|---|---|
| v1.5 | 2026-07-31 13:00 | Claude(Benny 授權) | **上線後現況回寫(review)**:§3 目前位置改為「M1–M5、M7 完成,M6 餘 T27;已上線 v0.1.1、v0.1.3 待發」;§4 上線後分級生效;§6 全數定案(短名/GHCR/SSO/第一管理員 ✅);§7 八問第 6 題(靜態檔已有)與第 7 題(§4.2a 顯示名稱快取)更新;§8 風險表:TDD 欠項已清償、新增 self-hosted runner 風險 |
| v1.4 | 2026-07-29 07:30 | Claude(Benny 授權) | §6 SSO client 狀態更新為「已建立、secret 待交付」;依施工單 v1.1 移除「待 Keycloak 部署」的錯誤前提 |
| v1.3 | 2026-07-29 03:20 | Claude(Benny 授權) | §6 依 portal 裁決函與施工單(2026-07-29)回寫:路徑前綴 `/upload/` **定案**、`client_max_body_size` 128MB **已設定**、資料庫/使用者名改為符合平台規約命名表的 `upload_program_db`/`upload_program_user`(尚未上線,現在改);SSO client 狀態改為「已受理,待 Keycloak 部署」 |
| v1.1 | 2026-07-28 02:25 | Claude(Benny 授權) | §6 待平台方分配補上 **🔴 第一個管理員的 `sub`**(沒有它首登全是零角色、沒人能開通任何人,系統鎖死);M6 出場條件掛上 SSO 接入計畫指標 |
| v1.0 | 2026-07-25 15:53 | Claude(Benny 授權) | 初版:目標與範圍界線、系統架構(含子路徑與不直連 MinIO 兩項推導)、M1–M7 里程碑與出場條件、資料影響分級、回滾原則、待平台分配清單、接入指南 §10 八問答覆、已知風險 |
| v1.2 | 2026-07-26 00:53 | Claude(Benny 裁示) | Q10 定案回寫:容量目標改為兩級距(標準 2 GB / 擴充 10 GB);風險表新增「磁碟隨時間線性成長」一列,點出舊版本清理策略(Q13)未定 |
| v1.1 | 2026-07-26 00:28 | Claude(Benny 裁示) | 依功能需求大綱 v2.0 的 Q1–Q8 定案回寫:新增 **M8 第二期**(掃毒);M6 納入稽核紀錄、M7 明確化前端範圍;非功能目標新增容量(單檔 100 MB);gateway `client_max_body_size` 由 ≥512 MB 改為 **≥128 MB**;風險表更新掃毒與大檔兩列,並新增「MVP 範圍擴大」一列 |
