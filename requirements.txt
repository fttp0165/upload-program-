# 執行期相依。版本以相容區間表示;正式發版前應改為 lock 檔(pip-compile)以求可重現建置。
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
gunicorn>=23.0,<24.0
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<0.31
alembic>=1.13,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
pyjwt[crypto]>=2.9,<3.0
httpx>=0.27,<1.0
# 非同步 S3/MinIO client:上傳走 multipart 串流(邊收邊算 SHA-256,不落暫存檔)
aioboto3>=13.0,<16.0
itsdangerous>=2.2,<3.0
# 網頁模板(T47 錯誤頁,T40 起的前端版型):autoescape 是選它而非字串拼接的主因
jinja2>=3.1,<4.0
# 解析 HTML 表單的 urlencoded body(T44 的建專案/建版本/發布表單)。
# 🔴 這**不是**回頭用 multipart 傳檔——檔案上傳仍是 raw body PUT 串流,
# 那個決定(不落暫存檔)沒有改變。這個套件只負責表單欄位。
python-multipart>=0.0.9,<0.1
